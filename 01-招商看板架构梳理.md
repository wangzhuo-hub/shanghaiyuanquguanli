# 上海园区招商管理看板 — 架构梳理

本文梳理本应用在**前端 React**与**后端 PocketBase 结构化集合**中的实现形态，为 OpenClaw 从企业微信等渠道读取集合化数据、做应收分析与核销类操作、以及代写合同/收款等提供依据。

## 1. 业务对象定义

核心实体与前端类型、后端集合对应关系如下（**业务主键**为各表中的 `original_id`，与前端 `id` 一致；PocketBase 记录 `id` 为系统主键）。

| 业务含义 | 前端类型（`types.ts`） | PocketBase collection |
|---------|------------------------|------------------------|
| 楼宇/场地 | `Building` | `pb_buildings` |
| 房间/单元 | `Unit`（嵌在 Building 内） | `pb_units` |
| 租户/合同 | `Tenant` | `pb_tenants` |
| 收款流水 | `PaymentRecord` | `pb_payments` |
| 发票 | `InvoiceRecord` | `pb_invoices` |
| 年度目标 | 聚合在 `DashboardData.yearlyTargets` | `pb_yearly_targets` |
| 月度初始化 | `MonthlyInitData` | `pb_monthly_init_data` |
| 预算假设 | `BudgetAssumption` | `pb_budget_assumptions` |
| 预算调整（含缓缴等） | `BudgetAdjustment` | `pb_budget_adjustments` |
| 预算方案快照 | `BudgetScenario` | `pb_budget_scenarios` |
| 租金账期备注 | `billingPeriodNotes`（`Record<string,string>`） | `pb_billing_period_notes` |
| 历史整包备份（JSON Blob） | — | `park_backups`（兼容/备份，**分析应收建议以结构化集合为准**） |

**多项目隔离**：上述结构化集合均带 `project_id`（与系统设置中的「项目 ID」一致，默认常为 `park_data_main`）。OpenClaw 查询与写入必须**始终带同一 `project_id`**，否则与看板数据不一致。

### 1.1 Tenant（合同/租户）核心字段（前端 ↔ 后端字段名）

前端 `Tenant` 与 `pb_tenants` 为同一业务对象；后端为 **snake_case**，见 `services/pocketbaseService.ts` 中 `mapRecordToTenant` 与 `syncToStructuredCollections` 映射。

要点：

- **building_id**：须为已存在的 `pb_buildings.original_id`
- **unit_ids**：`pb_units.original_id` 数组；房间状态与合同联动由前端业务逻辑维护
- **lease_start / lease_end**：必填（租期）
- **status**：`Active` / `Expiring` / `Terminated` / `Pending` / `Expired`（与 `ContractStatus` 一致）
- **root_id**：续签链关联上一份合同的 `original_id`
- **payment_cycle**：`Monthly` | `Quarterly` | `SemiAnnual` | `Annual`
- **free_rent_handling**：`Deduct` | `Defer`（免租处理方式）
- **rent_free_periods**、**key_moments**：JSON 数组

### 1.2 PaymentRecord（收款）与应收核销

定义见 `types.ts`；持久化在 `pb_payments`。

- **type**：`Rent` | `Deposit` | `ManagementFee` | `ParkingFee` | `Other` | `DepositToRent` | `DepositRefund`
- **计入某月租金应收**：仅 **`Rent` 与 `DepositToRent`** 参与租金类核销（见 `services/billingService.ts` 中 `paymentMatchesRentBillingPeriod`）
- **period**（可选，建议 YYYY-MM）：若填写，则**按账期**匹配应收；可与 **date（入账日）** 跨月（预收、跨期核销）。未填则按 `date` 的年月匹配（兼容旧数据）
- **核销匹配租户**：系统**按租户 id** 匹配，不按名称；续签后存在 **rootId 链**，流水可能挂在最新合同 id，而预算快照应收行仍为旧 id，需用 `paymentTenantMatchesBillingTenant` 同链规则（OpenClaw 侧应复现或调用网关封装该逻辑）

### 1.3 租金账期备注

- 集合 `pb_billing_period_notes` 通常**单条**记录：`original_id = "billing_period_notes"`，`notes_json` 为对象，键格式 **`tenantId###YYYY-MM`**，值为备注字符串。

## 2. 前端模块与交互入口

### 2.1 数据加载与保存总线

- **加载**：`services/pocketbaseService.ts` 的 `loadStructuredData(projectId)` 并行拉取各 `pb_*` 集合并组装为 `DashboardData`（含 `billingPeriodNotes`）。
- **保存**：`syncToStructuredCollections(data, projectId, pendingDeleteIds?)` 对各类记录 **upsert**（按 `original_id` + `project_id`），并仅在 `pendingDeleteIds` 中显式声明时删除对应记录，避免误删。

上层封装见 `services/cloudService.ts`（与看板「保存到云端」一致）。

### 2.2 主要 UI 与职责

- **`App.tsx`**：Tab 切换（看板、合同、财务、楼宇等），持有 `DashboardData`，调用加载/同步与增量更新（如 `updateTenants`、`updatePayments`）。
- **`components/ContractManager.tsx`**：合同/租户列表与编辑，变更通过回调回写内存态并最终同步 PocketBase。
- **`components/FinanceManager.tsx`**：收款、发票等与财务相关维护。
- **`components/BillingTable.tsx`**：应收视图、账期、缓缴/备注等（依赖 `billingService` 与 `payments`、`tenants`、预算调整等）。
- **`components/BuildingManager.tsx`**：楼宇与单元。
- **`components/AIAssistantDialog.tsx` / `AIContractRecognitionModal.tsx` / `AIPaymentRecognitionModal.tsx`**：对话与识别辅助填表；**不替代** PocketBase 持久化，落库仍走上述同步路径。
- **本地 AI 代理**：根目录 `ai-proxy.mjs` 将前端对话请求转发至配置的聊天 API（与 OpenClaw 为**不同通道**；OpenClaw 直连 PocketBase 或经网关即可）。

## 3. 后端（PocketBase）结构与读写路径

### 3.1 集合清单与权威描述

结构化 schema 说明见 **`pocketbase/schema.json`**（含索引与字段注释）。与实现对齐的集合名：

`pb_buildings`、`pb_units`、`pb_tenants`、`pb_payments`、`pb_invoices`、`pb_yearly_targets`、`pb_monthly_init_data`、`pb_budget_assumptions`、`pb_budget_adjustments`、`pb_budget_scenarios`、`pb_billing_period_notes`。

（`pb_budget_adjustments` 在同步逻辑中写入 **`adjustment_type`** 等字段；若自建库需与迁移文件保持一致。）

### 3.2 REST 读取（OpenClaw / 外部分析）

标准 PocketBase REST：

- 列表：`GET {PB_BASE}/api/collections/{collection}/records?filter=...&perPage=...&sort=...`
- 单条：`GET {PB_BASE}/api/collections/{records}/{id}`

**精准分析建议**：

1. 按 `project_id` 过滤。
2. 使用 `fields` 减少 payload。
3. 大表（租户、收款）使用分页或 `getFullList` 等价循环；前端为避免不一致使用 `getFullList`。
4. 应收/欠费逻辑不要只读 `park_backups` 大 JSON，应以 **`pb_tenants` + `pb_payments` + 预算相关表 + `pb_billing_period_notes`** 计算或与 `billingService` 规则对齐。

### 3.3 写入与一致性问题

- 前端写入统一走 **已认证的 PocketBase SDK**（`authenticatePocketBase` 支持管理员或 `users` 集合）。
- **直写 REST** 时须满足集合 **API Rules**；规则过宽时任意客户端可改数据。
- **合同 + 房间状态**：仅写 `pb_tenants` 而不更新 `pb_units.status`，可能导致看板房源状态与合同不一致；OpenClaw 代录入时应一并约定房间占用规则（或走网关事务化）。

## 4. 对「外部自然语言操作」的约束点

当由 OpenClaw 从企业微信等渠道代操作时需特别注意：

- **project_id**：必须固定为业务约定的项目标识。
- **租户标识**：核销、关联应收应以 **`tenant_id`（即 `original_id`）** 为准；名称仅作展示与检索，**不能**作为唯一核销键。
- **账期**：租金核销建议明确 **YYYY-MM** 写入 `period`，避免与入账日混淆。
- **幂等**：当前 schema **无** 通用 `external_id` 字段；企微重试易导致重复收款记录，宜由**网关**去重或扩展字段。
- **删除**：前端依赖 `pendingDeleteIds` 显式删除；OpenClaw 不应随意批量删除，除非业务确认。

> 以上约束在《OpenClaw 招商看板接入规范》中给出推荐 JSON 与网关方案。
