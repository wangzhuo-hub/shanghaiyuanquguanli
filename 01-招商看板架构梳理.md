# 招商管理看板 - 架构梳理（按当前代码）

本文按当前仓库实现梳理前后端真实架构，聚焦生产主链路：**PocketBase `pb_*` 结构化集合**。部署上可为 **多园区**（上海 / 深圳 / 北京等）：同一 PocketBase 实例、同一套 REST 路径，靠 **`project_id` + 用户授权字段 + API Rules** 隔离；详见 **`docs/04-多园区登录与Tailscale部署.md`**。

迁移 `1772000000_drop_park_backups.js` 已从标准库中**删除**遗留集合 `park_backups`；冷启动与运行时只认 `pb_*`，历史整包数据通过 **JSON 文件 + `scripts/migrate-json-to-pb.mjs`** 等运维路径导入。

## 1. 当前运行态（一句话）

- 前端是 `React + TypeScript + Vite` 单页应用。
- 后端为 **PocketBase**；业务数据的**权威存储**是 `pb_*` 集合（按业务对象分表，带 `project_id` 隔离）。
- 前端**加载**时从 `pb_*` 聚合为内存中的 `DashboardData`；**保存**时把当前看板状态拆分写回各 `pb_*` 表。
- 某 `project_id` 下若尚无业务行，拉取结果即为空看板；需由运维用 JSON 导入脚本灌入 `pb_*`，**不再**存在从库内 `park_backups` 自动迁移的前端逻辑。

## 2. 前端架构现状

### 2.1 技术栈与入口

- 入口：`App.tsx`
- 组件：`components/*`（含 `BudgetManager.tsx`、`TenantInsights.tsx`、`DashboardAlerts.tsx` 等）
- 业务类型：`types.ts`
- 数据计算：`services/billingService.ts`、`services/receivableListHelpers.ts`；OpenClaw 只读 KPI 聚合见 `services/openclawKpiSnapshot.ts`（`buildOpenClawKpiSnapshot`）。
- 云端封装：`services/cloudService.ts` → `services/pocketbaseService.ts`（直连 PocketBase，无独立 BFF）
- 部署默认连接：`config/deploymentDefaults.ts`（可被 `localStorage` 与 `VITE_POCKETBASE_*` 环境变量覆盖）
- 增量保存与脏跟踪：`services/dirtyTracker.ts`、`services/dirtyTrackerContext.tsx`、`services/dataDiff.ts`；冲突 UI：`components/ConflictDialog.tsx`

### 2.2 模块分工

- `App.tsx`：状态中枢、Tab 路由、云端初始化、从结构化数据加载/保存、本地 `localStorage` 草稿；指标重算时用全年 `calculateTrends('All')` 构建 KPI 并**防抖**调度写入 PocketBase（见下「集成快照 / KPI 年快照」）。**集团视图**：`platform_admin` / `group_admin` 等可在工作台顶部展示 **「所有园区经营汇总」**，按园区拉取当前内存数据、`pb_kpi_snapshots` 或备份/cache 聚合指标（`services/dashboardMetrics.ts` → `buildKpiSummaryFromProcessedData`、`normalizeKpiSummaryWithMonthlyTrends`）。多园区按园区拆分的明细表**不展示「预算完成」列**（与顶部 KPI 卡片中的预算执行类指标区分）。
- `BuildingManager.tsx`：楼宇与单元维护。
- `ContractManager.tsx`：合同/租户维护。
- `FinanceManager.tsx`：收款、发票、账务操作。
- `BudgetManager.tsx`：预算假设、调整、场景。
- `BillingTable.tsx`：应收视图与账期展示。
- `AIAssistantDialog.tsx` / `AssistantPanel.tsx`：AI 对话入口（不替代持久化）。

### 2.3 保存与加载链路（当前生效）

| 环节 | 行为 |
|------|------|
| 初始化 | `initCloud` → 初始化 PocketBase 客户端，用户登录态见 `users`（含 `project_id`、`allowed_project_ids`、`role`）。 |
| 加载 | `fetchCloudBackup` → `fetchPocketBaseBackup`：按 `project_id` 从各 `pb_*` 读全量并组装 `DashboardData`（含 `pb_billing_period_notes` 中的账期备注与 `cloudSaveVersion`）。**不**把集成快照装入 `DashboardData`；`pb_integration_snapshots` 仅供 OpenClaw / 外部系统只读拉取。 |
| 保存（主路径） | **`saveIncrementalToCloud`** → **`saveIncrementalToPocketBase`**：按 **DirtyPayload** 增量写入各 `pb_*`，提交前与服务端 **`recordMeta`（行级 `updated`）** 比对，冲突则进入 `ConflictDialog`。**运维/特殊场景**仍可选用历史 **`saveToPocketBase`**（整表 replace 策略，见 `pocketbaseService` 注释）。`pb_billing_period_notes` 中 **`dashboard_data_version`** 等与云端版本协同。 |
| 保存成功后 | 拉最新快照作 baseline（`captureBaselineFromCloud`）；可选 **`upsertCloudKpiSnapshot`** 写入 **`pb_kpi_snapshots`**（按 `project_id` + `year` 唯一，含 `summary_json`、`monthly_trends_json`）。 |
| 全量集成快照（OpenClaw / 外部） | `recalculateMetrics` 完成后：若已配置 `project_id`，则 `buildIntegrationFullSnapshotV1` + `scheduleUpsertIntegrationFullSnapshot`，约 **1.6s 防抖** upsert 至 `pb_integration_snapshots`（`snapshot_kind = full_dashboard_v1`）。`payload` 含与界面一致的 `dashboard`、`full_year_monthly_trends` 及 **`kpi`**。**不**递增 `dashboard_data_version`。 |
| 「历史」列表 UI | `getCloudHistory` → `getPocketBaseHistory`：从 `pb_billing_period_notes` 中 `original_id = "last_save_note"` 读取最近保存备注与时间。 |

说明：本地编辑仍可先落在 `localStorage`（按 `project_id` 分 key），用户点击保存后才触发结构化落库。

## 3. 后端架构现状（PocketBase）

### 3.1 主集合（`pb_*`，与前端读写一致）

实现见 `services/pocketbaseService.ts` 的 `saveToPocketBase` / `fetchPocketBaseBackup`：

- `pb_buildings`、`pb_units`、`pb_tenants`
- `pb_payments`、`pb_invoices`
- `pb_yearly_targets`、`pb_monthly_init_data`
- `pb_budget_assumptions`、`pb_budget_adjustments`、`pb_budget_scenarios`
- `pb_billing_period_notes`：同集合内用 `original_id` 区分用途——`billing_period_notes`（账期备注 JSON）、`last_save_note`（最近保存备注/时间）、`dashboard_data_version`（`notes_json.version` 整数，供保存前乐观并发）。历史环境可能存在 `openclaw_kpi_snapshot`，**新链路**已迁至 `pb_integration_snapshots`。
- `pb_integration_snapshots`：`project_id` + `snapshot_kind`（如 `full_dashboard_v1`）唯一；`payload` 为全量 JSON（含 `kpi` 子对象）。迁移 `1772100000_created_pb_integration_snapshots.js`。
- **`pb_kpi_snapshots`**：`project_id` + `year` 唯一；存精简 **`summary_json`**（年度目标/实收/预算完成率/出租率等）与可选 **`monthly_trends_json`**。用于保存成功后的年维度 KPI、管理员多园区汇总拉取、减轻重复计算。迁移 `1773100000_created_pb_kpi_snapshots.js`。
- **`pb_parks`**：园区目录（`project_id`、名称、启用等）。迁移 `1773000000_multi_park_auth_isolation.js`。
- **`users`（认证）**：扩展字段 **`project_id`**、**`allowed_project_ids`**（JSON 数组）、**`role`** 等，与集合 **API Rules** 联动实现多园区读写边界。

约定：

- 每条业务记录带 `project_id`（与前端 `CloudConfig.projectId` 一致，默认常见值 `park_data_main`）。
- 业务主键在库中多为 `original_id`（对应应用内 `id`），与 PocketBase 记录 `id` 不同。

### 3.2 已移除集合：`park_backups`

- 历史设计：单条大字段 `data` 存整包 `DashboardData` JSON。
- **标准迁移链**在 `pocketbase/pb_migrations/1772000000_drop_park_backups.js` 中删除该集合；跑完迁移的实例上 **OpenClaw 与前端均不应再查询或写入** `park_backups`。
- 若仍需从旧整包恢复：使用 **JSON 备份** 与 `scripts/migrate-json-to-pb.mjs` 写入 `pb_*`（脚本说明已明确不再从 `park_backups` 读取）。

### 3.3 迁移与运维脚本

- 结构化表创建：`pocketbase/pb_migrations/1770200000_created_structured_collections.js`
- 后续字段/集合增量：`1771200000_add_move_in_date_to_tenants.js`、`1771300000_created_pb_billing_period_notes.js`、`1771400000_add_payment_terms_to_tenants.js`、`1771500000_add_adjustment_kind_to_budget_adjustments.js`、`1771600000_add_budget_year_to_budget_scenarios.js`
- 删除遗留整包集合：`1772000000_drop_park_backups.js`
- 集成全量快照集合：`1772100000_created_pb_integration_snapshots.js`
- **多园区认证与 `pb_parks`：`1773000000_multi_park_auth_isolation.js`**
- **KPI 年快照 `pb_kpi_snapshots`：`1773100000_created_pb_kpi_snapshots.js`**
- **收款周期等扩展：`1773200000_expand_payment_cycles.js`**；**租户首期应收字段：`1773300000_add_first_receivable_fields_to_tenants.js`**
- 集合脚手架：`scripts/setup-pb-collections.mjs`
- 从 **JSON 备份文件** 导入结构化表：`scripts/migrate-json-to-pb.mjs`（运维/迁移用，**不是**运行时主路径）
- 多园区初始化示例：`scripts/init-multi-park.mjs`（参见 **`docs/04-多园区登录与Tailscale部署.md`**）

## 4. 业务对象口径（以 `types.ts` 为准）

- 合同：`Tenant`（含 `rootId`、`paymentCycle`、`paymentTerms`、`freeRentHandling`、`moveInDate` 等）
- 收款：`PaymentRecord`（含 `type`、`date`、`period`）
- 预算：`BudgetAssumption`、`BudgetAdjustment`、`BudgetScenario`（含 `budgetYear`、`adjustmentKind` 等）
- 账期备注：`DashboardData.billingPeriodNotes`（键常为 `tenantId###YYYY-MM`），持久化在 `pb_billing_period_notes` 中 `original_id = "billing_period_notes"` 的 `notes_json`
- 云端保存版本：`DashboardData.cloudSaveVersion` ↔ `pb_billing_period_notes` 中 `original_id = "dashboard_data_version"` 的 `notes_json.version`
- 集成全量快照：非 `DashboardData` 字段；由前端写入 `pb_integration_snapshots`；`payload.kpi` 与 `services/openclawKpiSnapshot.ts` 中 `OpenClawKpiSnapshot` 一致，详见 `services/integrationSnapshot.ts`
- 首页「预算执行」中的 **月度预算收入**（`monthlyTrends[].revenueTarget`，界面以万元展示）：**非单表字段**；口径由 `services/dashboardMetrics.ts` → **`calculateTrends`**（经 `recalculateMetrics` / `calculateDashboardMetrics`）统一产出，**与 `pb_invoices` 无关**。优先级如下（与 `components/BudgetManager.tsx` Excel 导入、`services/budgetTableImport.ts` 一致）：
  1. **已导入的生效预算表（最高）**：若 `DashboardData.billingPeriodNotes`（持久化在 `pb_billing_period_notes` 的 `billing_period_notes` 整包 JSON）中存在键 **`__budget_table_<统计年>__`**，解析为 `BudgetTableSnapshot`，则该年 **每月** `revenueTarget` **严格等于** 快照中的 **`monthlyTotals[0..11]`**（单位：**元**）。此时**不再**用计费引擎对当月 `amountDue` 求和覆盖，即使当年存在 **`is_active` 生效预算方案**。
  2. **无导入表时**：按 **当年生效预算方案**（`pb_budget_scenarios`：`is_active` + 年份匹配；注意库中 **`budget_year = 0` 在前端按「当前系统年」参与匹配**）的 `base_data_snapshot` + `assumptions` / `adjustments`，用与租户应收相同的计费内核汇总当月 **`amountDue`**。
  3. **无导入表且无生效方案上下文时**：可用 **`pb_monthly_init_data`** 中对应 `year`/`month` 的 **`revenue_target`** 覆盖当月预算收入（与代码中 `!importedBudgetTable && !budgetContext && initEntry` 分支一致）。
  快照租户缺月租等字段时，OpenClaw 在无导入表路径下应用当前 **`pb_tenants` 合并补齐**后再跑同款计费逻辑，禁止改走发票。详见 `02-OpenClaw招商看板接入规范.md` **§3.6**。
- **年度营收目标 vs 月度滚动预算**：`DashboardData.annualRevenueTarget` 来自 **`pb_yearly_targets`（手工年度指标）**；月度趋势汇总与 **`pb_kpi_snapshots` / 管理员汇总卡片** 展示时，若年度指标未维护，前端 **`buildKpiSummaryFromProcessedData`** 会 **回退为 `monthlyTrends` 应收目标合计**，避免「财务列为 0、预算分母仍含该园区」的展示不一致；OpenClaw 解析 **`payload.kpi`** 时应对照 **`annual_revenue_goal_yuan` 与 `annual_budget_revenue_sum_yuan`** 字段含义（见规范 §3.5.2）。
- **工作台数字格式**：首页三块区域——**所有园区经营汇总**（管理员）、**预算执行 (Budget vs Actual)**、**年度经营指标对比**——万元与百分比在 UI 上 **取整、不保留小数**；财务等其它页仍默认 **两位小数**（`services/numberFormat.ts`）。

应收分类与核销口径在 `services/receivableListHelpers.ts`：

- `paymentMatchesRentBillingPeriod()`
- `paymentTenantMatchesBillingTenant()`

OpenClaw 或外部分析需与上述规则对齐。

## 5. OpenClaw / 外部接入影响（摘要）

1. **读**：以 `pb_*` + `project_id` 为数据源；轻量 KPI 可 **`GET pb_kpi_snapshots`**（按 `project_id` + `year`）；整板同源可 **单条 `GET` `pb_integration_snapshots`**（`snapshot_kind=full_dashboard_v1`）使用 `payload.dashboard` / `payload.kpi`，或在网关侧按 `fetchPocketBaseBackup` 同构逻辑聚合。**经营类日报**可只解析 `payload.kpi`。
2. **写**：向对应 `pb_*` 写入；注意与前端 **增量保存 + `recordMeta` / `dashboard_data_version`** 的并发关系（见 `02-OpenClaw招商看板接入规范.md`）。集成快照与 KPI 年快照由前端在保存/重算路径维护；外部系统 **不必** PATCH `pb_integration_snapshots` / `pb_kpi_snapshots`（除非单独约定服务端任务）。
3. **项目隔离**：每条请求必须在 Rules 允许范围内绑定 **`project_id`**；多对接方可使用 **不同 `users` 账号**（`allowed_project_ids` 限定园区）实现「分园区读写」，无需部署多套 PocketBase API 域名（详见规范 §2）。
4. **勿依赖 JSON 文件**：运行时数据应来自 PocketBase 结构化集合；JSON 仅用于运维一次性导入或离线备份。

## 6. 风险与建议

- **并发保存**：主路径为 **增量写入 + 行级版本比对**；`dashboard_data_version` 等与 Notes 集合协同。外部系统经 REST 写入若**未** bump 版本或合并 `recordMeta`，仍可能与浏览器缓存状态冲突。需协调刷新、网关读改写合并。
- **幂等**：结构化表未统一 `external_id`；外部重试可能产生重复业务行，建议在网关层做幂等键与去重。
- **安全**：默认配置与示例密码仅作开发便利；生产应使用环境变量、收紧 API Rules，且勿将管理员凭据暴露给模型上下文。
- **集成快照新鲜度**：快照在前端完成指标重算且 PocketBase 写成功时更新（防抖）；长期无人打开看板则 `updated` 可能偏旧。若要求无人值守也每日刷新，需在网关或定时任务侧补充策略（本仓库当前未含独立服务端定时器）。

建议演进方向：网关统一鉴权/审计/幂等；必要时增加变更流水或版本快照，与「全量 replace」解耦。

## 7. 实现索引（对照代码）

| 主题 | 文件与符号 |
|------|------------|
| 结构化读/写 | `services/pocketbaseService.ts` → `fetchPocketBaseBackup`、`saveIncrementalToPocketBase`（主）、`saveToPocketBase`（兼容/运维） |
| 云端入口 | `services/cloudService.ts` |
| 看板指标与趋势 | `services/dashboardMetrics.ts` → `calculateDashboardMetrics` / `calculateTrends`；`App.tsx` → `recalculateMetrics`（与上同源的 `calculateTrends` 逻辑） |
| 预算表 Excel 导入与 `__budget_table_<年>__` | `services/budgetTableImport.ts` → `readImportedBudgetTable`、`mergeBudgetTotalsIntoInitData`；`components/BudgetManager.tsx` 导入确认写入 `billingPeriodNotes` |
| KPI 汇总（年度指标回退） | `services/dashboardMetrics.ts` → `buildKpiSummaryFromProcessedData`、`normalizeKpiSummaryWithMonthlyTrends` |
| KPI 年快照 PB | `services/pocketbaseService.ts` → `fetchKpiSnapshot`、`upsertKpiSnapshot` |
| OpenClaw KPI 子结构（嵌入全量快照） | `services/openclawKpiSnapshot.ts` → `buildOpenClawKpiSnapshot`（由 `integrationSnapshot` 调用） |
| 全量集成快照 | `services/integrationSnapshot.ts` → `buildIntegrationFullSnapshotV1`；`pocketbaseService` → `upsertIntegrationFullSnapshot`、`scheduleUpsertIntegrationFullSnapshot`、`INTEGRATION_FULL_SNAPSHOT_KIND` |
| OpenClaw 接入细则 | `02-OpenClaw招商看板接入规范.md` |
