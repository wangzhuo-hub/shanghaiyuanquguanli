# 上海商机及渠道管理系统 - 局域网部署指南

## 📋 系统架构

本系统采用三层架构：
- **前端**: React + Vite (端口: 3004~3010)
- **数据库1**: PocketBase - 招商管理系统房源数据 (端口: 8090) ✅
- **数据库2**: PocketBase - 本系统业务数据 (端口: 8091) ✅

### 🔄 数据同步说明

**房源数据流向：**
```
招商管理系统 (8090) → 自动同步 → 商机管理系统 (8091)
     ↓                              ↓
  房源销控表                    商机关联房源
```

**重要特性：**
- ✅ 两个PocketBase都已配置为局域网访问（监听 0.0.0.0）
- ✅ 局域网内任何设备都可以访问完整的房源数据
- ✅ 支持多设备同时访问，数据实时同步

## 🚀 快速启动

### 方法1：使用启动脚本（推荐）

```bash
# 进入项目目录
cd "/Users/wangzhuo/Desktop/上海商机及渠道管理系统（v5.0）"

# 启动所有服务（局域网模式）
./start-lan.sh
```

### 方法2：手动启动

```bash
# 1. 启动招商管理系统PocketBase
cd ../招商管理看板
./pocketbase serve --http="0.0.0.0:8090" &

# 2. 启动本系统PocketBase
cd "../上海商机及渠道管理系统（v5.0）"
./pocketbase serve --http="0.0.0.0:8091" &

# 3. 启动前端服务
npm run dev
```

## 🌐 访问方式

启动成功后，系统会显示访问地址：

### 本机访问
- 应用: http://localhost:3004 (端口可能为3004-3010)
- PocketBase 8090管理: http://localhost:8090/_/ (招商管理系统)
- PocketBase 8091管理: http://localhost:8091/_/ (本系统)

### 局域网访问
假设本机IP为 `192.168.0.11`：
- 应用: http://192.168.0.11:3004
- PocketBase 8090管理: http://192.168.0.11:8090/_/ (招商管理系统房源)
- PocketBase 8091管理: http://192.168.0.11:8091/_/ (本系统业务)

### 📦 房源数据同步验证

**验证招商管理系统连接：**
```bash
# 本地测试
curl http://localhost:8090/api/health

# 局域网测试
curl http://192.168.0.11:8090/api/health

# 查看房源数据
curl http://192.168.0.11:8090/api/collections/park_backups/records
```

**验证本系统连接：**
```bash
curl http://192.168.0.11:8091/api/health
```

## 🔐 默认登录账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | 123 | 管理员 |
| 李诚 | 123 | 一般账户 |
| 何慧勇 | 123 | 一般账户 |

## 🛑 停止服务

```bash
# 方法1：使用停止脚本
./stop-all.sh

# 方法2：手动停止
kill -9 $(lsof -ti:8090)  # 停止PocketBase 8090
kill -9 $(lsof -ti:8091)  # 停止PocketBase 8091
kill -9 $(lsof -ti:3005)  # 停止Vite
```

## ⚙️ 防火墙配置

### macOS 防火墙配置

1. 打开"系统偏好设置" → "安全性与隐私" → "防火墙"
2. 点击"防火墙选项"
3. 允许以下应用接收传入连接：
   - pocketbase
   - node (或您的终端应用)

### 临时允许所有端口（测试用）

```bash
# 关闭防火墙（测试时）
sudo pfctl -d

# 重新启用防火墙
sudo pfctl -e
```

## 📱 移动设备访问

1. 确保移动设备与服务器在同一局域网
2. 在移动设备浏览器输入：`http://服务器IP:3005`
3. 使用账号登录即可

## 🔧 故障排查

### 问题1：无法访问
```bash
# 检查服务是否运行
lsof -i :3004  # 前端
lsof -i :8090  # 招商管理PocketBase
lsof -i :8091  # 本系统PocketBase

# 检查本机IP
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### 问题2：PocketBase连接失败
- 确认PocketBase使用 `0.0.0.0` 监听（不是 `127.0.0.1`）
- 检查防火墙是否允耸8090、8091端口

**验证监听地址：**
```bash
lsof -i :8090 -i :8091 | grep LISTEN
# 应显示: TCP *:8090 (LISTEN) 和 TCP *:8091 (LISTEN)
# 如果显示: TCP localhost:8090 则需要重新启动
```

### 问题3：数据不同步
- 检查浏览器控制台是否有网络错误
- 确认访问的IP地址与PocketBase服务IP一致

### 问题4：无法获取房源信息 ⭐️

**症状：**
- 商机管理系统无法显示房源列表
- 资产销控页面空白
- 提示“同步引擎解析异常”

**原因分析：**
1. 招商管理系统的PocketBase (8090) 未运行
2. PocketBase只监听 localhost，局域网无法访问
3. 防火墙阻止8090端口

**解决方法：**
```bash
# 1. 检查招商管理系统PocketBase是否运行
lsof -i :8090

# 2. 检查是否监听所有网络接口
lsof -i :8090 | grep LISTEN
# 必须显示: TCP *:8090 (LISTEN)

# 3. 测试局域网访问
curl http://192.168.0.11:8090/api/health
# 应返回: {"message":"API is healthy.","code":200}

# 4. 测试房源数据
curl http://192.168.0.11:8090/api/collections/park_backups/records

# 5. 如果以上都失败，重启服务
cd "/Users/wangzhuo/Desktop/上海商机及渠道管理系统（v5.0）"
./stop-all.sh
./start-lan.sh
```

**验证修复：**
1. 访问应用: http://192.168.0.11:3004
2. 进入“资产销控”页面
3. 点击“同步招商管理系统房源”按钮
4. 如果显示房源列表，则同步成功 ✅

## 📊 技术说明

### 动态Host配置
系统会自动检测访问方式：
- 本地访问（localhost）→ 使用 `127.0.0.1`
- 局域网访问（IP地址）→ 使用对应的局域网IP

代码位置：`services/pocketbase.ts` 中的 `getHostUrl()` 函数

### 端口说明
- **3005**: Vite前端服务（会自动选择可用端口）
- **8090**: 招商管理系统PocketBase
- **8091**: 本系统业务数据PocketBase

## 🎯 生产环境部署

如需生产环境部署，建议：
1. 使用Nginx反向代理
2. 配置HTTPS证书
3. 使用PM2管理Node.js进程
4. 配置PocketBase的备份策略

## 📞 技术支持

如遇问题，请检查：
1. 启动脚本输出的日志
2. PocketBase日志：`/tmp/pb_8090.log` 和 `/tmp/pb_8091.log`
3. 浏览器开发者工具的Network面板
