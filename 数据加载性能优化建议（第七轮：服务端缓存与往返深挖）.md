# 数据加载性能优化建议（第七轮：服务端缓存与往返深挖）

> 在第六轮基础上，本轮**逐行精读了真正的实现**：`scripts/compute-engine.ts`、`services/cloudComputeClient.ts`、`services/pocketbaseService.ts`、`scripts/integration-gateway.ts`、以及 `App.tsx` 的 `recalculateMetrics / recalculateMetricsServerFirst / switchProject / prefetchParkDashboard`。
>
> 结论先行：**第六轮列为"最高杠杆/待核实"的几项，源码核实后部分不成立或已实现**。本轮先据实纠正，再给出经得起代码对照的真正剩余瓶颈与改法。所有结论带 `文件:行` 锚点。

---

## 一、先纠正前几轮的判断（避免无效返工）

精读后，下面几条**与此前推测不符**，请据此调整认知，不要再在这些方向上投入：

1. **服务端 compute 缓存读写键是同源的，不存在"恒 miss"（推翻《运算后移》2.4 的待核实项）。**
   - 读键 version：`computeDashboardData` / `computeBilling` 都用 `readVersionForCache` → `readCloudSaveVersion(projectId)`（compute-engine.ts:407、603；186）。
   - 写键 version：都用 `fetchPocketBaseBackup().data.cloudSaveVersion`（compute-engine.ts:478-482、639-642）。
   - 二者**读的是同一条记录**：`pb_billing_period_notes` 中 `original_id = DASHBOARD_DATA_VERSION_OID` 的 `notes_json.version`（pocketbaseService.ts:1287-1295 与 1720-1735）。→ 读键==写键，缓存能正常命中。

2. **MRU 内存常驻 + 悬停预取 + 本地持久缓存，已经全部实现（第六轮 P0-2 实为已完成）。**
   - 内存 MRU：`dashboardMruCacheRef`（App.tsx:2343），`rememberDashboardMru` 在每次成功加载后写入（多处，如 3745/3926/4098）。
   - **切换器悬停/聚焦预取**：`prefetchParkDashboard`（App.tsx:3719）已挂在 `onMouseEnter`/`onFocus`（App.tsx:7089-7090），预发 KPI 快照 + bootstrap 并写入 MRU 与 `parkDataPutObj`。
   - 切园区时**优先吃 MRU 内存**做瞬时展示（App.tsx:3808-3831）。
   → 已访问/已悬停过的园区，切换基本是"秒切 + 后台校正"。

3. **草稿去重 hash 很便宜，不是主线程热点（修正第六轮"全集合 hash 昂贵"的说法）。**
   - `dashboardDraftIdentityKey`（cloudComputeClient.ts:202）由 `collectionIdentityKey` 拼成，而后者用 **WeakMap 引用标识**（requestIdentityKey.ts:17-40），只做指针查表 + 长度，不做 JSON 序列化/深度 hash。代价是 O(行数) 拼串，远小于一次序列化。

4. **缓存命中日志已经内置。**
   - `logComputeCache('dashboard'|'billing', 'hit'|'miss'|'expired'|'store'|'skip', key)`（compute-engine.ts:119-125）。量化命中率**不用加埋点，直接看网关日志的 `[compute-cache]`**。

结论：客户端渐进式加载、MRU、预取、worker、去重都已到位，**真正的剩余瓶颈集中到了"服务端缓存的有效期与往返"上**——这正是本轮新挖到的部分。

---

## 二、真正的剩余瓶颈（本轮深挖）

### 2.1【已确认·最高杠杆】compute 缓存 TTL 只有 60 秒，且 env 未配置
`DASHBOARD_CACHE_TTL_MS` / `BILLING_CACHE_TTL_MS` 默认 `60_000`（compute-engine.ts:73-75），现网 `.env` / `compose.yaml` **未覆盖**，即实际就是 **60 秒**。

问题在于：缓存键已带 version（`projectId|year|...|v{version}`），而**写入会 bump version、网关在写成功后还会 `clearComputeCachesAfterSuccessfulWrite`**——也就是说**正确性根本不依赖 TTL**，数据一变缓存键就变/被清。60 秒 TTL 只会带来一个纯负面效果：

> **同一园区，只要超过 60 秒没人访问，下一次打开工作台/切回该园区，就必然 miss → 触发服务端全量重算（`fetchPocketBaseBackup` 全量拉取 + `calculateDashboardMetrics`）。**

这解释了"刚才还很快、过一会儿再开又卡"的体感。**这是当前最高性价比的点：本质是一行环境变量。**

### 2.2【已确认】每次 compute 调用都先打一次 `readCloudSaveVersion` 才查缓存
`computeDashboardData`/`computeBilling` 在查缓存**之前**先 `await readVersionForCache(projectId)`（compute-engine.ts:407、603），即便命中也要先付这一次 PB 读的往返。一次"打开工作台"通常并发 bootstrap + billing（+KPI 快照）多个端点，**各自都读一次版本**，往返被放大。

### 2.3【已确认】脏状态下走服务端草稿计算，会整包上传 DashboardData
有未保存改动时，`recalculateMetrics` 仍**优先**调 `fetchCloudDraftComputedDashboard(cloudConfig, metricsInput, options)`（App.tsx:5132），该端点 body 带 `draft_data: 整个 DashboardData`（cloudComputeClient.ts:476-483）。网关 `express.json({ limit: '10mb' })`（integration-gateway.ts:1934）给上传封了 10MB 顶——大园区带改动时，每次重算都要**序列化 + 上传整包**，逼近上限即明显变慢。（与《运算后移》2.2 同源，仍未解决。）

### 2.4【已确认】缓存是进程内 Map，重启/多 worker 即冷
`billingComputeCache` / `dashboardComputeCache` 为模块级 `Map`（compute-engine.ts:88-89），上限 dashboard 60 / billing 120 条（74-76）。网关重启、或以多进程/多实例部署时，缓存不共享、各自从冷开始。

### 2.5【已确认】网关调度器默认关闭，且不预热缓存
`GATEWAY_SCHEDULER_ENABLED` 默认 `0`（compose.yaml:45）；即便开启，`startScheduledJobs` 也只做**封账** `sealPreviousMonthAllProjects` 与**审计清理**（integration-gateway.ts:1919-1927），**没有任何 compute 缓存预热**。意味着部署后/缓存过期后的"第一个访问者"总是吃冷算。

### 2.6【沿用第六轮·仍未做】客户端两处轻量优化
- `FocusDashboard`（App.tsx:1690）、`DashboardCustomFields`（App.tsx:1323）仍是裸 `React.FC`、收整个 `data`，无关 setData（`isSyncing` 等）会触发重渲染与派生重算；
- 自动保存的 `dashboardDataToPbRecords + diffPbRecordsScoped`（App.tsx:2704-2721）虽已 scoped+缓存，仍在主线程。

---

## 三、优化建议（按投入产出排序）

### P0-1 把 compute 缓存 TTL 调大（一行 env，最高杠杆）— 对应 2.1
- 在网关环境变量设：
  ```
  COMPUTE_DASHBOARD_CACHE_TTL_MS=1800000   # 30 分钟，可按需更长
  COMPUTE_BILLING_CACHE_TTL_MS=1800000
  ```
- 理由：正确性由 version 键 + 写后清缓存保证，TTL 拉长**不会读到脏数据**，只会让"近期访问过的园区/月份"在更长时间内保持热缓存。
- 预期：消除"闲置 1 分钟后再开又冷算"，对工作台与切园区**同时**见效；风险极低（数据变更立即失效）。
- 进一步：可把 dashboard/billing 缓存改为"version 命中即长期有效（不靠 TTL 过期，仅靠版本+写后清）"，TTL 仅作兜底上限。

### P0-2 版本号短窗口缓存，合并一次打开内的多次版本读 — 对应 2.2
- 在 compute-engine 内对 `readCloudSaveVersion(projectId)` 加 1–3 秒级 micro-cache（同一 projectId 在窗口内复用），让一次"打开"里并发的 bootstrap/billing/KPI 共用一次版本读。
- 预期：每次开/切的固定往返从 2–3 次降到 1 次；改动小、纯服务端。

### P0-3 开启调度器并增加缓存预热 — 对应 2.5
- 生产置 `GATEWAY_SCHEDULER_ENABLED=1`（顺带让历史月封账生效，历史月不再实时重算）。
- 在 `startScheduledJobs` 增加**预热任务**：定时（或部署后）对活跃园区的当年当月 `computeDashboardData` + `computeBilling` 预跑一遍，写入缓存。配合 P0-1 的长 TTL，"第一个访问者"也命中热缓存。
- 预期：部署/低峰后的首次打开不再冷算。

### P1-1 草稿计算只上送脏子集 + 基线版本 — 对应 2.3
- `fetchCloudDraftComputedDashboard`/`...Billing` 改为只传 `dirty` 集合（`dirtyPbCollectionsRef` 已有，App.tsx:3063 一带）+ `cloudSaveVersion`，由服务端按版本补齐 baseline 再计算。
- 预期：大园区带改动时，每次重算的序列化 + 上传从"整包"降到"增量"，消除逼近 10MB 上限的卡顿。

### P1-2 进程内缓存的多实例策略 — 对应 2.4
- 单进程部署：知悉"重启=冷"，配合 P0-3 预热即可；
- 多 worker/多实例：将 compute 缓存抬到共享存储（如 Redis），或按 `projectId` 做**粘性路由**让同园区落同实例，避免各自冷算与重复计算。

### P1-3 memo 客户端轻组件 + 收窄 props — 对应 2.6
- 给 `FocusDashboard`、`DashboardCustomFields` 包 `React.memo`，props 从整个 `data` 收敛为各自切片，避免 `isSyncing`/自动保存等无关 setData 触发重渲染与派生重算。

### P1-4 自动保存的序列化/diff 移出主线程 — 对应 2.6
- `dashboardDataToPbRecords + diffPbRecordsScoped` 挪进 `requestIdleCallback` 或 Worker，消除"打开后过两秒再卡一下"。

### P2 其他健壮性
- **Worker 超时**（《运算后移》2.1 仍建议）：`computeMetricsInWorker` 加 15–30s 超时与 `pending` 回收，避免极端情况下 Promise 永挂。
- **大屏 `park_ids` 越权过滤**（《运算后移》2.5 待核实）：结合最新代码确认。

---

## 四、先量化再动手（用现成日志，零埋点）

1. **服务端缓存命中率**：看网关日志 `[compute-cache] dashboard hit|miss|expired|store` 与 `billing ...`（compute-engine.ts:125）。
   - 若大量 `expired`/`miss` 且紧随 `store` → 正是 2.1 的 60s TTL 在作祟，先做 P0-1；
   - 若大量 `hit` 但前端仍慢 → 瓶颈在网络往返（P0-2）或客户端渲染（P1-3）。
2. **前端重算耗时**：DevTools Console `[metrics] recalculate XXXms`（App.tsx:5158-5163，>80ms 才打）。命中服务端时应几乎不出现；频繁出现说明走了本地/草稿路径。
3. **切园区往返**：Network 面板数 bootstrap/computed/backup 各打了几次、哪次最慢（是否落到全量 `fetchCloudBackup`）。

---

## 五、优先级总表

| 顺序 | 事项 | 预期效果 | 工作量 | 对应 |
|---|---|---|---|---|
| 1 | P0-1 调大 compute 缓存 TTL（env 一行） | 消除"闲置即冷算"，开/切同时提速 | 10 分钟 | 2.1 |
| 2 | P0-2 版本号短窗口缓存 | 每次开/切固定往返 2-3→1 | 半天 | 2.2 |
| 3 | P0-3 开调度器 + 缓存预热 | 首次/低峰访问也命中热缓存 | 半天-1天 | 2.5 |
| 4 | P1-1 草稿只传脏子集 | 大园区带改动重算不再上传整包 | 1-2天 | 2.3 |
| 5 | P1-3 memo 客户端轻组件 | 无关 setData 不再触发重渲染 | 半天 | 2.6 |
| 6 | P1-4 autosave 移出主线程 | 消除"打开后两秒再卡一下" | 半天 | 2.6 |
| 7 | P1-2 多实例缓存策略 | 多实例部署时不各自冷算 | 视部署 | 2.4 |
| 8 | P2 Worker 超时 / 越权核查 | 健壮性与安全 | 半天 | — |

---

## 六、验证（落地前后各跑一遍）

```bash
npx tsc --noEmit -p tsconfig.json
npm test
npm run build
npm run check:pb
```

- P0-1：改 TTL 后，观察 `[compute-cache]` 中 `hit` 比例显著上升、`expired` 下降；
- P0-2：补"窗口内多次取版本只读一次"的单测；
- P1-1：补"草稿增量 payload + 服务端按版本补齐 baseline 结果一致"的单测。

---

## 七、结论

- 本轮据实**纠正了三处前几轮的误判**：服务端缓存键并不"恒 miss"、MRU+悬停预取已实现、草稿 hash 其实很便宜、命中日志已内置。
- 真正的剩余瓶颈下沉到**服务端缓存有效期与往返**：其中 **P0-1（60s TTL → 调大）是一行环境变量、风险极低、对开/切双双见效，应最先做**；其次是 P0-2 版本读合并与 P0-3 预热。
- 客户端侧（memo、autosave 后移）仍值得做，但在新架构下已是次要项。
- 建议先读现成的 `[compute-cache]` 与 `[metrics]` 日志，用数据确认是"等冷算（服务端缓存）"还是"等渲染（客户端）"，再按上表推进。
