# 第三轮复核结论

## 一、P0 修复确认（全部通过）

| 问题 | 状态 | 复核依据 |
|---|---|---|
| 2.1① notes 空 PATCH 丢数据 | ✅ 已修 | notes 行 `addToResidual` 后 continue，不进 batch（pocketbaseService.ts:2411-2414），残余统一走串行合并路径 |
| 2.1② batch 无乐观锁 | ✅ 已修 | Phase 1 一并抓取 `updated`，Phase 2 与 baseUpdated 比对，不一致进残余由串行路径出冲突（2415-2422） |
| 2.1③ 跳过项静默丢失 | ✅ 已修 | `residualPayload` 机制完整：pbId 缺失、chunk 查询失败、notes 行全部进残余，调用方对残余跑串行并合并 applied（2077-2096、2296） |
| 2.2 基线按年过滤错位 | ✅ 已修 | `refreshAfterSave` 回拉已去掉 `{ year }`（App.tsx:1381），加载与基线口径恢复一致 |
| 2.3 互斥锁 waiter 卡死 | ✅ 已修 | 改为 release 闭包 + 自增 holder id，waiter 移交正确，超时 timer 有 clearTimeout（App.tsx:335-377） |
| 2.4 contractReceivable 缓存串号 | ✅ 已修 | 缓存移入 `BillingCache.contractReceivableByKey` 实例级（dashboardMetrics.ts:72/110/909/935），模块级缓存已删 |
| 2.5 boot 读陈旧 localStorage | ✅ 已修 | boot 改为 `await parkDataGet`，IndexedDB 优先（App.tsx:593） |
| OR filter 超长 / batchResp.ok | ✅ 已修 | 按 50 个 id 分片查询（2354）；`batchResp.ok` 显式检查（2469） |

PB 二进制也确认已升级（pb_hooks 注释标注 0.36，二进制内含 `/api/batch` 路由；`pocketbase/CHANGELOG.md` 还是旧版残留文件，建议删掉防误导）。

## 二、新发现：batch 在服务端是关闭的（P0，一行配置）

查看线上 `pb_data` 设置实测：

```json
{"enabled": false, "maxRequests": 50, "timeout": 3, "maxBodySize": 0}
```

**`batch.enabled = false`** —— 当前每次保存的真实路径是：Phase-1 分片预查（多次 getFullList）→ `/api/batch` 被拒 → 整包回退串行。前端 batch 代码全部白跑，且比纯串行多了预查开销。需要做三件事：

1. Admin UI → Settings → Application 启用 Batch API；
2. `maxRequests: 50` 偏小——前端没有按 50 个请求分组，改动超 50 条时整个 batch 会被服务端拒绝再整包回退串行。建议 maxRequests 提到 200~500，且前端按 maxRequests 分组发送多个 batch（批量导入场景必踩）；
3. `timeout: 3` 秒对大事务偏紧，建议 10 秒。

## 三、本轮新发现的小问题（P1/P2）

**3.1 batch 不可用时没有记忆（P1）**：403/404 后下一次保存仍会先做 Phase-1 预查再撞一次 batch。建议会话级能力缓存：首次被拒后本会话直接走串行（并跳过 Phase-1 预查——预查只为 batch 服务）。

**3.2 addToResidual 可能重复入队（P2）**：Phase-1 某 chunk 查询失败时其 oid 已进残余；Phase-2 遍历全量 updates 时同一 oid 因 pbId 缺失会再次入队 → 串行对同一行跑两遍，第二遍因 updated 已变化必报伪冲突。仅 Phase-1 网络抖动时触发，但修复简单：用 Set 记录已入残余的 `collection::oid`，push 前查重。

**3.3 Phase-1 分片串行 await（P2）**：chunk 查询逐个 await，可 `Promise.all` 并行（每集合最多并发 3~5）。

**3.4 回归测试仍缺位**：`__tests__` 中没有 batch/残余/互斥相关用例。上一轮建议的两条测试（notes 行不得进 batch；保存后 diff(baseline, data) 应为空）仍然值得补——这次复核是人工读码确认的，下次改动没有测试网兜底。注：我的沙箱无法运行 vitest（node_modules 是 macOS 平台二进制），请在本地跑一次 `npx vitest run` 确认全绿。

## 四、遗留架构项（与上轮一致，优先级不变）

1. **派生/原始数据分离**：`setData(processedData)` 依旧（App.tsx:1820），仍是 Worker 化前置；
2. **欠款封账快照**：2026-01 起逐月循环依旧（dashboardMetrics.ts:1415），随时间线性变慢的问题没有解除；
3. **VirtualizedTable 仍未接线**：组件存在但全项目零引用，长表性能改善尚未真正发生；
4. 小清理：`parkDataGetSync` 已成死代码可删；`pocketbase/CHANGELOG.md`、`pocketbase.arm64.backup`、`billingService.ts.bak` 建议移出仓库。

## 五、下一步行动清单（按顺序）

1. 启用服务端 Batch API + 调整 maxRequests/timeout（5 分钟，立刻让 batch 改造生效）；
2. 前端按 maxRequests 分组发 batch + 失败能力记忆（半天）；
3. 补两条核心回归测试（半天）；
4. VirtualizedTable 接入收款明细/应收核销表（1 天）；
5. 派生数据分离 → 欠款封账 → Worker 化（按序推进的架构线）。
