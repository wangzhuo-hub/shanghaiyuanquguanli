# 招商管理看板 代码优化建议

审查范围：`App.tsx`、`services/dashboardMetrics.ts`、`services/billingService.ts`、`services/dataDiff.ts`、`services/pocketbaseService.ts`、`services/cloudService.ts`、`services/localCloudMerge.ts` 及主要组件。

先说做得好的地方（建议保留）：quickMode 跳过非工作台页重算、KPI 快照预览先行渲染、BillingCache 按租户分桶收款、账单明细 requestIdleCallback 延迟挂载、行级乐观锁 + 冲突弹窗、notes_json 按顶层 key 打补丁、组件懒加载。以下建议都是在这个基础上的增量改进。

---

## 一、运算逻辑

### 1.1 全量重算跑在主线程，且每次编辑都触发（最高优先）
所有 `update*` 入口（`updateTenants`/`updatePayments` 等，App.tsx:1876-1881）都同步调用 `recalculateMetrics` → `calculateDashboardMetrics`。在工作台页一次重算包含：

- `calculateTrends` 跑 **3 遍**（全年 + 季度 + 上一年，dashboardMetrics.ts:1321-1378），每遍 12 个月、每月对全租户跑一次账单生成；
- 累计欠款循环（dashboardMetrics.ts:1404-1430）从 **2026-01 逐月算到上月**，每月一次 `buildBillingDetailsForPeriod` 全租户遍历。这是个随时间线性膨胀的定时炸弹：2027 年要跑 ~24 个月，2028 年 ~36 个月，租户越多越慢。

建议（按收益排序）：

1. **把 `calculateDashboardMetrics` 移入 Web Worker**。输入输出都是纯 JSON，可直接结构化克隆；主线程只 `setData` 原始数据，指标异步回填。一次性解决编辑卡顿。
2. **历史月份"封账"快照**：欠款/趋势中早于某个封账点的月份结果落库（已有 `pb_kpi_snapshots` 机制可扩展），重算只覆盖未封账月份，使欠款循环成本恒定。
3. 季度趋势不必第二次调 `calculateTrends`——`quarter !== 'All'` 时直接从 `fullYearMonthlyTrends` 切 3 个月即可（口径完全相同）。上一年趋势仅用于同比展示，可懒算或缓存。

### 1.2 缓存只活一次重算
`createBillingCache` 在每次 `calculateDashboardMetrics` 时重建（dashboardMetrics.ts:1223），`generatedBillsByTenantYear`、`detailsByKey` 等跨重算全部作废。多数编辑只动一个租户/一笔收款，其余 99% 租户的账单生成结果本可复用。

建议：把 cache 提升到模块级，用 `tenants`/`payments`/`assumptions` 的引用（已有 `contextSeq` WeakMap 机制）或版本号做失效，按租户粒度失效而非整体重建。

### 1.3 `buildContractOnlyReceivableForPeriod` 无结果缓存
该函数（dashboardMetrics.ts:886-926）每月调用都重新 `buildReceivableContextForScenario`（含 snapshot 租户 id remap，O(T)）+ `mergeTenantsForReceivablePeriod` + 全租户账单内核。一次重算被 trends 调 12×3 次。给它加 `detailsByKey` 同款的 key 缓存（year|month|contextId）即可，改动很小。

### 1.4 派生数据混入原始数据（影响运算、保存、加载三处）
`processedData` 把 `monthlyTrends`、`currentMonthBilling`、`recentSignings`、`expiringSoon`、`parkingStats` 等派生结果写回 `DashboardData` 并 `setData`，导致：

- 每次重算顶层 `data` 引用变化，整棵组件树重渲染；
- 自动保存 effect 背着派生数据做 `JSON.stringify` 写 localStorage；
- diff 前的 `dashboardDataToPbRecords` 也要遍历这些字段。

建议：拆成两个 state——`rawData`（参与持久化与 diff）和 `metrics`（纯派生、只读）。这是后面几条优化的地基。

---

## 二、并发保存

### 2.1 没有保存互斥（最高优先）
2 秒防抖自动保存（App.tsx:605-660）与手动保存 `runCloudSave` 可以并发执行；自动保存本身耗时超过 2 秒时，下一轮 timer 也会带着同一个 baseline 再次发起。后果：同一行重复 update（双写）、重复 create（目前靠 duplicate-key 幂等兜底）、两个 `refreshAfterSave` 竞相刷新 baseline。

建议：加一个模块级保存互斥——保存中置 `savingPromise`，新请求 await 它之后用**最新 data 重新 diff** 再发（合并窗口期编辑），手动保存与自动保存共用同一队列。改动集中在 App.tsx，收益最大。

### 2.2 每行 2 次串行 HTTP，保存慢且放大并发窗口
`saveIncrementalToPocketBase`（pocketbaseService.ts:2047-2272）对每条 update/delete 先 `findOne`（getList）再 PATCH/DELETE，且全部串行 await。N 条改动 ≈ 2N 个串行往返；批量导入或改 50 条合同时，保存要数十秒，期间被他人写入的概率也随之增大。

建议：

1. **升级 PocketBase 到 ≥ 0.23**（当前 bundled 版本是 v0.22），用 `/api/batch` 一次提交全部 creates/updates/deletes——batch 在**单事务**内执行，天然解决"部分成功"导致的 baseline 修补复杂度（`applyPartialIncrementalSave` 那套逻辑可大幅简化）。
2. 升级前的过渡方案：去掉 `findOne`——加载时 `fetchPocketBaseBackup` 已拿到每行数据，把 PB 内部 `id` 一并存入 recordMeta，更新时直接按 id PATCH；并按集合并行 + 并发上限（5~10）。

### 2.3 乐观锁是 check-then-write，非原子
`findOne` 读 `updated` 与实际 PATCH 之间存在时间窗，两个客户端可同时通过校验，后写者**静默覆盖**且不会进冲突弹窗。低频协作下概率小，但正是"偶发丢数据查不出原因"的来源。

建议：用 `pb_hooks` 在服务端做 CAS——客户端把 `baseUpdated` 随请求带上（header 或字段），`onRecordUpdateRequest` 中不匹配直接拒绝（409），前端按现有冲突流程处理。配合 2.2 的 batch 事务，整个写路径才真正闭环。

### 2.4 billing_period_notes 单行 JSON 是并发热点
缓缴、特殊业态、手工应收、跟进备注、导入预算表全部挤在一行 `notes_json` 里。冲突判定（2175 行）发生在 patch 合并（2187 行）**之前**：两人改互不相干的 key 也会被判冲突打断保存。

建议：对带 `notes_json_patch` 的更新跳过行级 `updated` 比对，直接服务端取最新行做 per-key 合并（特殊业态行已有 `updatedAt` 可做 per-key LWW）。中长期可把高频写的 key（跟进备注、特殊业态）拆成独立集合，每行独立乐观锁。

### 2.5 每次保存成功都全量回拉 + 全量重算
`refreshAfterSave`（App.tsx:1234-1280）做三件重活：quickMode=false 的完整指标重算、KPI/集成快照上传、`fetchCloudBackup` 全集合回拉。自动保存每次有变更都走一遍。

建议：常规路径改用 `applied[].newUpdated` 增量刷新 recordMeta + baseline（`applyPartialIncrementalSave` 已具备此能力，可推广为默认路径）；全量回拉校准降频为定时（如 5 分钟）或手动保存时执行。`bumpCloudSaveVersion` 是读-改-写，多端并发会丢计数——仅审计用影响小，但别基于它做业务判断。

---

## 三、数据量大后的加载迟缓

### 3.1 全量拉取所有集合（首屏慢的主因）
`fetchPocketBaseBackup`（pocketbaseService.ts:1531-1581）对 12 个集合 `getFullList` 无差别全量拉取。`pb_payments`、`pb_invoices` 随时间无限增长——三五年后收款流水上万条，每次启动、切园区、保存后回拉都要全部下载 + 反序列化 + map。

建议：

1. **收款/发票按年拉取**：工作台与应收只需当年（欠款封账后连上一年都不用），历史年份在财务报表切换年份时按需加载。PB filter 直接支持 `date >= "2026-01-01"`。
2. `getFullList` 显式传 `batch: 1000`（默认 500，减半分页次数）；对宽表（pb_tenants 有 50+ 列含多个 JSON 列）用 `fields` 投影，列表场景不拉 `base_data_snapshot` 这类大字段（budget_scenarios 的快照可能非常大）。
3. 确认 PB 端为各集合建了 `(project_id)` 与 `(project_id, original_id)` 复合索引（增量保存的 `findOne` 全靠它）。
4. 反向代理层开启 gzip/br 压缩，JSON 体积通常可减 80%。

### 3.2 localStorage 整包缓存会先于后端崩掉
每次变更 2 秒后 `JSON.stringify(data)` 全量同步写 localStorage（App.tsx:616），包含派生字段。问题：① 同步序列化大对象阻塞主线程；② localStorage 约 5MB 配额，数据量上去后 `setItem` 抛 QuotaExceeded，被 catch 后只打 console，**用户无感知地失去了本地缓存兜底**——而启动恢复、断网兜底全依赖它。

建议：迁移到 IndexedDB（`idb-keyval` 几行代码），异步写、容量 GB 级；只持久化原始数据（配合 1.4）；写失败时给用户可见提示。

### 3.3 渲染无虚拟化
`ContractManager`(5119 行)、`FinanceManager`、`BillingTable` 等直接渲染全部行，无 react-window/分页。租户数百、流水上千后 DOM 数量成为新瓶颈（数据加载快了也白搭）。

建议：长表统一上 `@tanstack/react-virtual`（或先做简单分页），优先改收款明细与应收核销两张最长的表。

### 3.4 单一顶层 `data` state
任意字段变化 → 顶层引用变化 → 所有 tab 组件 props 变化。配合 1.4 的拆分，再对大组件加 `React.memo` + 按需传切片（或引入 zustand 做切片订阅），可显著减少无关重渲染。

---

## 四、实施优先级

| 优先级 | 事项 | 主要改动点 | 预期效果 |
|---|---|---|---|
| P0 | 保存互斥队列 | App.tsx | 消除双写/baseline 竞态 |
| P0 | PB 升级 0.23+ 改 batch 保存 | pocketbaseService.ts | 保存从 ~2N 次串行请求 → 1 次事务 |
| P0 | 收款/发票按年加载 | pocketbaseService.ts | 首屏与切园区加载时间不随年限增长 |
| P0 | localStorage → IndexedDB | App.tsx | 防数据量增长后缓存静默失效 |
| P1 | 原始数据与派生指标分离 | App.tsx / types | 重渲染、diff、持久化三处同时减负 |
| P1 | 欠款/历史月封账快照 | dashboardMetrics.ts | 重算成本恒定，不随时间膨胀 |
| P1 | refreshAfterSave 改增量、降频全量校准 | App.tsx | 自动保存后不再卡顿 |
| P1 | buildContractOnlyReceivableForPeriod 加缓存；季度趋势改切片 | dashboardMetrics.ts | 单次重算约省 1/3~1/2 |
| P2 | 指标计算迁 Web Worker | dashboardMetrics.ts | 编辑彻底零卡顿 |
| P2 | 长表虚拟滚动 | 各 Manager 组件 | 大列表渲染流畅 |
| P2 | pb_hooks 服务端 CAS；notes 热点 key 拆集合 | pocketbase/pb_hooks | 并发写入真正原子化 |
