# UI 毛玻璃设计优化建议（对照 Apple Liquid Glass 规范）

> 评估对象：招商管理看板前端（`index.css` 约 3778 行 + `App.tsx` + `components/`）
> 评估视角：Apple「Liquid Glass / 毛玻璃」设计语言（WWDC25）与 Apple HIG（材质、对比度、触控、自适应明暗）
> 评估日期：2026-06-24

---

## 总览结论

现有实现**已经是一套相当成熟的 Liquid Glass 系统**，不是简单的「半透明 + 模糊」堆砌。值得肯定的基础：

- 分层高光做得到位：每个玻璃面用 `::before`（顶部镜面高光）+ `::after`（内描边/底部阴影）模拟玻璃边缘，缓动用 `cubic-bezier(0.22,1,0.36,1)`，质感比一般 glassmorphism 高一档。
- 已具备三项关键兜底：`@supports not (backdrop-filter)` 降级、`prefers-reduced-transparency: reduce` 关闭透明、`prefers-reduced-motion: reduce` 关闭位移动画。
- 已按层级细分材质（`-toolbar`/`-sidebar`/`-panel`/`-control`/`-subtle`/`-readable`），blur 与 saturate 分级合理。

因此下面的建议**不是推倒重来，而是补齐「真实缺陷 + 规范契合 + 移动端细节」三类缺口**。按优先级分为 P0 / P1 / P2。

---

## P0 · 高优先级（真实缺陷 / 高性价比）

### P0-1　`viewport-fit=cover` 缺失，导致全部安全区适配失效 ⚠️
- **证据**：`index.html` 第 6 行 viewport 为 `width=device-width, initial-scale=1.0`，**没有** `viewport-fit=cover`；但 `App.tsx`、各弹窗 footer 等 **15+ 个文件**用了 `env(safe-area-inset-bottom)`（如底部导航 `App.tsx:6728`、`liquid-elevated-footer`）。
- **影响**：没有 `viewport-fit=cover` 时，iPhone 上 `env(safe-area-inset-*)` 恒为 0——也就是说**现有所有安全区代码在刘海屏/灵动岛机型上全部静默失效**，底部玻璃导航与弹窗按钮会被 Home Indicator 横条压住。
- **方向**：viewport 补 `viewport-fit=cover`，一行修复即可激活已有的全部 `env()` 代码。

### P0-2　断点三套并存，平板/横屏手机区间「夹生」
- **证据**：CSS `@media` 用 **640px**（4 处）；`hooks/useMediaQuery.ts` 的 `useMinMdViewport` 用 **768px**；`App.tsx:1082` 的 `useMobileNavLayout` 用 **1023px**。
- **影响**：在 641–1023px（平板竖屏、手机横屏、小窗）区间，JS 已切换到「移动端底部导航」布局，但 CSS 里 `max-width:640px` 的移动微调**不生效**，出现布局错位、间距不协调的「夹生态」。
- **方向**：统一为一套语义断点（如 mobile ≤640、tablet 641–1024、desktop ≥1025），让 JS 判断与 CSS 媒体查询共用同一组阈值。

### P0-3　移动端高度用 `100vh`，被动态地址栏遮挡
- **证据**：全项目 `h-screen / min-h-screen / 100vh` 共 **26 处**，`dvh/svh` **0 处**（如 `App.tsx:6344/6347` 侧栏与外壳）。
- **影响**：移动 Safari/Chrome 地址栏收放时 `100vh` 大于真实可视高度，导致底部固定导航被工具栏盖住、出现「多滚一截」的诡异空白。
- **方向**：移动端高度改用 `100dvh`（动态视口高度），并保留 `100vh` 作为旧浏览器回退。

### P0-4　键盘焦点态几乎不存在（无障碍硬伤）
- **证据**：3778 行 CSS 里 `:focus-visible` **仅 1 处**（且只针对 `liquid-building-add-slot`）；核心交互类 `liquid-pressable` 被用了 **303 次**，却只有 `:hover` / `:active`，没有可见焦点环。
- **影响**：键盘 / 辅助技术用户完全看不到焦点落在哪里，违反 WCAG 2.4.7（Focus Visible），也不符合 Apple HIG 对焦点态的要求。
- **方向**：给所有玻璃可点击元素加统一的 `:focus-visible` 焦点环（建议品牌色描边 + 轻微外发光），与玻璃质感协调。

---

## P1 · 中优先级（Apple 规范契合度 + 关键细节）

### P1-1　玻璃被「全局滥用」，缺少内容层 / 控件层的层次（最核心的理念差距）
- **证据**：`liquid-glass-*` 不止用于导航，还铺满了面板、卡片、表头、单元格、表格、合计行等几乎所有层级（独立 `liquid-*` 类数量极多，玻璃叠玻璃普遍存在）。
- **Apple 规范**：Liquid Glass 的定位是**漂浮在「不透明内容层」之上的导航 / 控件层**，官方明确「不要把玻璃叠在玻璃上」。玻璃用得越克制，质感越高级。
- **影响**：当前玻璃叠玻璃既稀释了材质辨识度，又是**可读性与性能的双重负担**。
- **方向**：把内容层（数据表格、长列表、正文卡片）改为接近不透明的「类纸面」；玻璃**只保留给**顶栏 / 侧栏 / 底部导航 / 悬浮工具条 / 弹窗这类「控件与导航层」。

### P1-2　无暗色模式，玻璃高光被写死为浅色
- **证据**：`prefers-color-scheme` **0 处**；高光/描边大量写死 `rgba(255,255,255,…)`，背景渐变也是固定浅色（`.ios-liquid-app`）。
- **Apple 规范**：Liquid Glass 的本质是**随明暗环境自适应**的材质。
- **影响**：系统切到深色时，整套玻璃会发灰、白色高光过曝、文字对比异常。
- **方向**：至少接入 `prefers-color-scheme: dark`，把高光 / 描边 / 底色改为「随明暗切换」的语义令牌（见 P2-4）。

### P1-3　半透明叠加下的文字对比度风险
- **证据**：大量弱化文字（muted `#64748b`）与彩色小字（如琥珀 chip 文字 `#92400e` 叠在半透明彩底上）；`prefers-contrast` **0 处**兜底。
- **Apple 规范**：玻璃之上的文字需有足够不透明衬底，否则在亮背景上极易跌破 WCAG AA（正文 4.5:1 / 大字 3:1）。
- **影响**：`liquid-glass-readable` 已是正确解法（好），但 `subtle` 玻璃 + 渐变背景上的 muted / 彩色小字仍有读不清的风险。
- **方向**：正文一律走「readable」级材质；为文字区增加不透明衬底；补 `prefers-contrast: more` 兜底。

### P1-4　触控目标尺寸不统一（< 44pt 隐患）
- **证据**：底部导航项用 `min-h-12`（48px）✅ 合规；但全局混用 `h-11`（44px，41 处）、`min-h-[44px]`（仅 1 处），年份步进器、chips、表格行内操作等**未系统审计**，存在小于 44pt 的命中区。
- **Apple HIG**：可点击元素最小命中区 **44×44pt**。
- **方向**：统一一个「最小命中区」工具类，套用到所有图标按钮 / 步进器 / chip / 行内操作。

### P1-5　圆角未遵循「同心圆角」
- **证据**：固定圆角混用 `rounded-[32px] / [24px] / [22px] / [18px]`，嵌套层未按「内圆角 = 外圆角 − 内边距」推导。
- **Apple HIG**：嵌套容器应保持**同心圆角**，否则玻璃层叠处圆角不贴合、出现「错位的弧」。
- **方向**：定义一组圆角令牌，嵌套时按内边距推导内层圆角。

---

## P2 · 细节打磨（移动端手感 / 体感）

### P2-1　移动端点按出现系统默认高亮块
- **证据**：`-webkit-tap-highlight-color` **0 处**。
- **影响**：在手机上点玻璃按钮会闪现系统默认的灰 / 蓝色方块，瞬间破坏玻璃质感。
- **方向**：全局把 tap highlight 设为透明，改由已有的 `:active` 缩放反馈承担点按反馈。

### P2-2　滚动 / 回弹细节缺失
- **证据**：`overscroll-behavior`、`touch-action`、`scrollbar-gutter` 均 **0 处**。
- **影响**：弹窗内滚动会带动整页 / 误触发下拉刷新；滚动条出现时布局横向抖动。
- **方向**：弹窗 / 抽屉容器加 `overscroll-behavior: contain`；滚动容器预留 `scrollbar-gutter: stable`。

### P2-3　模糊层叠的性能成本
- **证据**：`backdrop-filter` 共 **177 处**，最大 `blur(30px) + saturate(1.68)`，且存在玻璃叠玻璃。
- **影响**：大半径实时模糊 + 多层叠加在中低端手机上明显掉帧、发热。
- **方向**：移动端调低 blur 半径（如 ≤18px）；避免玻璃嵌套；对玻璃层加 `contain` / 合理分层，减少重绘范围。

### P2-4　颜色全靠字面量，缺设计令牌
- **证据**：`rgba(...)` 字面量约 **958 处**，CSS 变量极少（仅 `.ios-liquid-app` 里定义了少量 `--liquid-*`）。
- **影响**：难维护、难统一、几乎无法做暗色 / 主题切换（直接卡住 P1-2）。
- **方向**：抽象语义令牌——材质透明度、描边、高光、阴影、品牌色各成一组变量，玻璃类只引用令牌。

### P2-5　进阶加分：动态镜面高光与边缘折射
- **现状**：已有静态 `::before/::after` 高光与内描边（基础已具备）。
- **Apple 规范**：真正的 Liquid Glass 有**随内容 / 滚动 / 指针移动而流动的镜面高光**，以及边缘的「透镜」折射感。
- **方向**：作为锦上添花，可用 CSS 变量 + 指针位置做轻量近似（顶栏 / 主操作按钮上的流动高光），不必全量铺开。

---

## 建议落地顺序

| 阶段 | 内容 | 说明 |
|---|---|---|
| 第一步（当天可做） | P0-1 viewport-fit、P0-3 dvh、P2-1 tap-highlight | 改动极小、收益立竿见影，先把移动端「明显错」修掉 |
| 第二步 | P0-2 断点统一、P0-4 焦点态、P1-4 触控尺寸、P2-2 滚动细节 | 无障碍与一致性，工作量中等 |
| 第三步 | P2-4 设计令牌 → P1-2 暗色模式 → P1-3 对比度兜底 | 令牌是暗色与对比度的前置条件，需成体系推进 |
| 第四步（理念升级） | P1-1 玻璃分层（内容层不透明化）、P1-5 同心圆角、P2-5 动态高光 | 最能拉开与普通 glassmorphism 差距，但改动面大，建议分模块灰度 |

> 一句话总结：**当前最大的两个缺口——一是移动端安全区/视口/焦点的「真实缺陷」（P0，廉价高收益），二是「玻璃用得太满、缺少内容层与暗色自适应」这一与 Apple 规范的理念差距（P1）。** 先清 P0，再循序补 P1/P2。

---

## 2026-06-24 落地补充

- **已落地 P2-4 第一阶段**：`index.css` 的核心玻璃材质开始令牌化，`.ios-liquid-app` 新增 `--liquid-app-bg-*`、`--liquid-material-*` 等语义变量，并迁移通用 `liquid-glass` / panel / toolbar / control / readable / monthly-detail 基础层的背景、边框、高光和阴影引用。该阶段保持现有浅色视觉不变，目标是为后续暗色模式和高对比主题提供集中修改入口。
- **后续约束**：新增全局玻璃层时优先引用 `--liquid-material-*` 令牌；只有模块专属状态色、风险色或业务图表色才允许保留局部字面量。暗色模式不得直接大面积覆盖组件色值，应先扩展令牌，再逐层验证文本对比度。
- **已落地 P1-2 第一阶段**：在 `prefers-color-scheme: dark` 下接入暗色材质令牌，覆盖全局背景、基础玻璃边框/底色/高光/阴影，并为 readable 内容层、常见 slate 文本、浅色边框和白底工具类提供高对比兜底。该阶段属于系统级暗色基础，不替代后续逐模块暗色 QA。
- **后续约束**：后续做模块级暗色模式时，应优先验证工作台、月度明细、楼宇管理、合同、财务和弹窗中的数字对比度；业务状态色如 amber/rose/blue/cyan 不应被全局 slate 兜底误改。
- **已落地移动玻璃守卫脚本**：新增 `npm run qa:mobile-ui`，将 `viewport-fit=cover`、`100dvh`、44px 触控目标、移动端 12–16px blur、`contain: paint`、暗色材质令牌和相关文档约束纳入源码级检查；真实截图和模块暗色 QA 仍需继续执行。
- **已落地移动字号守卫**：`mobile-typography-guard` 将核心移动卡片内的 9/10/11px 辅助文字抬到 12px 可读下限；后续新增移动卡片应优先使用 `text-xs` 以上字号，避免在半透明玻璃背景上继续依赖过小文字承载信息。
- **已落地移动弹层语义**：移动「更多管理」bottom sheet 补齐 dialog / modal 语义、可见标题关联和 Escape 关闭；后续新增玻璃浮层时应保持明确标题、关闭按钮、背景点击和键盘退出路径，避免只靠视觉遮罩表达模态状态。
- **已落地移动空态守卫**：合同、财务和发票移动空态纳入 `npm run qa:mobile-ui`，防止 readable 玻璃空态退回浅灰单行提示；空态必须继续给出清晰标题、原因说明和安全的下一步动作。
- **已落地移动数字输入守卫**：关键数字输入不再只依赖运行时兜底，`npm run qa:mobile-ui` 会扫描 `type="number"` 是否同时声明 `inputMode` 和 `enterKeyHint`；新增金额、面积、单价、月份等输入时应在源码层直接补齐移动键盘提示。
- **已落地移动长列表 / 上下文守卫**：`npm run qa:mobile-ui` 会检查移动长列表的 `overscroll-behavior`、iOS 惯性滚动、固定底栏安全留白、横滑边缘淡出，以及移动顶栏上下文线，防止毛玻璃导航层遮挡内容或用户失去当前数据范围判断。
- **已落地平板主从预览守卫**：`npm run qa:mobile-ui` 会检查 641–1023px 轻管理主从布局、sticky 预览和筛选后的预览重置逻辑；平板竖屏和手机横屏应继续使用卡片 + 预览，而不是回退到桌面宽表。
- **已落地主色 / 焦点态守卫**：`npm run qa:mobile-ui` 会检查 `.ios-liquid-app` 的蓝 / 青主色令牌和玻璃控件统一 `:focus-visible` 焦点环；绿色只保留为业务成功状态，键盘用户必须能看到明确的蓝色描边与轻外发光。
- **已落地关注字段跨设备偏好**：自定义关注字段从仅本机 localStorage 升级为“本地即时兜底 + 登录用户云端偏好”；新建 `pb_user_preferences` 存个人 UI 偏好，不进入业务数据版本，也不改变字段 resolver 计算口径。
- **已落地关注字段同步状态反馈**：关注字段标题区新增轻量同步文案，在读取、保存、成功和云端失败回退时分别提示 `读取账号偏好 / 同步中 / 已同步到账号 / 本机已保存`；该文案用蓝 / 青 / 琥珀 / 灰色状态，不新增业务状态色，也不改变字段值来源。
- **已落地关注字段设置弹层语义**：「自定义关注字段」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；该阶段只增强玻璃弹层可访问性，不改变关注字段显示、保存或计算口径。
- **已落地移动查询筛选弹层语义**：「筛选客户」bottom sheet 改为面板自身承载 dialog / modal 语义，补齐可见标题关联、背景点击关闭、关闭按钮和 Escape 关闭；该阶段只增强筛选弹层可访问性，不改变楼栋、到期、收款、当前账期或历史欠费筛选口径。
- **已落地月度收款明细弹层收口**：「月度收款明细」弹层补齐可见标题关联、背景点击关闭和 Escape 关闭；手机逐月卡片的指标标签抬到 12px 可读下限。该阶段只增强弹层操作和文字可读性，不改变月度收款汇总数据来源。
- **已落地月度收款明细口径说明语义守卫**：「月度收款明细」dialog 通过 `aria-describedby="monthly-collection-detail-description"` 关联“年初预算、合同应收与实际收款按现有工作台口径汇总”说明。该阶段只让毛玻璃明细弹层的口径说明可被辅助技术读取，不改变月度收款汇总数据来源。
- **已落地月度收款明细焦点闭环**：「月度收款明细」复用 `useMobileSheetFocus`，打开后焦点进入关闭按钮，Tab / Shift+Tab 限定在明细弹层内，关闭后回到打开它的月度明细入口。该阶段只增强毛玻璃明细弹层的键盘和辅助技术操作闭环，不改变月度收款汇总数据来源。
- **已落地移动月度明细卡语义补全**：手机逐月卡和深色合计卡继续保持毛玻璃卡片视觉，但补齐完整 `aria-label/title` 摘要；完成率、累计达成、实际收款、合同应收、年初预算、去年同期和合计口径都能被完整读取。该阶段只增强弹层卡片语义，不改变月度收款汇总数据来源。
- **已落地移动首页可读玻璃层级**：移动工作台首屏改为浅色 readable 玻璃承托，蓝色只用于主值、进度条和选中态；顶部园区 / 状态、年度达成、收款进度、三枚微指标、今日待办和关注字段都使用深色正文，避免 iPhone 深色外观或低亮度环境下出现白字叠高透明蓝色玻璃。该阶段只调整材质、对比度和层级，不改变任何 KPI 或字段计算口径。
- **已落地关注字段设置焦点闭环**：「自定义关注字段」弹层复用 `useMobileSheetFocus`，补齐焦点移入、Tab 圈定、关闭后返回触发入口；字段选项补齐 `aria-pressed`，已选数量补齐 polite live region。该阶段只增强毛玻璃设置弹层的可访问性和可读性，不改变字段候选、最多 5 个限制或云端 / 本地偏好保存逻辑。
- **已落地关注字段底栏避让细化**：手机视口下，「我的关注字段」区与固定底部导航之间改为包含 `env(safe-area-inset-bottom)` 与 `100dvh` 的动态安全间距，小屏避开 Home Indicator，大屏手机避免关注字段卡被底部毛玻璃导航覆盖。该阶段只增强移动端毛玻璃层叠安全区，不改变关注字段内容或保存逻辑。
- **已落地关注字段横滑提示细化**：移动端「我的关注字段」横滑列表纳入统一横滑轨道材质，使用边缘淡出和隐藏滚动条提示可继续滑动，并补充列表 / 列表项语义与“横向滚动”标签；该阶段只增强毛玻璃卡片组的浏览可发现性和辅助技术理解，不改变关注字段内容、最多 5 个限制或保存逻辑。
- **已落地合同退租弹层语义**：「办理退租」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；该阶段只增强危险合同动作的退出路径和辅助技术理解，不改变退租日期、退租类型、部分退租拆分、提前退租扣回或租户记录写入口径。
- **已落地合同初始化收款弹层语义**：「初始化历史收款」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；该阶段只增强历史收款录入弹层的退出路径和辅助技术理解，不改变金额校验、日期限制、收款记录生成或付款数据写入口径。
- **已落地合同提示 / 确认层语义**：合同自定义提示 / 确认层补齐可见标题关联、背景点击关闭、面板点击隔离和 Escape 关闭；确认类的背景点击 / Escape 等价于取消，提示类等价于知道了，避免玻璃确认层只靠视觉遮罩表达状态。
- **已落地合同名称 / 付款周期变更弹层语义**：名称变更和付款周期变更弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭、面板点击隔离和 Escape 关闭；该阶段只增强合同轻变更弹层的退出路径和辅助技术理解，不改变变更记录、账单预览或保存结构。
- **已落地合同轻变更弹层字号收口**：名称变更和付款周期变更弹层的历史日期、当前合同标签和生效说明不再使用 11px / slate-400 灰字；该阶段只优化 elevated 毛玻璃弹层文字清晰度，不改变名称历史、付款周期历史或账单预览口径。
- **已落地 AI 合同 / 收款识别弹层语义**：AI 合同录入和 AI 收款录入弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭、面板点击隔离和 Escape 关闭；该阶段只增强 AI 导入强焦点弹层的退出路径和辅助技术理解，不改变 AI 解析、匹配、选中项或导入写入口径。
- **已落地 AI 识别弹层字号收口**：AI 合同 / 收款识别弹层里的上传提示、识别摘要、移动结果卡标签、客户匹配说明和低置信提示不再使用 10px / 11px 弱灰字；该阶段遵循“确认型弹层优先清楚”的毛玻璃原则，只提升确认阅读性，不改变识别或导入逻辑。
- **已落地楼宇提示 / 确认层语义**：楼宇自定义提示 / 确认层补齐可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；确认类背景点击 / Escape 等价于取消，提示类等价于知道了，避免玻璃确认层只靠视觉遮罩表达状态。
- **已落地楼宇移动轻管理弹层语义**：单元「快速处理」和批量状态处理 action sheet 补齐可见标题关联与 Escape 关闭；该阶段只增强楼宇轻管理弹层的可访问性，不改变房源状态保存、合同控制锁定、面积统计或租户引用口径。
- **已落地楼宇移动房源卡玻璃层级**：移动房源列表增加楼层分组玻璃层和状态优先主视觉区，用状态色边线、房号主标题、面积 / 租户双指标和固定操作区替代平铺小表格感；该阶段只增强扫读、触控和材质层次，不改变房源状态保存、合同控制锁定、面积统计或租户引用口径。
- **已落地预算移动弹层语义**：预算行月度明细和单月预算增减 / 导入月额编辑弹层补齐可见标题关联与 Escape 关闭；该阶段只增强预算移动轻管理弹层的可访问性，不改变预算、实收、差额、人工调整或导入快照更新口径。
- **已落地财务移动核销弹层语义**：单笔收款核销和批量核销弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；该阶段只增强财务核销弹层的可访问性，不改变核销金额、尾差结清或付款记录写入口径。
- **已落地财务缓缴 / 发票延期弹层语义**：「申请缓缴」和「延期开票」弹层补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭和 Escape 关闭；该阶段只增强高风险写入弹层的退出路径和辅助技术理解，不改变缓缴账期、开票日期、状态切换或记录写入口径。
- **已落地云端保存冲突弹层语义**：数据冲突决策弹窗补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；该阶段只增强保存冲突强焦点弹层的退出路径和辅助技术理解，不改变默认跳过、用我的值 / 用服务端值 / 跳过此条、批量决策或 `onResolve(decisions)` 写入口径。
- **已落地云端保存冲突弹层字号收口**：保存冲突弹层中的更新时间、字段名、本地值 / 服务端值标签和空态不再使用 11px / slate-400 弱灰字；冲突处理属于高风险决策任务，毛玻璃层可以轻，但字段差异必须清楚。该阶段只优化 elevated 冲突弹层阅读性，不改变冲突合并逻辑。
- **已落地预算导入名称关联工具字号收口**：名称关联工具里的年度、导入预算行、合同客户、候选 ID 和已保存关联标签不再使用 10px / 11px 或 slate-400 弱灰字；这类数据修正入口按确认型任务处理，宁可少一点密度，也要让旧名称与目标合同客户清楚可辨。该阶段只优化 readable / elevated 玻璃层文字，不改变名称关联写入口径。
- **已落地合同 / 楼宇导入结果弹层语义**：批量导入结果弹窗补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；该阶段只增强导入反馈弹层的退出路径和辅助技术理解，不改变导入汇总、失败明细、失败明细导出、楼宇合同同步行或导入写入口径。
- **已落地移动首屏底栏避让细化**：375–430px 手机视口下，工作台 hero、三列指标和今日待办卡采用紧凑垂直节奏；底部毛玻璃导航避让聚焦在待办和关注字段区域，园区切换已上移到顶栏，不再用底部园区汇总卡占用首屏滚动空间；该阶段只调整移动间距和最小高度，不改变数据口径。
- **已落地移动 CSS 结构守卫**：`npm run qa:mobile-ui` 会检查核心 CSS 大括号平衡，并防止移动预算布局出现重复嵌套选择器；关注字段同步状态补充组件渲染测试，避免玻璃标题区状态文案在后续拆组件时丢失。
- **已落地移动查询空态收口**：客户查询无结果时使用 `liquid-mobile-empty-state` readable 玻璃卡片，区分筛选无结果和暂无客户，并提供「清除筛选 / 回到指标」安全动作；该阶段只增强空态可读性和操作反馈，不改变搜索、筛选或结果总数口径。
- **已落地移动查询长结果渐进展示**：移动客户查询默认显示 6 条结果，超出时提供「显示更多」，展开后提供「收起结果」，并显示「已显示 X / 共 Y 条」且通过 `aria-live` 轻量播报变化；该阶段只改善长列表扫读与触控反馈，不改变搜索索引、筛选、当前账期、历史欠费或结果总数口径。
- **已落地移动查询状态 chips 横滑提示**：顶部合同状态 chips 接入 `liquid-mobile-search-chips` 共享横滑轨道，和月份筛选、楼宇切换一样保留边缘淡出、隐藏滚动条与 iOS 惯性滚动；该阶段只增强可滑动感知，不改变筛选口径。
- **已落地移动导航上下文语义守卫**：移动底部导航补充稳定 `aria-label="移动端主导航"`，更多管理弹层通过 `aria-describedby="mobile-more-management-context"` 关联当前园区与年份说明。该阶段只增强毛玻璃导航层的上下文理解，不改变导航项、权限或跳转逻辑。
- **已落地移动筛选分组选项语义守卫**：移动客户查询顶部状态 chips 命名为“合同状态筛选”；筛选 sheet 内楼栋、到期月份、收款记录、当前账期和历史欠费选项均由可见标题关联 `role="group"` / `aria-labelledby`，筛选按钮会播报已启用高级筛选数量。该阶段只增强移动筛选上下文和 VoiceOver 理解，不改变筛选计算口径。
- **已落地移动查询筛选徽标显式字号收口**：移动查询筛选数量圆标和历史欠费前置条件说明统一使用 `text-xs`，让透明玻璃控件里的状态徽标直接达到 12px 可读下限；该阶段只提升状态文案辨识度，不改变筛选逻辑或历史欠费口径。
- **已落地移动顶部与快捷入口可读性收口**：顶部导航标题增加 `liquid-mobile-toolbar-title` 并保持 16px 手机阅读下限，避免毛玻璃 toolbar 上的页面名发虚；工作台快捷入口的辅助说明改为 `liquid-mobile-action-helper`，去掉半透明 opacity，蓝 / 青入口使用高对比浅字，琥珀 / slate 入口使用深色说明。该阶段只调整移动 chrome 和快捷入口文字材质，不改变导航或入口动作。
- **已落地移动微指标截断语义补全**：移动首屏三列微指标保留紧凑 `truncate` 版式，但收款缺口、已租面积、出租目标的完整值同步写入 `title` 和 `aria-label`；长面积值在 390px 视口被截断时，信息不会只停留在不可见文本里。该阶段只补语义和可读性，不改变 KPI 数据来源。
- **已落地移动首屏截断语义扩展**：顶部 toolbar 标题、园区胶囊、固定 KPI cell、快捷入口与今日待办按钮均补完整文本语义；玻璃层仍保持轻量和紧凑，但 `truncate` 不再意味着信息丢失。该阶段只增强长文本确认能力，不改变任何入口跳转或指标口径。
- **已落地移动顶部园区切换语义补全**：移动工作台不再渲染底部园区汇总 / 切换卡；园区选择上移到顶栏胶囊，弹层只列授权园区，通过 `aria-current` 表达当前选择，并沿用原 `switchProject` 加载与权限逻辑。该阶段只优化移动园区切换位置、材质和语义，不改变授权园区或单园区 KPI 计算逻辑。
- **已落地移动查询结果上下文动作**：客户查询卡片的「合同 / 核销」不再只跳模块首页，而是携带当前客户进入合同轻管理或财务应收核销，并复用模块内搜索框预填客户名；该阶段只优化触达路径和玻璃卡片操作感，不改变合同、应收、收款、欠费或保存逻辑。
- **已落地移动查询长文本语义补全**：客户查询卡片继续保持紧凑单行截断，但整卡、客户名、楼栋房号、状态和收款 / 欠费摘要补充完整 `aria-label` / `title`，合同 / 核销按钮读出当前客户名；该阶段只增强长企业名、长位置和金额摘要在移动端的可理解性，不改变搜索、筛选、跳转或结果排序口径。
- **已落地移动查询清除动作触控收口**：顶部「清除」按钮接入 `liquid-mobile-filter-clear mobile-pressable`，实际命中区进入 44px 触控目标组，并补充明确 `aria-label`；该阶段只增强触控与辅助技术命名，不改变清除筛选逻辑。
- **已落地移动选中态语义收口**：底部导航和「更多管理」当前项补充 `aria-current`，移动查询搜索框补充 `type="search"` / `enterKeyHint="search"` / 明确 `aria-label`，筛选 chips 和 bottom sheet 选项补充 `aria-pressed`；该阶段只增强玻璃控件的可访问状态表达和移动键盘意图，不改变搜索或筛选口径。
- **已落地登录入口分段语义收口**：登录 / 注册分段控件补充 `role="group"` 与“登录方式”标签，两个分段按钮用 `aria-pressed` 同步当前模式，避免视觉上高亮但辅助技术无法判断当前处于登录还是注册申请。该阶段只增强入口页选中态语义，不改变登录 / 注册切换和提交逻辑。
- **已落地登录入口反馈语义收口**：登录错误使用 `role="alert"` / `aria-live="assertive"`，注册申请失败走 assertive alert，成功或普通提示走 `role="status"` / `aria-live="polite"`；提交按钮同步 `aria-busy`，让等待、失败和成功反馈不只停留在视觉文字。该阶段只增强状态播报和提交中语义，不改变登录、注册或审批逻辑。
- **已落地移动弹层触发器语义关联**：底部「更多」和移动查询「筛选」按钮补充 `aria-expanded` / `aria-controls`，分别指向稳定的更多管理面板和筛选面板 id；「更多」触发器只表达展开状态，当前页面仍由具体页签或面板项表达。该阶段只增强毛玻璃 bottom sheet 的语义连接，不改变展开、筛选或跳转逻辑。
- **已落地移动弹层焦点圈定**：底部「更多管理」和移动查询「筛选客户」打开后焦点进入弹层关闭按钮，Tab / Shift+Tab 被限定在弹层内，关闭后焦点回到对应触发按钮，避免键盘 / 辅助技术用户打开、浏览或关闭毛玻璃弹层后丢失操作位置；该阶段只增强交互闭环，不改变弹层内容或筛选 / 跳转逻辑。
- **已落地移动弹层焦点逻辑组件化**：新增 `components/useMobileSheetFocus.ts` 统一承载焦点进入、Tab 圈定、Escape 退出和焦点返回；后续新增毛玻璃 bottom sheet、筛选面板或轻管理 action sheet 时，应优先复用该 hook，再补面板自身标题、触发器和业务动作。
- **已落地楼宇移动 action sheet 焦点闭环**：楼宇单元「快速处理」和批量状态处理弹层复用 `useMobileSheetFocus`，让毛玻璃 action sheet 具备焦点移入、Tab 圈定、Escape 退出和触发器焦点返回；该阶段只增强移动端可访问性与操作确定性，不改变房源状态或合同锁定规则。
- **已落地楼宇移动 action sheet 触发器语义**：楼宇轻管理的「快速处理」和批量「处理 N」触发器补齐 `aria-expanded` 与 `aria-controls`，对应面板补稳定 id，确保毛玻璃 bottom sheet 不只靠视觉弹出表达关系；该阶段只增强辅助技术理解，不改变状态处理或保存逻辑。
- **已落地关注字段设置触发器语义**：关注字段「自定义」按钮与空状态添加入口补齐 `aria-expanded` / `aria-controls`，统一指向稳定的 `dashboard-custom-field-settings-panel`；该阶段只让毛玻璃设置弹层拥有清晰触发关系，不改变关注字段内容、同步偏好或计算口径。
- **已落地楼宇移动 action sheet 组件化测试**：楼宇快速处理与批量状态处理弹层抽到 `components/BuildingMobileActionSheets.tsx`，并新增渲染测试覆盖标题、关闭、状态动作与口径说明；后续楼宇轻管理视觉调整应优先改该组件，而不是继续加大 `BuildingManager` 的 JSX 体量。
- **已落地楼宇移动轻管理字号收口**：楼宇快速处理、批量状态处理和单元编辑底部抽屉里的状态胶囊、已选单元、合同锁定说明和锁定徽标都达到 `text-xs` 可读下限；毛玻璃 sheet 内不再用 11px 灰字承载关键房源状态。该阶段只提升移动轻管理文字清晰度，不改变房源状态、合同控制或面积统计口径。
- **已落地楼宇平面 / 列表扫读字号二次收口**：楼宇切换、平面图图例、楼层标签、移动房源楼层标识和桌面列表状态不再使用 11px / slate-400 弱灰字；楼宇内容画布继续保持低噪，但关键扫读信息按 12px 可读下限处理。该阶段只优化楼宇玻璃内容层清晰度，不改变面积、出租率、状态或租户引用口径。
- **已落地上线验收清单**：新增 `docs/10-移动端与字段口径上线验收清单.md`，把毛玻璃移动端截图 QA、字段口径来源、历史欠费迁移、关注字段偏好、预算 / 楼宇轻管理和部署前回归命令收束为上线门槛，并纳入 `npm run qa:mobile-ui` 守卫。
- **已落地上线回归一键化**：新增 `npm run qa:rollout`，将移动毛玻璃守卫、关键字段 / 移动组件测试、验收记录脚本测试、类型检查和生产构建串成部署前单入口，减少上线前漏跑截图 QA 以外基础校验的风险。
- **已落地上线验收记录模板**：新增 `docs/11-移动端与字段口径上线验收记录模板.md`，让每次上线都能记录移动 / 平板截图、毛玻璃文字可读性、封账 dry-run、云端接口、灰度观察和最终结论，避免只用自动命令替代真实视觉验收。
- **已落地上线验收记录生成器**：新增 `npm run qa:rollout:record`，上线前可一键生成验收记录草稿，自动带入日期、分支和 commit，方便后续补齐截图与生产环境验证证据。
- **已落地上线验收记录完整性检查**：新增 `npm run qa:rollout:record:check`，用于检查记录中是否还残留关键占位选择，提醒补齐移动截图、接口、dry-run、灰度观察和上线结论。
- **已落地验收记录脚本回归**：新增 `scripts/__tests__/rolloutRecordScripts.test.ts`，覆盖生成记录不泄漏模板说明、同日多次生成不覆盖、未填草稿失败和填完记录放行，保证发布留痕工具本身可回归。
- **已落地移动截图 QA 辅助命令**：新增 `npm run qa:mobile-screenshots -- --url http://127.0.0.1:5173/`，批量输出 375x812、390x844、430x932、768x1024、1024x768 五个基础视口截图和报告到 `output/mobile-screenshot-qa/`；该命令用于快速留存毛玻璃首屏和断点证据，仍需人工补充更多入口、筛选 sheet、月度明细、预算编辑和楼宇 action sheet 的状态截图。
- **已落地移动端深色系统可读性修复**：移动工作台在 `ios-liquid-app.mobile-typography-guard` 下增加浅色 readable 玻璃的深色文字兜底，覆盖顶部上下文、KPI 辅助字段、关注字段、今日待办、空态和底部导航；该修复针对 iPhone 深色外观下全局 slate 文本被改浅导致的白字叠白玻璃问题，不改变任何字段计算口径。
- **已落地移动登录页深色可读性修复**：登录 / 注册页的 `liquid-auth-panel`、分段控件和输入框在深色系统下保持浅色 readable 玻璃与深色正文，避免未登录首屏出现白色标题、浅色标签或占位符压在浅灰毛玻璃上的问题；该阶段只修正入口页材质和对比度，不改变登录、注册或权限加载逻辑。
- **已落地移动登录入口触控守卫**：登录 / 注册输入框和园区多选项纳入统一 44px 命中区与蓝色 `focus-visible` 焦点环，避免入口页虽然可读但输入框、园区选项在手机和键盘操作下不好点 / 不好定位；该阶段只增强触控与焦点，不改变登录、注册或审批逻辑。
- **已落地移动登录入口表单语义守卫**：登录 / 注册的邮箱、姓名、密码字段补齐 `label htmlFor` / `id` 关联，邮箱输入声明 `inputMode="email"`、关闭自动首字母大写和拼写检查，密码输入按登录 / 注册声明 `go` / `done` 完成键，园区多选项在 checkbox 聚焦时显示整行焦点环；该阶段只增强移动键盘、表单语义和键盘定位，不改变提交或审批逻辑。
- **已落地登录入口表单标题关联守卫**：登录 / 注册 `<form>` 补齐 `aria-labelledby="auth-form-title"` 与 `aria-describedby="auth-form-description"`，可见标题和辅助说明拥有稳定 id；该阶段只增强入口表单的名称与说明关系，不改变展示文案、切换或提交逻辑。
- **已落地注册入口园区多选语义守卫**：注册申请的园区多选区补充可见标签关联、`role="group"`、加载态 `aria-busy`、已选数量 polite 播报、加载 / 空态 status 播报，并为每个园区 checkbox 提供项目编号说明；该阶段只增强园区选择的可理解性和状态反馈，不改变可选园区来源或 `requestedProjectIds` 写入逻辑。
- **已落地移动端工作台玻璃承托二次收口**：年度收款达成主 KPI 从透明蓝底控件改为 `liquid-mobile-primary-metric` 浅色可读玻璃卡，缺口胶囊改为 `liquid-mobile-primary-badge`；顶部上下文、底部导航标签和主 KPI 辅助文案抬到 12px 可读下限。该阶段遵循“玻璃服务层级，内容必须先可读”的原则，只调整材质、字体和对比度，不改变年度达成或收款缺口计算。
- **已落地移动端 PC 同源阅读层加固**：手机首屏新增 `--liquid-mobile-readable-fill / border / shadow` 统一承托变量，工作台主壳、hero、年度收款达成、三列辅助指标、今日待办、关注字段与顶部上下文都回到高不透明白色 readable 玻璃；蓝色仅作为主数字、进度、选中态和少量状态强调，避免大面积蓝色毛玻璃在真机截图和低亮度环境下吞掉文字。本阶段只优化材质清晰度和移动可读性，不改变 KPI、待办、关注字段或月度明细计算。
- **已落地移动工作台真机可读层加固**：移动工作台不再只依赖通用 `liquid-mobile-card / hero`，而是新增 `liquid-mobile-dashboard-shell / hero / section`、移动顶栏 `liquid-mobile-toolbar-shell` 与底栏 `liquid-mobile-bottom-readable`，并把这些类纳入深色系统、高对比、降低透明度和源码守卫；毛玻璃保留 iOS 式边缘、阴影和背景折射，但正文始终落在近白 readable 层上。该阶段只优化移动端字体清晰度、PC 对齐和材质稳定性，不改变任何 KPI、待办、关注字段或月度明细口径。
- **已落地移动首屏显式字号收口**：移动工作台顶部上下文、hero 状态、今日待办范围和年度达成微指标标签直接使用 `text-xs`，不再依赖 `text-[11px]` 被全局兜底抬升；该阶段让毛玻璃承载层里的核心辅助文案更稳定可读，不改变任何字段口径。
- **已落地移动首屏显式字号二次收口**：固定 KPI 辅助胶囊与关注字段说明同步改为 `text-xs`，让首屏 readable 玻璃上的状态、字段限制和同步文案直接达到 12px 可读下限；该阶段只提升文案辨识度，不改变 KPI 或关注字段逻辑。
- **已落地工作台提醒卡片字号收口**：发票风险、关键时刻、本月关怀、下月预告、生日和对接人徽标都不再使用 10px / 11px；这些提醒虽然不是主 KPI，但属于移动工作台行动信息，必须按 12px 可读下限处理。该阶段只增强提醒卡片玻璃层清晰度，不改变开票或关键时刻计算。
- **已落地移动入口与初始化字号收口**：注册园区选择里的已选数量、可见园区编号、初始化数据入口 chips 和欠款口径徽标都直接使用 `text-xs`；这些信息虽然是辅助层，但在毛玻璃入口和移动管理流里仍按 12px 可读下限处理。该阶段只提升文字清晰度，不改变登录、注册、园区授权或初始化字段口径。
- **已落地移动关注字段卡摘要语义**：横滑关注字段卡保留 iOS 式轻玻璃和 `truncate` 视觉节奏，但卡片本体必须暴露由字段名、resolver 数值和辅助说明组成的完整 `aria-label/title`，子文本也保留独立 `title`；该规则只增强小屏截断后的可理解性，不允许为移动端新增字段计算路径。
- **已落地工作台辅助表字号收口**：最新签约、到期预警、预算执行和年度经营对比这些首页辅助表不再使用 11px / slate-400 弱灰字承载日期、空态、预算 / 实收 / 同比标签；毛玻璃辅助表可以比主 KPI 更轻，但仍必须按 12px 可读下限处理。该阶段只优化 `liquid-shared-*` 表格与移动卡片阅读性，不改变工作台辅助数据来源。
- **已落地工作台预算执行明细字号收口**：`StatsCards` 的月度预算执行卡、完成率胶囊、累计达成胶囊、合计区和累计欠款说明不再使用 10px / 11px 或 slate-300 / slate-400；首页核心经营数据即使在密集表格中也必须优先可读。该阶段只优化 `liquid-workbench-*` 阅读层，不改变预算执行计算。
- **已落地移动模块顶部 readable 收口**：客户查询、合同轻管理和财务核销入口的移动 `liquid-mobile-hero` 不再使用白字叠浅玻璃，统一为深色主标题、深色辅助说明和浅色玻璃控件；搜索 chips、筛选、清除等控件保留 iOS 式半透明边缘，但内容层回到可读白底。该阶段只优化顶部模块 chrome 的毛玻璃对比度，不改变任何查询、筛选、合同或财务口径。
- **已落地共享客户选择器字号收口**：共享客户选择器里的合同编码 / 房号 / 楼宇副信息、重选按钮、空态和默认展示范围提示都不再使用 10px / 11px 灰字；搜索输入同时声明 search 键盘动作。该阶段只优化 elevated 毛玻璃表单控件的手机可读性，不改变检索字段、候选排序或选择结果。
- **已落地移动合同卡片字号收口**：合同移动卡片继续保留轻玻璃和紧凑信息密度，但楼层条、合同状态徽标、账期 / 特殊业态 / 免租处理胶囊、字段网格都不再使用 10px / 11px；物业费共用标签在移动和平板用 `text-xs`，桌面宽屏再回到 10px 紧凑态。该阶段只按移动阅读下限优化字体，不改变合同、物业费或账期字段口径。
- **已落地移动合同概要弹层字号收口**：合同概要弹层的账单预览卡、当前账期 / 预算新增 / 金额调整等状态徽标和预算叠加标签在手机上都使用 `text-xs`，桌面表格才回到 `md:text-[10px]` 的紧凑徽标。该阶段只优化 elevated 毛玻璃弹层里的文字清晰度，不改变账单匹配、金额、覆盖租期或预算叠加口径。
- **已落地移动发票卡片字号收口**：发票排期的平板 sticky 预览、状态胶囊、四项日期 / 视图指标和手机发票卡预测胶囊统一达到 `text-xs`；手机日期块标签从浅灰普通字改为更强的 `font-black text-slate-500`。该阶段只增强 readable 玻璃层上的文字清晰度，不改变开票计划生成、预测禁用、确认 / 恢复、延期或预算归属月逻辑。
- **已落地发票状态字号二次收口**：发票桌面表格和移动卡里的预测 / 已开票时间不再使用 10px 或 slate-400 弱灰字；状态类信息即使在桌面宽表中也按 12px 可读下限处理。该阶段只优化发票玻璃状态层清晰度，不改变开票计划、状态切换或延期逻辑。
- **已落地客户来源分析字号收口**：来源分析 KPI、手机来源卡小指标、展开合同副信息和空态不再使用 10px / 11px 或 slate-400 弱灰字；分析型页面允许保持密度，但解释来源稳定性的标签必须先清楚。该阶段只优化 `liquid-analysis-*` 玻璃层阅读性，不改变来源指标或稳定指数算法。
- **已落地移动财务卡片字号收口**：财务移动收款流水、应收核销和手机核销入口里的类型胶囊、账期、缓缴流向、预算说明、备注、筛选统计和本月摘要都从 9/10/11px 收口到 `text-xs`；标签文字使用更稳的 slate-500 加粗，而不是在玻璃层上继续用浅灰小字。该阶段只提升阅读清晰度，不改变应收、已收、待收、缓缴或核销写入逻辑。
- **已落地财务平板预览 / 概览字号二次收口**：财务主从预览、特殊业态录入卡、财务概览卡和缓缴说明里的辅助标签不再使用 10px / 11px 弱灰字；平板和手机横屏按移动阅读下限处理，桌面宽表仍可保留紧凑密度。该阶段只优化毛玻璃财务卡片层级，不改变流水、应收、特殊业态或缓缴计算。
- **已落地移动预算卡片字号收口**：预算主列表卡、月度明细抽屉和预算 / 实收 / 差额标签统一按 12px 可读下限处理；“无特殊标记”等辅助文案也用 slate-500 加粗，避免在透明玻璃背景上变成低对比灰字。该阶段只优化预算移动端的阅读层级，不改变预算、实收、累计或差额口径。
- **已落地预算管理移动 / 平板阅读层收口**：预算行卡不再只依赖视觉截断，完整 `aria-label/title` 会汇总客户、楼宇房号、年度预算、累计实收和达成率；空预算月份、空置去化预测、应收专用说明、方案继承说明和导入预览提示也统一回到 12px readable 文字层。该阶段只优化预算管理毛玻璃阅读和辅助技术理解，不改变预算、实收、空置去化、导入或调整口径。
- **已落地工作台账单明细阅读层收口**：账单明细卡片和汇总区不再用 10px 灰字承载缓缴、预算对齐、备注和应收 / 实收标签；卡片完整摘要通过 `aria-label/title` 保留，视觉上继续保持轻玻璃层，内容文字回到 12px readable 层。该阶段只优化工作台账单明细毛玻璃阅读性，不改变应收、实收、手工差额、租金 / 物业费或备注口径。
- **已落地系统与备份阅读层收口**：系统设置、云端备份、本地数据和账号管理里的小按钮、数量角标、项目编号和备份时间不再使用 10px / 11px 弱灰字；这些维护动作虽然低频，但错误成本高，毛玻璃层必须优先清楚而不是追求密度。该阶段只优化维护型界面的阅读性，不改变连接、审批、备份、导入导出或账号写入口径。
- **已落地财务应收核销表格阅读层收口**：应收来源、缓缴流向、预算对齐、原账面缓出、合计脚注和流水编号不再用 10px / slate-300 这类低对比层表达；财务表格可以保持密度，但账期、结算和核销解释必须回到 12px readable 层。该阶段只优化财务毛玻璃表格阅读性，不改变核销、缓缴、结算调整或流水写入口径。
- **已落地客户洞察阅读层收口**：客户洞察的搜索空态、未选客户空态、系统生成徽标、名称变更箭头和付款周期变更箭头统一回到 `text-xs` / slate-500 以上；时间轴仍保留轻玻璃和蓝色动作语义，但正文、状态和变更关系不再依赖弱灰小字。该阶段只优化分析入口可读性，不改变 AI 动态采集、客户筛选、动态排序、园区周年生成或变更记录展示逻辑。
- **已落地 AI 助手抽屉阅读层与语义收口**：助手浮层里的实时状态、消息时间和输入提示回到 12px readable 层，用户气泡时间使用更高对比蓝底文字；抽屉同时补齐 dialog / modal 语义、可见标题关联、Escape 关闭和加载状态播报。该阶段只优化毛玻璃浮层的阅读与可访问性，不改变 AI 分析或消息发送逻辑。
- **已落地图表轴与空态阅读层收口**：通用趋势图和来源分析图的坐标轴 tick、趋势徽标、图表空态，以及虚拟滚动表默认空态统一回到 12px / slate-500 以上；毛玻璃图表可以轻，但坐标轴和暂无数据提示不能变成装饰灰字。该阶段只优化图表阅读层，不改变图表 dataKey、tooltip formatter、来源分析指标或虚拟滚动实现。
- **已落地合同轻变更弹层阅读层查漏补缺**：名称变更和付款周期变更弹层里的箭头、账单预览分界、计算中 / 无账单状态与更多账单提示全部回到 slate-500 / blue-700 可读层；确认型弹层不得用弱灰小字表达状态关系。该阶段只优化 elevated 弹层细节，不改变变更记录、账单预览或保存结构。
- **已落地移动客户查询阅读层查漏补缺**：移动客户查询的搜索图标、搜索占位符和到期月份数量统一回到 slate-500 可读层；搜索入口和筛选 sheet 继续保留 iOS 式轻玻璃，但文字不再降到弱灰装饰层。该阶段只优化查询入口可读性，不改变搜索索引、筛选、结果数量或跳转逻辑。
- **已落地移动底部导航阅读层查漏补缺**：底部“更多管理”面板方向箭头回到 slate-500 可读层，并把移动导航组件纳入弱灰 / 小字号源码守卫；常驻导航属于 iOS 毛玻璃 shell，图标、标签和方向提示都不能变成低对比装饰层。该阶段只优化导航层可读性，不改变底部导航、更多入口或焦点管理逻辑。
- **已落地移动月度明细合计卡阅读层查漏补缺**：移动「月度收款明细」深色合计卡的辅助说明和累计达成值不再使用 text-white/70、text-white/78 低透明白字，统一提升到 text-white/85、text-white/90。该阶段只优化深色玻璃卡片上的对比度，不改变月度明细计算或弹层交互。
- **已落地移动登录 / 注册入口阅读层查漏补缺**：邮箱、姓名、密码输入的图标和占位符统一回到 slate-500 可读层；入口表单可以保持轻玻璃和简洁留白，但输入意图不能靠低对比灰字表达。该阶段只优化登录 / 注册面板可读性，不改变认证、注册审批或园区选择逻辑。
- **已落地初始化数据录入单位提示阅读层查漏补缺**：初始化数据录入中的“￥ / %”单位提示统一回到 slate-500 可读层；单位符号属于输入语义的一部分，不应在毛玻璃表单里被处理成弱灰装饰。该阶段只优化输入单位清晰度，不改变初始化数据保存或计算逻辑。
- **已落地 App 外壳状态与图标阅读层查漏补缺**：启动 / 工作台加载文字、同步中状态、移动侧栏关闭、顶栏用户和云端保存操作人员图标统一回到 slate-500 可读层；毛玻璃外壳的状态反馈和入口图标也必须清楚，不应被当成装饰灰处理。该阶段只优化全局 chrome 可读性，不改变任何业务写入或同步流程。
- **已落地侧栏品牌区字号查漏补缺**：Kingdee 侧栏里的 `V4.0` 和 `Software Park` 不再使用 10px 小字，统一进入 `text-xs` 可读下限；品牌辅助信息可以低调，但在毛玻璃侧栏展开时仍必须清楚。该阶段只优化 chrome 品牌层，不改变侧栏交互或导航结构。
- **已落地月度收款明细桌面宽表空值状态查漏补缺**：桌面宽表里的空完成率和合计行同比占位不再使用 slate-300，统一提升到 slate-500；空值可以弱于主数据，但不能在毛玻璃表格里淡到不可辨。该阶段只优化空值阅读层，不改变月度明细计算或表格结构。
- **已落地移动高对比系统可读性守卫**：`prefers-contrast: more` 兜底范围补齐登录 / 注册页、移动年度达成主 KPI、缺口胶囊、三列 KPI、关注字段外壳、底部导航和月度明细桌面表格壳；这些内容层在系统高对比偏好下转为近不透明白底和深色正文，避免透明材质压低文字对比度。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段不改变任何 KPI、月度收款或关注字段计算口径。
- **已落地移动降低透明度可读性守卫**：`prefers-reduced-transparency: reduce` 兜底范围同步补齐登录 / 注册页、移动年度达成主 KPI、缺口胶囊、三列 KPI、今日待办、关注字段外壳、底部导航、筛选项和月度明细表格壳；系统要求降低透明度时，这些关键内容层会取消实时模糊并使用更实的背景。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只响应系统可访问性偏好，不改变字段计算口径。
- **已落地移动减少动态效果守卫**：`prefers-reduced-motion: reduce` 兜底从移动卡片进入动画扩展到 `animate-in` 弹层、`animate-spin` 加载旋转、跑马灯、pulse 状态和 Tailwind transition 工具类；月度明细、移动筛选 sheet、更多管理 sheet、抽屉和 elevated 弹层在减少动态效果时不再滑入 / 缩放 / 淡入，加载图标保留静态状态。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只响应系统动效偏好，不改变加载、跳转、弹层或业务写入逻辑。
- **已落地移动入场动画可读性兜底**：`mobile-card-in` 第一帧和 `mobile-card-enter` 默认终态都保持可见，并在 `prefers-reduced-motion: reduce` 下强制 `opacity: 1` / `transform: none`，避免毛玻璃卡片因动画被禁用或中断而停在透明初始帧。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只保障文字可读，不改变任何字段计算口径。
- **已落地截图报告证据校验**：新增 `qa:mobile-screenshot-report`，对移动截图 QA 的 `report.json` 做结构化检查，要求登录态、`requireAuthenticated=true`、登录后壳 selector、浅色+深色、默认+减少动态、五个基础视口、截图命令成功和截图文件存在都成立；上线记录检查会自动复用该校验，防止毛玻璃可读性问题只用弱截图证据带过。
- **已落地移动截图 QA 深色 / 动效 / 登录态参数**：截图辅助命令支持 `--color-scheme light|dark|both`、`--reduced-motion no-preference|reduce|both` 和 `--storage-state 登录态文件`，可在同一次验收里留存浅色 / 深色、默认动态 / 减少动态基础视口截图；需要验证登录后毛玻璃工作台、更多入口、月度明细或楼宇 action sheet 时，应加载登录态文件，避免截图证据停留在登录页。
- **已落地登录态保存辅助命令**：新增 `npm run qa:mobile-auth-state`，把手动登录后的 Playwright storage state 保存到 `output/mobile-auth-state/`，并配合 `.gitignore` 防止本机会话凭据进入仓库；后续移动毛玻璃视觉验收应优先用该登录态抓真实业务页面，并在截图命令中追加 `--require-authenticated`，防止登录态失效时误把登录页当作验收截图。
- **已落地云端保存弹层输入提示查漏补缺**：云端保存 elevated 弹层中的操作人员和备份备注 placeholder 统一回到 slate-500 可读层；毛玻璃输入框可以轻，但输入意图不能依赖浏览器默认浅灰。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化保存弹层阅读层，不改变云端保存或同步写入口径。
- **已落地财务移动搜索提示查漏补缺**：财务应收核销移动搜索条里的搜索图标和“搜索客户” placeholder 回到 slate-500 可读层；搜索入口要像 PC 工具条一样清楚可扫，不应在 iOS 玻璃白底上使用弱灰。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化搜索入口阅读层，不改变应收筛选或核销写入口径。
- **已落地共享客户选择器提示查漏补缺**：复用在财务记账 / 押金转租金等弹层里的客户选择器，副信息分隔符提升到 slate-400，搜索 placeholder 提升到 slate-500；共享输入组件必须和 PC 端 readable 表单同层级，而不是靠弱灰提示维持轻感。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化选择器阅读层，不改变客户检索或选择回调。
- **已落地合同移动搜索提示查漏补缺**：合同轻管理移动搜索条里的搜索图标和“搜索企业名称” placeholder 回到 slate-500 可读层；合同与财务两条高频移动检索入口保持同一 PC readable 表单层级。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化搜索入口阅读层，不改变合同筛选、预览或写入口径。
- **已落地合同移动 / 平板预览阅读与触控收口**：合同主从预览里的“合同快速预览 / 历史退租合同”、合同状态和面积 / 租金 / 月租 / 付款周期标签都回到 `text-xs`，查看详情 / 续签 / 退租 / 回退按钮统一到 `min-h-11`。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化预览层的文字清晰度和手指命中区，不改变合同详情、续签、退租、回退或筛选联动逻辑。
- **已落地合同 PC / 平板搜索提示查漏补缺**：合同工具条搜索框的“搜索企业名称…” placeholder 回到 slate-500，可读层级与移动合同搜索一致；同一个合同检索入口不因断点变化而回到弱灰提示。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化搜索入口阅读层，不改变合同筛选、批量操作或写入口径。
- **已落地 AI 识别弹层输入提示查漏补缺**：AI 合同识别与 AI 收款识别的 elevated 输入 placeholder 回到 slate-500，识别结果备注输入也保持同一可读层；AI 弹层可以保持轻透，但输入意图不能靠弱灰提示表达。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化输入提示阅读层，不改变 AI 解析、匹配或导入口径。
- **已落地 AI 收款匹配下拉箭头查漏补缺**：AI 收款识别中客户匹配 select 的移动卡片箭头和桌面表格箭头都回到 slate-500；下拉箭头是操作提示，不应在 elevated 玻璃表单里变成弱灰装饰。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化 select affordance，不改变匹配或导入口径。
- **已落地 AI 上传区主图标查漏补缺**：AI 合同 / 收款识别的截图上传和 Excel 上传 Dropzone 主图标回到 slate-500；上传入口是第一操作，不应在毛玻璃弹层里像背景装饰一样变浅。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化上传入口视觉信号，不改变上传、解析或识别口径。
- **已落地移动首页次级 KPI 材质收口**：移动工作台的出租率、累计欠款、客户数字段从浅分割区改为独立 readable 玻璃小卡，关注字段外壳和字段卡也提升为更实的 readable 材质；这些字段不是主视觉，但属于经营判断信息，不能在蓝色玻璃背景上被透明度冲淡。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化材质与可读性，不改变 KPI 或自定义字段计算口径。
- **已落地移动更多管理触控与材质收口**：更多管理 bottom sheet 的关闭按钮回到 44px，管理入口行从轻透明层提升到 readable 玻璃卡片，且每个入口都有完整 `aria-label/title`；底部导航是移动端核心信息架构，不能只靠轻透质感和截断文字表达模块入口。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化入口触达与可读性，不改变导航权限或跳转逻辑。
- **已落地楼宇移动房源卡操作触控收口**：楼宇移动卡片与批量工具条中的批量、全选 / 清空、取消、处理 N、选择、快速处理和编辑按钮全部从源码层统一到 `min-h-11`，不再只依赖全局 `mobile-pressable` 兜底。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化手机楼宇轻管理的触达稳定性，不改变房源状态、批量二次确认、合同锁定、面积统计或保存逻辑。
- **已落地楼宇移动 action sheet 触控与说明语义收口**：楼宇快速处理和批量状态处理 bottom sheet 的关闭按钮、取消 / 返回 / 退出批量按钮统一达到 44px 触控目标，并通过 `aria-describedby` 把可见说明绑定到弹层；房源上下文额外保留完整 `title`，避免小屏截断后难以确认当前房源。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化移动毛玻璃弹层触达、说明和可读性，不改变房源状态、合同锁定或保存逻辑。
- **已落地财务移动 / 平板预览操作触控收口**：财务主从预览与手机横屏场景中的修改、删除、保存、合同概要、清除、收款、缓缴、缓缴撤销和撤销核销按钮全部从源码层统一到 `min-h-11`，不再只依赖全局触控兜底。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化财务毛玻璃操作按钮的触达稳定性，不改变流水编辑、特殊业态应收、核销、缓缴或撤销逻辑。
- **已落地移动客户查询结果 / 筛选操作触控收口**：客户查询顶部「筛选 / 清除」按钮、筛选 sheet 关闭按钮、结果卡里的合同 / 核销按钮、空态清除 / 回到指标按钮，以及长结果显示更多 / 收起结果按钮全部从源码层统一到 `min-h-11` 或 `h-11 w-11`，不再只依赖全局触控兜底。`npm run qa:mobile-ui` 与组件渲染测试已纳入该规则检查，本阶段只优化查询顶部操作、结果与筛选弹层的手指命中区，不改变搜索、筛选、结果分页、合同定位或核销跳转逻辑。
- **已落地移动首页年度切换触控收口**：移动工作台年度左右步进按钮从 `h-9 w-9` 提升到 `h-11 w-11`，让顶部主 KPI 旁的高频切换控件在源码层直接达到 Apple 44pt 触控目标，不再只依赖全局 `mobile-pressable` 兜底。`npm run qa:mobile-ui` 已纳入该规则检查，本阶段只优化年度切换触达和焦点稳定性，不改变年度筛选、KPI 重算或缓存读取逻辑。
