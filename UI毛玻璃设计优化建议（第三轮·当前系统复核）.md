# UI 毛玻璃设计优化建议（第三轮 · 当前系统复核）

> 评估对象：招商管理看板前端当前代码（`index.css` 现 5266 行 / 421 个 `liquid-*` 类）
> 评估视角：Apple Liquid Glass / 毛玻璃设计规范
> 评估日期：2026-06-24（基于已落地前两轮建议后的最新代码）

---

## 一、先确认：上两轮建议已基本落地（复核通过）

逐项核对当前代码，以下已修复，**本轮不再重复**：

- `index.html` 已加 `viewport-fit=cover` → 安全区适配真正生效。
- 已引入 `dvh`（css 6 处 + tsx 8 处），核心外壳用 `min-h-[100dvh]`。
- 移动手感四件套到位：`-webkit-tap-highlight-color`、`overscroll-behavior`、`touch-action`、`scrollbar-gutter` 均已出现。
- **暗色模式已搭建**：`@media (prefers-color-scheme: dark)`（约 197 行），核心玻璃走 token 自动切换 + 对 Tailwind `text-slate-*`/`bg-white/*` 做兜底覆盖。
- `prefers-contrast` 高对比兜底已加。
- **焦点态已补**：`:focus-visible` 现覆盖 `liquid-pressable / mobile-pressable / liquid-glass-control / 底部导航 / 年份步进 / 登录字段`（`index.css:72`）。
- **设计令牌已起步**：新增 `--liquid-material-fill/border/sheen/shadow/highlight/edge-*`、`--mobile-touch-target:44px`、`--mobile-bottom-nav-height` 等 52 个变量。

基础已经扎实。本轮聚焦**「token 化只做了一半」与几个 Apple 玻璃的进阶规范**。

---

## 二、P0 · 高优先级（最影响一致性的残留项）

### P0-1　暗色模式「只深了一半」——组件级玻璃面仍硬编码浅色
- **证据（关键）**：全项目 **421 个 `liquid-*` 玻璃类，仅 7 个**引用了 `var(--liquid-material-*)` token；其余 ~414 个组件面仍硬编码浅色 rgba。抽查 `liquid-budget-data-cell`、`liquid-settings-card`、`liquid-building-overview-card`、`liquid-contract-mini-card` 全部是 `rgba(255,255,255,0.x)` 渐变 + 浅 slate 描边，且在暗色块中出现次数均为 **0**（未被纳入暗色覆盖）。
- **影响**：系统切暗色时，核心面板/侧栏/工具栏会变暗（走 token），但**预算单元格、设置卡片、楼宇卡片、合同 mini-card、workbench 单元格、发票表**等大量模块面仍是亮白玻璃 → 出现「半亮半暗」的割裂界面。当前暗色块靠枚举 Tailwind 工具类兜底，是「打地鼠」式覆盖，碰不到这些 bespoke 渐变。
- **方向**：把**所有玻璃变体统一改为引用 token**（按「高度/elevation」分级出 material-fill/border/sheen/shadow 几套语义变量，组件只选层级、不写颜色）。这样暗色自动继承，顺带把约 **1210 处 rgba 字面量**大幅收敛、根治维护问题。这是当前系统最该做的一件事。

---

## 三、P1 · 中优先级（与 Apple 规范的契合度）

### P1-1　玻璃 tint 偏「彩」，不够中性
- **证据**：材质里烘焙了约 **90 处蓝/青 tint** + **17 处琥珀 tint**（`rgba(14,165,233)`、`rgba(37,99,235)`、`rgba(245,158,11)` 等直接进了 base 材质的 background）。
- **Apple 规范**：Liquid Glass 以**中性/无彩**为主，颜色应来自「玻璃背后透出的内容」；tint 仅用于强调态。当前持续性的蓝/青/琥珀底色，观感更接近「品牌化 glassmorphism」而非 Apple 的中性材质。
- **方向**：默认材质改为中性（近无彩的白/深灰玻璃），把蓝/青/琥珀 tint **只保留给强调态**——激活导航项、主操作按钮、状态徽标，而非铺在每张卡片/单元格的底。

### P1-2　缺少「滚动自适应玻璃」（Apple 标志性行为）
- **证据**：`App.tsx` 中滚动处理 **0 处**；顶栏/头部玻璃透明度恒定。
- **Apple 规范**：导航栏/工具栏初始接近透明，**当内容滚到其下方时自动增不透明度并浮现 1px 分隔线**，以保证标题文字始终可读。
- **影响**：现在内容滚到玻璃顶栏下方时，半透明叠半透明，文字可读性会瞬时变差。
- **方向**：监听滚动，给 `liquid-glass-toolbar`/头部在「内容已滚动」时切换到更不透明的材质 + hairline 分隔线（轻量 JS + class 切换即可，配合已有 token 很顺）。

### P1-3　阴影/高度未成体系
- **证据**：目前仅 `--liquid-material-shadow` 一级阴影 token，其余组件各自硬编码 `box-shadow`（深浅、扩散、方向各不相同）。
- **影响**：缺统一「深度语言」，玻璃层叠时高低关系不一致，弱化了玻璃的空间层次感。
- **方向**：定义 `--elevation-1/2/3`（甚至 -nav/-sheet/-modal）阴影 token，所有玻璃按所在层级引用，明暗各一套。

---

## 四、P2 · 细节打磨 / 锦上添花

- **P2-1　同心圆角仍未系统化**：固定圆角 `[32]/[24]/[22]/[18]` 并存，无圆角 token，嵌套层未按「内圆角 = 外圆角 − 内边距」推导，玻璃层叠处弧线不贴合。建议出一套 `--radius-*` 并在嵌套时推导。
- **P2-2　玻璃叠玻璃仍普遍、`backdrop-filter` 增至 209 处**：内容层（表格/长列表）仍是玻璃而非不透明纸面，移动端多层实时模糊叠加有性能/发热成本。建议内容层不透明化、限制玻璃嵌套层数（玻璃只做导航/控件/悬浮层）。
- **P2-3　静态高光 → 可选的动态镜面 / vibrancy**：当前玻璃上的文字/图标是纯色平铺；可引入 vibrancy（标签颜色随背景微调、更"融"进玻璃）与随指针/滚动流动的镜面高光，作为高级感加分。
- **P2-4　focus ring 一致性收尾**：主控件已覆盖（好），可补齐链接、可读卡片表面等可聚焦元素，并确保**明暗两套下焦点环对比一致、带 1–2px offset**。

---

## 五、建议落地顺序

| 阶段 | 内容 | 说明 |
|---|---|---|
| 第一步（最高价值） | **P0-1 全量 token 化 + 暗色继承** | 一次性根治「半亮半暗」与 1210 处 rgba 维护问题，是当前系统的关键短板 |
| 第二步 | P1-1 材质中性化、P1-3 阴影分级 token | 都建立在 token 体系上，与第一步同源推进最省力 |
| 第三步 | P1-2 滚动自适应玻璃、P2-4 焦点环收尾 | 轻量交互增强，体感提升明显 |
| 第四步 | P2-1 同心圆角、P2-2 内容层不透明化、P2-3 动态高光 | 体系化与高级感收尾，按模块灰度 |

> 一句话总结：**基础规范本轮已基本达标，当前最大的残留是「token 化只覆盖了 421 个玻璃类里的 7 个」——导致暗色半亮半暗、且 rgba 难维护。把全部玻璃变体收归 token 体系（顺带材质中性化、阴影分级），是本轮性价比最高、最该先做的一步；其后再补 Apple 标志性的「滚动自适应玻璃」。**
