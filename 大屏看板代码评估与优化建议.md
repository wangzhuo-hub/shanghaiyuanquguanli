# 大屏看板代码评估与优化建议

评估日期：2026-05-16  
评估范围：用户手动新增/调整的大屏看板相关代码  
评估方式：代码审阅 + `npm run build` 构建验证

## 1. 总体结论

当前大屏方向是正确的：

- 已新增独立大屏入口：`?screen=tv`
- 已拆出大屏容器：`components/BigScreenDashboard.tsx`
- 已拆出大屏展示组件：`components/big-screen/*`
- 已拆出数据聚合、动态事件、智能预警服务：
  - `services/bigScreenMetrics.ts`
  - `services/bigScreenEvents.ts`
  - `services/bigScreenAlerts.ts`
- 指标计算总体上复用了 `services/dashboardMetrics.ts`，方向符合“不要另写一套口径”的原则。

但目前存在两个会影响大屏可用性的高优先级问题：

1. 智能预警页由于轮播页数计算不一致，实际永远播不到。
2. 大屏读取的云端配置 key 和本地缓存 key 与主应用不一致，可能导致连接配置和离线兜底失效。

建议先修复这两个问题，再继续优化数据口径和显示细节。

## 2. 构建验证

已执行：

```bash
npm run build
```

结果：

- TypeScript 编译通过。
- Vite 构建通过。
- 存在构建警告，但未阻断构建。

构建警告：

```text
Expected identifier but found whitespace
min-width: ${K?"120px":"95px"};
```

当前用 `rg 'min-width: \${'` 在源码中未找到对应文本，暂时不像本次大屏新增代码直接引入。建议后续单独排查构建产物来源或第三方样式注入来源。

## 3. 高优先级问题

### 3.1 智能预警页永远轮播不到

优先级：P1  
影响：大屏“智能预警”页面不会展示，预警能力看似实现但实际不可见。

相关文件：

- `components/BigScreenDashboard.tsx`

问题位置：

```ts
// Carousel rotation
const totalSlides = 3 + bigScreenData.parks.length; // summary + compare + parks + alerts
```

但渲染区使用：

```ts
const totalSlides = 2 + bigScreenData.parks.length;
const slide = currentSlide % totalSlides;
```

随后预警页条件是：

```tsx
{slide === 2 + bigScreenData.parks.length && (
  <BigScreenAlerts alerts={bigScreenData.alerts} />
)}
```

由于 `slide` 是对 `2 + parks.length` 取模，最大只会到 `1 + parks.length`，因此 `slide === 2 + parks.length` 永远不会成立。

建议修复：

统一 slide 总数，建议抽成一个函数或常量：

```ts
const getTotalSlides = (parkCount: number) => 3 + parkCount;
```

渲染处：

```ts
const totalSlides = getTotalSlides(bigScreenData.parks.length);
const slide = currentSlide % totalSlides;
```

键盘控制、轮播定时器、底部指示器全部使用同一个 `totalSlides`。

验收标准：

- 集团总览可播放。
- 园区对比可播放。
- 每个园区单页可播放。
- 智能预警页可播放。
- 底部指示器数量与实际页面数量一致。

### 3.2 大屏配置 key 和主应用不一致

优先级：P1  
影响：大屏可能无法读取主应用已保存的 PocketBase 配置，也无法正确读取当前园区本地缓存。

主应用当前 key：

```ts
const STORAGE_KEY = 'kingdee_park_data_v1';
const CLOUD_CONFIG_KEY = 'kingdee_park_cloud_config_v2';
const getParkStorageKey = (projectId: string) => `${STORAGE_KEY}:${projectId || 'unknown'}`;
```

大屏当前 key：

```ts
const getParkStorageKey = (projectId: string) =>
  `kingdee_dashboard_data_v2:${projectId || 'unknown'}`;
```

以及：

```ts
localStorage.getItem('cloud_config')
```

问题：

- `cloud_config` 与主应用 `kingdee_park_cloud_config_v2` 不一致。
- `kingdee_dashboard_data_v2` 与主应用 `kingdee_park_data_v1` 不一致。
- 大屏在非默认 PocketBase URL 场景下可能连错后端。
- 大屏 fallback 到 localStorage 时，大概率读不到主应用缓存。

建议修复：

把存储 key 抽成共享常量，例如新增：

```ts
// config/storageKeys.ts
export const STORAGE_KEY = 'kingdee_park_data_v1';
export const CLOUD_CONFIG_KEY = 'kingdee_park_cloud_config_v2';

export const getParkStorageKey = (projectId: string) =>
  `${STORAGE_KEY}:${projectId || 'unknown'}`;
```

然后 `App.tsx` 和 `BigScreenDashboard.tsx` 统一引用。

如果暂时不抽文件，也至少把大屏里的 key 改成与主应用一致。

验收标准：

- 主应用登录并保存配置后，访问 `?screen=tv` 能读取同一 PocketBase 地址。
- 断网或后端读取失败时，大屏 fallback 能读取当前园区本地缓存。
- 多园区切换后的缓存不会串园区。

## 4. 中优先级问题

### 4.1 `/api/integration/dashboard` fallback 返回 schema 不一致

优先级：P2  
影响：同一个接口在快照命中和快照缺失时返回结构不同，外部系统或后续大屏调用该接口时容易解析错误。

相关文件：

- `scripts/integration-gateway.ts`

当前逻辑：

- 命中 `pb_integration_snapshots` 时返回 `payload.dashboard`，通常是前端 `DashboardData` 形态。
- 快照缺失时直接返回 PocketBase 原始行：

```ts
dashboard: { tenants, buildings, units, payments }
```

这里的对象字段是 PB 原始字段，例如：

- `original_id`
- `lease_start`
- `building_id`
- `unit_ids`

而不是前端对象字段：

- `id`
- `leaseStart`
- `buildingId`
- `unitIds`

建议修复：

方案 A：fallback 也组装成 `DashboardData` 形态。  
方案 B：如果确实要返回原始行，则把字段名改为：

```ts
raw: { tenants, buildings, units, payments }
```

并避免与 `dashboard` 混用。

推荐方案 A，因为对外接口应尽量保持稳定。

验收标准：

- `/api/integration/dashboard` 无论是否命中快照，返回字段结构一致。
- 外部系统不需要根据 `source` 写两套解析逻辑。

### 4.2 “本月实收”口径需要明确

优先级：P2  
影响：大屏显示的“本月实收”可能被理解为自然月到账，但当前实现更接近“当前账期已核销/已收”。

相关文件：

- `services/bigScreenMetrics.ts`

当前逻辑：

```ts
const currentMonthCollected = billingDetails.reduce((s, b) => s + (b.amountPaid || 0), 0);
```

该值来自 `processedData.currentMonthBilling.amountPaid`，它是当前账期应收明细里的已收金额，受账期、提前收款、缓缴、核销 period 等规则影响。

这不是简单的：

```ts
payments.filter(p => p.date.slice(0, 7) === currentMonth)
```

建议优化：

同时展示两个指标：

| 指标 | 来源 | 含义 |
|---|---|---|
| 本月到账 | `payments.date` 在当前自然月，且 `status = Received` | 现金流口径 |
| 本账期已收 | `currentMonthBilling.amountPaid` | 应收核销口径 |

若大屏只保留一个指标，建议改名为“本账期已收”，避免经营会上误读。

验收标准：

- 财务人员能区分“自然月到账”和“账期核销”。
- 大屏指标名称与数据口径一致。

### 4.3 开票动态缺少客户名称

优先级：P2  
影响：最新动态里开票事件信息不完整，不便于大屏阅读。

相关文件：

- `services/bigScreenEvents.ts`

当前逻辑：

```ts
title: `${parkName} ${inv.tenantId ? '' : ''}已开票`
```

由于 `InvoiceRecord` 只有 `tenantId`，没有 `tenantName`，当前标题基本只能显示“某园区 已开票”。

建议修复：

在 `generateAllEvents()` 中传入租户列表，并建立 map：

```ts
const tenantNameById = new Map(tenants.map(t => [t.id, t.name]));
```

生成开票事件时：

```ts
const tenantName = tenantNameById.get(inv.tenantId) || inv.tenantId || '—';
title: `${parkName} ${tenantName} 已开票`;
tenantName;
```

验收标准：

- 开票动态显示园区、客户名、金额、日期。

## 5. 低优先级优化

### 5.1 未使用 import 和 state 可清理

相关文件：

- `components/BigScreenDashboard.tsx`
- `services/bigScreenEvents.ts`
- `services/bigScreenAlerts.ts`
- `components/big-screen/BigScreenEventTicker.tsx`

示例：

- `formatWan`, `formatPercent` 在 `BigScreenDashboard.tsx` 中未使用。
- `authUser`, `parks` state 目前只 set 未使用。
- `BillingDetail` 在 `bigScreenEvents.ts` 中未使用。
- `BigScreenEvent` 在 `bigScreenAlerts.ts` 中未使用。
- `useEffect` 在 `BigScreenEventTicker.tsx` 中未使用。

因为 `tsconfig` 中 `noUnusedLocals` 为 `false`，不会阻断构建，但建议清理，减少后续维护噪音。

### 5.2 大屏入口识别建议更精确

当前：

```ts
window.location.search.includes('screen=tv')
```

建议：

```ts
const params = new URLSearchParams(window.location.search);
const isBigScreen = params.get('screen') === 'tv';
```

这样可以避免 query 中其它参数意外包含 `screen=tv` 字符串。

### 5.3 事件时间解析建议用本地日期工具

当前 `new Date('YYYY-MM-DD')` 在浏览器中按 UTC 解析，跨时区可能出现一天偏差。项目里已有 `parseDateLocal` 相关逻辑，后续可考虑复用，尤其是合同到期和签约日期。

### 5.4 轮播区域可能出现内部滚动

当前 slide 区域：

```tsx
<div className="flex-1 min-h-0 overflow-auto">
```

大屏播放场景通常希望一屏内完整展示，避免电视端出现滚动条。建议后续用 Playwright 或浏览器实测 1920x1080、1366x768、移动横屏，确保不会遮挡底部动态条。

## 6. 数据准确性建议

### 6.1 坚持复用现有计算链路

当前 `buildBigScreenParkData()` 已复用：

```ts
calculateDashboardMetrics()
buildKpiSummaryFromProcessedData()
```

这是正确方向。后续应继续避免在大屏中直接用合同月租、收款表手写计算年度应收、出租率、欠款等核心指标。

### 6.2 逾期预警需避免误报

当前逾期预警使用 `currentMonthBilling`，方向正确。但后续建议进一步区分：

- `Overdue`：明确逾期，适合高亮。
- `Partial`：部分收款，是否逾期需要结合账期截止日。
- `Unpaid`：未收款，未必已经逾期。

建议不要把所有 `Partial` 和 `Unpaid` 都文案写成“逾期未收”。可改成：

| Billing 状态 | 建议文案 |
|---|---|
| `Overdue` | 逾期未收 |
| `Partial` | 部分未收 |
| `Unpaid` | 待收款 |

这样更符合财务表达。

### 6.3 低回款预警建议按月份进度精细化

当前规则：

```ts
timeProgress = ((now().getMonth() + 1) / 12) * 100;
```

这会在月初直接按整月进度计算。比如 5 月 1 日已经按 5/12 计算，可能偏激进。

建议改成按年内天数：

```ts
timeProgress = elapsedDaysOfYear / totalDaysOfYear * 100;
```

或者在经营场景中保留月份口径，但文案明确为“月份进度”。

## 7. 推荐整改顺序

建议按以下顺序处理：

1. 修复轮播页数，确保智能预警页可见。
2. 统一 localStorage 配置 key 和园区缓存 key。
3. 明确“本月实收”口径，必要时拆成“本月到账”和“本账期已收”。
4. 修复开票动态客户名。
5. 修复 `/api/integration/dashboard` fallback schema。
6. 清理未使用 import/state。
7. 用浏览器实测大屏 1920x1080 展示效果。

## 8. 验收清单

### 功能验收

- 访问 `?screen=tv` 能进入大屏。
- 已登录用户能读取对应授权园区。
- 集团管理员能看到多个园区。
- 园区用户只能看到当前园区。
- 大屏可自动轮播。
- 智能预警页能正常播放。
- 最新动态能滚动播放。
- 暂停/继续轮播可用。

### 数据验收

- 出租率与工作台一致。
- 年度实收与工作台一致。
- 合同应收与管理员汇总一致。
- 回款完成率与管理员汇总一致。
- 新签约动态来自 `Tenant.signingDate`。
- 收款动态只统计 `PaymentRecord.status = Received`。
- 开票动态只统计 `InvoiceRecord.status = Invoiced`。
- 到期预警排除已退租、已到期历史合同。
- 逾期/待收预警复用 `currentMonthBilling`。

### 稳定性验收

- `npm run build` 通过。
- 大屏连续播放 30 分钟无明显报错。
- 数据刷新失败时不清空上一版有效数据。
- 网络恢复后可重新刷新。

## 9. 结论

这次手动调整已经把大屏的主体框架搭起来了，整体方向值得继续推进。当前最需要先修的是“轮播页数”和“存储 key 不一致”两个问题。修完后，大屏的基础播放和数据读取会稳定很多，再继续打磨本月收款口径、开票动态和外部接口 schema。
