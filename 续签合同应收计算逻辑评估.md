# 续签合同「应收」计算逻辑评估

> 范围：北京园区 4-713 快乐庄园、3-377 楂酩电子。仅评估、定位问题，**未改动任何代码**。
> 数据来源：`pocketbase/pb_data/data.db`（实测）＋ `services/dashboardMetrics.ts` / `billingService.ts` / `receivableListHelpers.ts` / `components/ContractManager.tsx`（源码梳理）。

---

## 一、结论先说（TL;DR）

「续签后原合同应收时有时无」是**两类原因叠加**的结果：

1. **季度账期的"按月归集"是设计行为，不是续签独有的 bug。** 这两个合同都是季度付（Quarterly）。系统把整季应收只归集到收款当月（1/4/7 月），其余两个月该合同本月应收本来就是 0，且金额为 0 的行会被丢弃。**当前默认查询的是 6 月——不是收款月——所以原合同本月没有金额是正常的。**

2. **续签操作引入了 3 个真实缺陷**，放大了混乱、并在部分场景下确实造成原合同的应收/可见性异常，是真正需要处理的问题。

---

## 二、数据现状（实测）

| 合同 | id | 状态 | 租期 | rootId | 首期收款日 | 账期 |
|---|---|---|---|---|---|---|
| 原·快乐庄园 | `1v4ta96e3pupj46` | **Expired** | 2023-07-21 → **2026-07-20（未到期）** | 空 | 2023-07-20 | 季度 |
| 续·快乐庄园 | `31z51viz79bnvle` | Pending | 2026-07-21 → 2029-07-20 | **`t1776994815161_10`（悬空）** | **2026-06-03** | 季度 |
| 楂酩电子 | `zwbv51jf1uwgzil` | Active | 2023-07-07 → 2026-07-06 | 空 | 2023-07-05 | 季度 |

注：
- 原·快乐庄园已收 4 笔租金、楂酩已收 1 笔；续签合同 0 笔。
- **楂酩电子在库中没有续签记录、也没有 rootId**——若确实做过续签，可能未保存成功。

---

## 三、应收是怎么算出来的（调用链）

```
财务报表 getBillingDetails(year,month)
  └ buildBillingDetailsForPeriod                （dashboardMetrics.ts:805）
      └ getReceivableScenarioForYear            命中「2026应收款专用方案」(即使 is_active=0 也会被选中)
      └ mergeTenantsForReceivablePeriod         方案快照为底 + 实时合同按「变更月」叠加
      └ getBillingDetailsForPeriodInternal      逐租户算 amountDue
          └ calculateBudgetedReceivableForTenant
              └ generateBudgetedBills           生成每张账单(date/amount/覆盖期)
              └ receivableBudgetMonthForBill    按 bill.date 所在「月」归集应收
```

关键点：
- **每张账单只归到 `bill.date` 所在的那一个月**（`receivableListHelpers.ts:289`）。季度合同每季只有一张账单，所以**一个季度里只有 1 个月有应收，另外 2 个月为 0**。
- `getBillingDetailsForPeriodInternal`（`dashboardMetrics.ts:654`）：`amountDue` 与 `amountPaid` 都 ≈ 0 时**不生成该行**——这正是"原合同那行直接消失/没有金额"的直接原因。

按上表手工推算 6 月（当前默认账期）：
- 原·快乐庄园：季度收款月为 1/4/7 月，**6 月应收 = 0**（4 月已收整季，覆盖 4/20–7/20）。
- 续·快乐庄园：首期收款日 2026-06-03 → **首期应收落在 6 月**，因此 6 月有金额。
- 楂酩电子：季度收款月 1/4/7 月，**6 月应收 = 0**。

→ 6 月查询时：续签有钱、原合同无钱，视觉上像"续签把原合同的钱抢走了"，但这主要是账期归集 + 默认月份所致。

---

## 四、三个真实缺陷（需要处理）

### 缺陷 1：续签把原合同误标为「已到期 Expired」，但租期其实没到
- 原合同 `leaseEnd = 2026-07-20` 尚未到，状态却是 `Expired`。
- 连锁影响：
  - **合同中心「在租明细」会隐藏 Expired 合同**（`ContractManager.tsx:873`：`续租后的旧合同不应出现在在租明细`），原合同从列表中"消失"。
  - **口径不一致**：仪表盘"本月应收"**排除** Expired（`dashboardMetrics.ts:1284` 仅算 Active/Expiring/Pending），而财务报表应收**不排除** Expired——同一合同两处表现不同。
  - 系统用 `transitionContractStatuses`（`sharedUtils.ts:95-128`）在**每次指标重算时**把"未到期却 Expired"临时纠回 Active（`App.tsx:1878`）。这是**运行时纠正、未必回写数据库**，于是"重算前/重算后、刷新前/刷新后"表现不同——**这就是你感受到的"时有时无"。**

### 缺陷 2：续签链 rootId 断裂（悬空）
- 续签 `rootId = t1776994815161_10`，而原合同 `id = 1v4ta96e3pupj46`、`rootId` 为空 → **两者的"根"不相等**。
- `t1776994815161_10` 是旧的临时 id 形态，疑似续签创建时引用了原合同迁移到 PocketBase 之前的临时 id，迁移后没同步更新。
- 影响：应收核销的**跨合同匹配**靠 `payRoot === billRoot`（`receivableListHelpers.ts:194-196`）。链断后，**原合同上的收款无法核销续签的应收、反之亦然**。虽有"按客户名兜底"分支，但它**只在流水的 tenantId 已不在租户列表时才触发**（`:189`）；此处两条合同都还在列表里，兜底不生效。

### 缺陷 3：续签「首期收款日」早于「起租日」，把应收前拉一个月
- 续签 `firstPaymentDate = 2026-06-03`，但 `leaseStart = 2026-07-21`，首期应收因此落到 6 月。
- 需复核：该首期账单的**覆盖区间**是否与原合同最后一季（4/20–7/20）**重叠**——重叠会重复计应收，留缝会漏算。这是金额正确性的关键校验点。

---

## 五、评估意见与建议（不改代码，按优先级）

**1) 先分清"设计行为"还是"缺陷"。** 6 月原合同为 0 属季度归集的正常表现。请先切到**收款月（1/4/7 月）**核对原合同应收是否正确，再判断是否真异常，避免误判。

**2) 数据修复优先（比改代码更紧急、风险更低）：**
- 把原·快乐庄园状态从 `Expired` 改回 `Active`（其租期未到 2026-07-20）。
- 修复续签的 `rootId`，使其与原合同同根（指向 `1v4ta96e3pupj46`，或给原合同补同一个 rootId）。
- 复核续签首期收款日/起租日是否应当一致（建议核对是否应为 2026-07-21）。
- 楂酩电子若确需续签，补录续签合同（当前库内无记录）。

**3) 逻辑层面待统一（后续真正改代码时再做）：**
- **统一 Expired 口径**：仪表盘排除、财务报表不排除、在租明细隐藏——三处不一致需对齐；尤其"租期未到不应判 Expired"。
- **续签流程应在保存时自动写正确 rootId 并正确收尾旧合同**，不应依赖运行时 `transitionContractStatuses` 兜底纠状态（这是"时有时无"的根源）。
- **首期收款日早于起租日时给出校验/告警**，并明确首期覆盖区间，杜绝与上一份合同的最后一期重叠/留缝。

**4) 验证建议：** 修完数据后，分别查询 **1 月 / 4 月 / 7 月**，确认：① 原合同在各收款月金额正确；② 续签从其首个收款月起金额正确；③ 续签首期与原合同末期**无重复、无缺口**；④ 原合同重新出现在"在租明细"。

---

*附：本评估未运行线上代码、未修改任何文件，仅基于本地数据库快照与源码静态分析；最终金额请以收款月实查为准。*
