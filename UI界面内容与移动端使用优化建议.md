# UI 界面内容与移动端使用/管理 优化建议

> 评估对象：招商管理看板前端（`App.tsx` + `components/`）
> 评估视角：界面「展示了什么内容、怎么组织、移动端是否便于使用与管理」（区别于上一份偏「毛玻璃材质/细节」的报告）
> 评估日期：2026-06-24

---

## 总览结论

从内容与信息架构看，**移动端已被有意识地拆成两类**：

- **高频外勤场景做得很好**——「合同」「核销」有真正的移动端形态，用卡片、mini-card、迷你指标替代了宽表格，拇指可用。
- **重管理场景正在分层补齐**——「预算管理」已补移动摘要、预算行卡片、月度明细和轻编辑；楼宇资管已有移动单元卡与底部轻管理浮层，但楼宇批量管理、财务报表、发票等仍需要继续减少宽表横滑依赖。

也就是说，**当前移动端能「用」（看、查、录、核销），但不便于「管理」（看预算盘子、调楼宇、出报表）**。下面按「能不能用/能不能管 → 信息层级 → 细节」分 P0/P1/P2。

---

## P0 · 高优先级（直接决定移动端能不能用、能不能管）

### P0-1　重管理模块在移动端只能从抽屉深处进入
- **证据**：底部导航只暴露 3–4 个高频入口——`工作台 / 合同 / 核销 / 查询`（园区经理角色仅 `工作台 / 合同 / 收款` 3 项，见 `App.tsx:6451+`）。而 `预算管理 / 楼宇资管 / 财务报表 / 客户管理 / 系统与备份`（均为独立 `activeTab`）**都不在底部导航**，只能滑出汉堡侧栏才能找到。
- **影响**：移动端做任何「管理」动作，路径深、可发现性差，用户默认认为「这些只能上电脑做」。
- **方向**：底部导航最后一位放「更多」聚合入口（九宫格/列表式 bottom sheet），把中频管理模块收纳进去；并支持按角色把最常用的管理模块直接提到底部导航。让「管理」在移动端**可被发现、两步可达**。

### P0-2　大表在移动端退化为「横向滚动的电子表格」
- **证据**：预算管理已从宽矩阵补出移动摘要、分组卡片和行详情抽屉；楼宇资管已有移动单元卡，但平面图、批量导入、财务报表和发票排期仍有部分横向滚动或桌面表格残留。
- **影响**：手机单屏能完成部分轻管理，但遇到批量调整、复杂比较和报表追溯时仍容易回到电脑。
- **方向**：移动端把核心大表继续改为「卡片 + 关键指标优先 + 点开看明细」的列表形态；预算已进入轻编辑阶段，下一步重点放在楼宇单元轻管理、财务报表卡片化和平板主从布局。

### P0-3　移动端信息密度偏高、关键字号偏小
- **证据**：移动卡片大量用 `text-[10px]` / `text-xs` 承载标签与次要数值（如退租 mini-card `ContractManager.tsx:4429+`，单卡叠加「主数值 + 进度条 + 多行标签」）。
- **影响**：10px 文字接近手机可读下限；数字看板信息过密，拇指操作时易误读、误点。
- **方向**：移动端主数值 ≥ 17px、正文 ≥ 14px、标签 ≥ 12px；遵循「**一卡一主指标 + 一句上下文**」，把次要信息下沉到二级，靠留白而非缩字来塞信息。

---

## P1 · 中优先级（信息层级与内容组织）

### P1-1　工作台「什么都想第一眼看到」，缺主次与「待办优先」
- **现状**：仪表盘 / `StatsCards` 信息丰富但偏并列堆叠（出租率、客户数、累计欠款、到期预警…平铺）。
- **方向**：移动端首屏先回答「**今天我要处理什么**」——到期预警、待核销、待跟进置顶为可点击的行动卡；全量统计下沉到二级。用渐进披露而非一屏全展示。

### P1-2　高频「管理动作」要在移动端触手可及
- **现状**：核销/续签/编辑/催收等动作在桌面多为表格行内小图标按钮。
- **方向**：移动端把高频动作做成**卡片右侧主操作按钮**或**左右滑动作**（如左滑催收、右滑核销），避免依赖 < 44pt 的小图标；危险动作（删除/退租）需二次确认。

### P1-3　大数据量下的筛选/查询移动可用性
- **方向**：列表顶部常驻搜索框 + 常用筛选 chips（到期月份、园区、合同状态、欠费）；复杂筛选用**底部弹层（bottom sheet）**而非跳转新页；筛选结果给出「已筛 N 条 / 一键清除」反馈。

### P1-4　表单录入的移动键盘优化
- **证据**：抗缩放的 16px 输入字号已广泛覆盖（135 处，**很好**，避免了 iOS 聚焦放大）；金额/数字用了 `type="number"`（110 处），日期/电话用了原生 `type`（81 处）；当前已用运行时兜底和关键源码字段补齐 `inputMode` / `enterKeyHint`。
- **方向**：金额/数字改用 `inputMode="decimal"`/`"numeric"`（弹出更顺手的数字键盘，且规避 `type=number` 在千分位、前导零、滚轮误改值上的老问题）；金额输入实时显示千分位与「万/元」单位。

### P1-5　每屏的标题、返回与上下文要连续
- **证据**：移动端标题随模式切换（`招商工作台 / 收款查询 / 快速查询`，`App.tsx:6398+`）。
- **方向**：保证每一屏都有**清晰标题 + 返回路径 + 常驻的「当前园区 / 年份」上下文**，切换园区或年份后给明确反馈，降低多层级下「我在哪、看的是哪个园区」的迷失。

---

## P2 · 细节打磨

- **P2-1　空/加载/错误态的移动呈现**：列表为空、加载中、请求失败要有专门的移动占位（图标 + 一句话 + 可点的下一步），而非空白或一行小字。
- **P2-2　长列表手感**：已用 `VirtualizedTable`（好），移动端注意保留滚动位置、下拉刷新、触底加载更多的明确反馈。
- **P2-3　数字与货币的窄屏一致性**：统一千分位、单位（万/元）、右对齐与小数位；超长金额/企业名做优雅截断 + 点按查看全文。
- **P2-4　平板 / 横屏（641–1023px）的内容布局**：呼应上一份报告的「断点夹生」——这一区间既不该是手机的单列，也不必是桌面的全表，建议给两列/主从布局，让 iPad 与横屏手机也好管理。
- **P2-5　大屏看板（BigScreen）与移动端的角色区分**：大屏只读展示、移动端轻管理、桌面重管理——三端定位写进规范，避免把大屏的密集图表直接塞进手机。

---

## 建议落地顺序

| 阶段 | 内容 | 说明 |
|---|---|---|
| 第一步 | P0-3 字号/密度、P1-4 inputMode、P2-3 数字格式 | 改动小、对「好不好用」体感提升最直接 |
| 第二步 | P0-1 底部「更多」聚合入口、P1-1 待办优先工作台、P1-3 移动筛选 | 让移动端「找得到、看得清重点」 |
| 第三步 | P0-2 大表移动卡片化（先做预算/楼宇）、P1-2 滑动管理动作 | 把「管理」真正搬上手机，工作量较大，建议按模块灰度 |
| 第四步 | P2-4 平板/横屏布局、P2-5 三端定位规范 | 体系化收尾 |

> 一句话总结：**移动端目前「外勤够用、管理偏弱」。最该补的两点——一是让重管理模块在移动端「找得到、两步可达」（P0-1）并把大表卡片化（P0-2）；二是降低信息密度、突出「今日待办」，让一屏先回答「我现在要处理什么」（P0-3 + P1-1）。**

---

## 2026-06-24 落地进度

- **已落地 P0-1 第一阶段**：移动端底部导航新增「更多」入口，用 bottom sheet 聚合楼宇资管、预算管理、初始化数据、系统与备份；移动端进入预算 / 楼宇不再被自动重定向回工作台。
- **已落地 P0-2 第一阶段**：预算管理在手机端新增预算摘要、12 个月横向小计、分组折叠和逐行预算卡；桌面端继续保留原预算矩阵。
- **已落地 P0-2 第二阶段**：预算管理移动端逐行预算卡点击后打开「预算行月度明细」底部抽屉，展示该行年度预算、累计实收、达成率、签约单价、面积、本年免租和 12 个月预算 / 实收 / 差额；抽屉保留「合同概要」入口，避免手机端直接跳出预算明细层。
- **已落地 P0-2 第三阶段**：预算行月度明细抽屉新增「单月预算增减」轻编辑能力，仅对已登记合同客户行开放；保存时新增 `amount_delta` 预算调整，`originalYear/originalMonth = -1`，由既有账单服务叠加到目标月份，不修改合同租金、免租期、付款周期或导入预算表。
- **已落地 P0-2 第四阶段**：楼宇资管移动端单元卡新增轻管理提示，单元 / 资产编辑在小屏改为底部毛玻璃浮层；单元浮层顶部新增快捷状态切换（待租、预留、自用），已租单元继续由合同控制并锁定状态，保存、拆分、删除仍复用原资产台账函数。
- **已落地 P1-2 第一阶段**：楼宇资管移动端房源卡新增「快速处理」action sheet，可直接把非已租单元切为待租 / 预留 / 自用；已租或当前有租户的单元只展示合同控制说明和完整编辑入口，避免绕过合同释放房源。
- **已落地 P1-2 第二阶段**：楼宇资管移动端房源列表新增「批量」模式，可多选非合同控制单元并通过底部 action sheet 批量设为待租 / 预留 / 自用；已租或当前有租户的单元显示锁定并自动跳过，批量操作只更新资产台账 `status` / `isSelfUse`。
- **已落地 P0-3 / P1-4 基础项**：核心移动卡片内 9/10px 小字提升到可读下限；现有数字输入运行时补齐 `inputmode="decimal"` 与 `enterkeyhint="done"`，改善手机数字键盘体验。
- **已落地 P1-1 第一阶段**：移动工作台新增「今日待办」行动区，按累计欠款、本月待核销、开票风险、合同到期聚合最多 3 条优先事项；无急办事项时显示稳定空态，避免首屏继续堆叠全量统计。
- **已落地 P1-3 第一阶段**：移动查询页新增合同状态筛选 chips、已筛数量和一键清除反馈；筛选基于 `services/mobileTenantSearch.ts` 的同一份客户搜索索引，默认空搜索的计数与列表一样不把已退租客户计入可见结果。
- **已落地 P1-3 第二阶段**：移动查询页新增「筛选」bottom sheet，可按楼栋、是否有收款记录继续缩小客户列表；楼栋和收款记录筛选与状态 chips、关键词搜索、已筛数量共用 `filterMobileTenantSearchIndex` / `selectMobileTenantSearchResults` 同一条结果流。
- **已落地 P1-3 第三阶段**：移动查询 bottom sheet 新增到期月份筛选，月份选项从当前搜索索引中的合同 `leaseEnd` 自动生成，最多展示 12 个实际存在月份；筛选结果、已筛数量、清除按钮与状态 / 楼栋 / 收款记录共用同一条结果流。
- **已落地 P1-3 第四阶段**：移动查询 bottom sheet 新增「当前账期」筛选，可按 `当前账期未收 / 当前账期无未收` 过滤客户；筛选结果卡在启用该筛选时优先展示当前账期未收金额或无未收状态。
- **已落地 P1-3 第五阶段**：移动查询 bottom sheet 新增「历史欠费」筛选，可按 `有历史欠费 / 无历史欠费` 过滤客户；客户级历史欠费汇总复用封账月 `details_json` 或未封账月既有应收计算口径，筛选结果卡在启用该筛选时优先展示历史欠费金额和最近欠费月份。
- **已落地 P1-3 第六阶段**：历史欠费筛选改为进入移动查询后按需加载客户级欠费计算模块；未进入查询时不拉取 `tenantHistoricalArrears` / `dashboardMetrics` 链路，筛选弹层在计算中或缺少封账明细时保持禁用并给出原因。
- **已落地 P1-3 第七阶段**：移动客户查询 UI 抽成 `components/MobileTenantSearchPanel.tsx`，客户查询输入、状态 chips、筛选 bottom sheet、结果卡和局部弹层 / 焦点状态从 `App.tsx` 移出；`App.tsx` 继续只负责索引、筛选状态、权限上下文、历史欠费按需加载和跳转回调。
- **已落地 P1-3 第八阶段**：旧封账数据补客户级明细迁移补齐，`scripts/backfill-sealed-months.ts` 默认会创建缺失封账月、补齐已有但缺少 `details_json` 的旧封账行，并跳过已有明细的封账月；新增 `--missing-details-only` 用于只修复旧明细，不创建缺失月份。
- **已落地 P1-3 第九阶段**：历史欠费筛选新增用户态云端接口 `POST /api/integration/app/compute/tenant-historical-arrears`；移动查询进入后优先请求云端客户级欠费汇总，网关不可用、服务端计算关闭、token 缺失或本地存在未保存编辑时才回退前端按需动态计算。云端和本地 fallback 都按当前账号 `receivablePermissions` 过滤租金 / 物业费明细，避免物业账号看到租金欠费。
- **已落地 P2-4 第一阶段**：修正 768–1023px 平板 / 横屏断点夹生问题，工作台移动卡、月度收款明细、预算移动卡和楼宇移动房源卡统一延续到 `lg` 断点；平板宽度下预算组、楼宇房源和月度明细使用两列卡片，桌面宽表延后到 1024px 以上。
- **已落地 P0-2 / P1-2 预算移动轻编辑补齐**：存在导入预算表的年份，预算行月度明细抽屉不再只能提示回模板修改；已匹配到导入明细的行可逐月「编辑导入月额」，保存后直接更新导入预算表快照、重算行合计 / 月度目标 / 年度合计，并继续提示点击系统「保存」同步云端。
- **已落地 P2-4 第二阶段**：合同列表、财务报表（应收核销 / 收款明细 / 特殊业态）和发票排期的桌面宽表统一延后到 `lg` 断点；641–1023px 下继续使用移动卡片 / 轻管理列表，财务与发票卡片在平板宽度居中或两列排布，避免“移动底部导航 + 桌面宽表”的夹生体验。
- **已落地 P2-1 第一阶段**：合同、财务和发票移动端的无数据状态从一行浅灰提示升级为 readable 毛玻璃空态卡片，区分「真实无数据」与「筛选无结果」；合同无在租客户时提供新增入口，财务收款 / 应收 / 特殊业态和发票排期均给出下一步解释。
- **已落地 P1-4 / P2-3 第二阶段**：财务记账 / 押金转租 / 核销 / 特殊业态应收、合同租金 / 押金 / 物业费 / 退租结算 / 账期调整、预算假设和初始化月度数据等关键数字输入，在源码层显式补齐 `inputMode="decimal"` 或 `numeric` 与 `enterKeyHint="done"`；继续保留全局运行时兜底，提升移动端数字键盘和完成键一致性。
- **已落地 P2-2 第一阶段**：移动长列表统一增加滚动容器兜底，合同、财务、发票、预算和楼宇移动列表具备 `overscroll-behavior`、稳定滚动槽、iOS 惯性滚动与底部导航安全留白；预算月份、移动筛选月份和楼宇切换横滑轨道增加边缘淡出提示，强化“可横向滑动”的感知。
- **已落地 P1-5 第一阶段**：移动端全局顶栏新增上下文行，按当前模块展示「园区 · 年度 / 账期 · 当前模式」，覆盖工作台、客户查询、合同、核销、预算、初始化、楼宇和系统设置；顶部园区胶囊保留，页面切换后用户能持续确认当前数据范围。
- **已落地 P2-4 第三阶段**：合同列表在 641–1023px 平板 / 横屏宽度下新增轻主从布局，左侧继续使用移动合同卡片，右侧 sticky 预览当前合同的面积、租金、合同期、账期、物业费和退租状态，并保留详情 / 续签 / 退租 / 回退入口；手机窄屏仍保持单列卡片。
- **已落地 P2-4 第四阶段**：财务报表在 641–1023px 平板 / 横屏宽度下新增轻主从布局，应收核销左侧按状态展示移动账单卡、右侧 sticky 预览当前应收的合同应收、实际核销、结算调整、已收 / 待收和缓缴信息；收款明细左侧按月份展示流水卡、右侧预览金额、类型、账期与备注，并保留修改 / 删除入口。
- **已落地 P2-4 第五阶段**：发票排期在 641–1023px 平板 / 横屏宽度下新增轻主从布局，左侧继续使用移动发票卡，右侧 sticky 预览当前开票计划的客户、楼宇、金额、原计划应收日、计划开票日、发票视图月和预算归属月，并保留确认开票 / 恢复待开 / 延期入口；预测计划继续禁用操作。
- **已落地 P2-4 第六阶段**：特殊业态收入录入在 641–1023px 平板 / 横屏宽度下新增轻主从布局，左侧继续使用移动录入卡，右侧 sticky 预览当前客户的房源概要、已保存应收、草稿金额、本次差额和备注，并保留保存本行、清除本月录入和合同概要入口；手机窄屏仍保持单列录入。
- **已落地 P2-4 / P2-5 移动截图 QA 第一阶段**：使用 in-app browser 对 375×812、390×844、430×932、768×1024、1024×768 做首屏截图和横向溢出检查；修正移动端全园区 KPI 未就绪时的空态，改为优先展示当前可用园区 KPI，年度达成不再显示 `.../读取中` 作为主焦点；底部导航高度收敛到约 57px，并在短高手机上给关注字段前增加安全间距，避免底栏遮挡字段标题。
- **已落地 P1-2 / P1-4 触控命中区基础项**：`index.css` 在移动 / 触屏环境为 `liquid-pressable`、`mobile-pressable`、底部导航、更多入口、筛选 chip、年份切换和楼宇轻管理 pill 等交互控件统一补足 44px 最小触控目标，并启用 `touch-action: manipulation`；视觉按钮可以继续保持轻量，但手机上不再只依赖 32–40px 的小命中区。
- **已落地 P2-2 / P2-3 移动玻璃性能基础项**：`index.css` 将移动端底部导航、更多入口、筛选弹层、月度明细弹层、抽屉和遮罩纳入统一性能规则；移动端实景 `backdrop-filter` 控制在 12–16px，并用 `contain: paint` 限制重绘范围，避免多层实时大模糊拖慢手机滚动和弹层动画。
- **已落地移动 UI 守卫脚本**：新增 `npm run qa:mobile-ui`，用于在源码层快速检查 `viewport-fit=cover`、`100dvh`、44px 触控目标、移动玻璃 blur / `contain: paint`、底部导航 / 更多入口、查询 bottom sheet、自定义字段 5 个上限、历史欠费唯一口径和相关文档约束；该脚本不替代截图 QA，但作为每次移动端改动后的第一道回归闸口。
- **已落地移动导航组件化**：新增 `components/MobileNavigation.tsx`，承载移动底部导航与「更多管理」bottom sheet 的展示、触控与标签结构；`App.tsx` 仍负责构造导航项、权限、园区、年份和跳转回调，避免业务动作被拆散，同时减少主应用文件中的移动展示 JSX。
- **已落地关注字段组件化**：新增 `components/DashboardCustomFields.tsx`，承载「我的关注字段」横滑展示与自定义字段设置弹层；字段候选、最多 5 个限制、默认恢复、用户本地保存 key 和字段值 resolver 仍复用 `services/dashboardCustomFields.ts`，`App.tsx` 只保留读取 / 保存偏好和打开弹层状态。
- **已落地移动字号守卫第二阶段**：`index.css` 的 `mobile-typography-guard` 将核心移动卡片中的 `text-[9px]` / `text-[10px]` / `text-[11px]` 统一抬到 12px 可读下限；工作台顶部园区胶囊与切换弹层使用 `text-xs` 以上可读层，并纳入 `npm run qa:mobile-ui` 检查。
- **已落地移动更多入口语义化**：`components/MobileNavigation.tsx` 的「更多管理」bottom sheet 补齐 `role="dialog"`、`aria-modal="true"`、可见标题关联和 Escape 关闭；`npm run qa:mobile-ui` 纳入对应检查，保证移动管理入口不仅可点，也能被键盘和辅助技术理解。
- **已落地关注字段设置弹层语义化**：`components/DashboardCustomFields.tsx` 的「自定义关注字段」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；字段保存、默认恢复和最多 5 个限制不变。
- **已落地移动查询筛选弹层语义化**：`components/MobileTenantSearchPanel.tsx` 的「筛选客户」bottom sheet 改为面板自身声明 dialog / modal 语义，补齐可见标题关联、背景点击关闭、关闭按钮和 Escape 关闭；筛选项、已筛数量、历史欠费和当前账期筛选口径不变。
- **已落地移动空态守卫**：`npm run qa:mobile-ui` 新增合同、财务和发票移动空态检查，确保无数据状态继续使用 readable 玻璃卡片，并区分「真实无数据」与「筛选无结果」；合同真实空数据时保留新增合同入口，财务 / 发票继续给出下一步解释。
- **已落地移动数字输入守卫第二阶段**：AI 合同识别、AI 收款识别、付款周期变更和楼宇单元编辑中的残留数字输入补齐 `inputMode` / `enterKeyHint`；`npm run qa:mobile-ui` 新增数字 input 源码级扫描，防止后续关键数字字段只依赖运行时兜底。
- **已落地移动长列表 / 上下文守卫**：`npm run qa:mobile-ui` 新增移动长列表与顶栏上下文检查，锁住底部导航安全留白、iOS 惯性滚动、横滑淡出提示，以及工作台 / 查询 / 合同 / 财务 / 预算 / 楼宇等页面的常驻上下文文案。
- **已落地平板主从预览守卫**：`npm run qa:mobile-ui` 新增 641–1023px 合同、财务和发票主从布局检查，确保左侧列表 + 右侧 sticky 预览仍存在，并在筛选后自动把预览重置到当前结果集，避免右侧显示已被筛掉的数据。
- **已落地主色 / 焦点态守卫**：`npm run qa:mobile-ui` 新增矿物蓝 / 冰蓝主色令牌和 `focus-visible` 焦点环检查，防止后续把绿色重新作为页面主色，或让玻璃按钮、底部导航、更多入口和年份切换只保留 hover / active 反馈。
- **已落地关注字段跨设备偏好**：新增 `pb_user_preferences` 用户偏好集合与 `GET/PUT /api/integration/app/preferences/dashboard-custom-fields`，主界面关注字段保存时继续先写本地兜底，再同步当前登录用户云端偏好；云端不可用时不影响当前界面显示。
- **已落地关注字段同步状态反馈**：主界面关注字段在移动端和桌面端都会显示 `读取账号偏好 / 同步中 / 已同步到账号 / 本机已保存` 等轻状态文案；该状态只反馈 UI 偏好同步结果，不参与字段值计算、业务保存或园区数据版本。
- **已落地月度收款明细弹层可读性与退出路径**：`App.tsx` 的「月度收款明细」弹层补齐可见标题关联、背景点击关闭和 Escape 关闭；移动逐月卡片的指标标签抬到 12px，年初预算、合同应收、实际收款、完成率和累计达成继续复用 `buildMonthlyCollectionSummary`。
- **已落地月度收款明细口径说明语义守卫**：「月度收款明细」dialog 通过 `aria-describedby="monthly-collection-detail-description"` 关联“年初预算、合同应收与实际收款按现有工作台口径汇总”说明。该阶段只增强弹层说明和辅助技术理解，不改变月度汇总、完成率或累计达成计算。
- **已落地月度收款明细焦点闭环**：「月度收款明细」复用 `useMobileSheetFocus`，打开后焦点进入关闭按钮，Tab / Shift+Tab 限定在明细弹层内，关闭后回到打开它的月度明细入口。该阶段只增强键盘和辅助技术操作闭环，不改变月度收款明细内容或计算口径。
- **已落地移动月度明细卡语义补全**：移动端逐月明细卡和合计卡补 `role="group"`、完整 `aria-label/title`，摘要包含完成率、累计达成、实际收款、合同应收、年初预算和去年同期；合计卡补年度实际收款、累计达成、年初预算和合同应收。该阶段只增强卡片化明细的完整读取，不改变 `buildMonthlyCollectionSummary` 口径。
- **已落地移动首页可读玻璃层级**：移动工作台首屏从“白字叠高透明蓝色玻璃”收口为“浅色 readable 玻璃 + 蓝色关键强调”，园区标题、缓存状态、年度达成、进度条、三枚微指标、今日待办和关注字段均保持深色正文；该阶段只优化移动端可读性、层级和 PC 端信息对齐，不改变年度达成、缺口、出租率、欠款、客户数、今日待办或关注字段计算口径。
- **已落地关注字段设置焦点闭环**：「自定义关注字段」弹层复用 `useMobileSheetFocus`，打开后焦点进入关闭按钮，Tab / Shift+Tab 限定在弹层内，关闭后回到自定义入口；字段选项同步暴露 `aria-pressed`，已选数量用 polite live region 播报。该阶段只增强移动端设置体验和辅助技术理解，不改变字段候选、最多 5 个限制、保存位置或字段 resolver。
- **已落地关注字段底栏避让细化**：手机视口下，「我的关注字段」区与固定底部导航之间改为 `max(移动底栏高度 + safe-area + 0.75rem, 100dvh - 45rem)` 的动态间距，小屏优先避开 Home Indicator，大屏手机优先把关注字段推出底部毛玻璃导航覆盖区。该阶段只调整移动端安全留白，不改变字段展示、横向滑动或偏好保存逻辑。
- **已落地关注字段横滑提示细化**：移动端「我的关注字段」横滑列表新增 `liquid-mobile-custom-fields-track`，纳入预算月份、筛选 chips 和楼宇切换同一套边缘淡出 / 隐藏滚动条 / iOS 惯性滚动规则，并补充 `role="list"` / `role="listitem"` 与“横向滚动”标签，提示用户最多 5 个关注字段可以横向浏览。该阶段只增强横滑可发现性和辅助技术理解，不改变字段候选、排序、保存或 resolver 计算口径。
- **已落地合同退租弹层语义化**：`components/ContractManager.tsx` 的「办理退租」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；整单退租、部分房源退租、提前退租免租扣回、押金扣款和 `onUpdateTenants` 写入口径不变。
- **已落地合同初始化收款弹层语义化**：`components/ContractManager.tsx` 的「初始化历史收款」弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；累计已收金额、截止日期校验、`PaymentRecord` 生成和 `onUpdatePayments` 写入口径不变。
- **已落地合同提示 / 确认层语义化**：`components/ContractManager.tsx` 的 `ContractPromptOverlay` 补齐可见标题关联、背景点击关闭、面板点击隔离和 Escape 关闭；确认类弹层的背景点击 / Escape 按取消处理，提示类按知道了处理，删除、保存校验、导入失败和账期调整等业务分支不变。
- **已落地合同名称 / 付款周期变更弹层语义化**：`components/NameChangeDialog.tsx` 与 `components/PaymentCycleChangeDialog.tsx` 补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；`NameChangeRecord`、`PaymentCycleChange`、账单预览、历史记录和 `onConfirm` 写入口径不变。
- **已落地合同轻变更弹层字号收口**：名称变更历史日期、付款周期当前合同标签、生效日期说明和历史记录说明统一使用 `text-xs` / slate-500 可读层；该阶段只提升移动 elevated 弹层阅读性，不改变 `NameChangeRecord`、`PaymentCycleChange`、账单预览或 `onConfirm` 写入口径。
- **已落地 AI 合同 / 收款识别弹层语义化**：`components/AIContractRecognitionModal.tsx` 与 `components/AIPaymentRecognitionModal.tsx` 补齐 dialog / modal 语义、可见标题关联、背景点击关闭、面板点击隔离和 Escape 关闭；AI 解析、Excel 读取、客户 / 楼宇 / 房源匹配、选中项和导入生成 `Tenant` / `PaymentRecord` 口径不变。
- **已落地 AI 识别弹层字号收口**：AI 合同识别的上传提示、识别摘要、楼宇未匹配提示和房源面积 chips，以及 AI 收款识别的移动结果卡字段标签、匹配客户说明和低置信度提示统一使用 `text-xs` / slate-500 或 amber-700 可读层；该阶段只提升 AI elevated 弹层确认环节清晰度，不改变 AI 解析、匹配、选中项或导入写入口径。
- **已落地楼宇提示 / 确认层语义化**：`components/BuildingManager.tsx` 的 `BuildingPromptOverlay` 补齐可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；确认类背景点击 / Escape 按取消处理，提示类按知道了处理，资产删除、单元删除、导入失败和校验提示等业务分支不变。
- **已落地楼宇移动轻管理弹层语义化**：`components/BuildingManager.tsx` 的单元「快速处理」和批量状态处理 action sheet 补齐可见标题关联与 Escape 关闭；待租 / 预留 / 自用切换、合同控制锁定、二次确认、面积和租户引用口径不变。
- **已落地楼宇移动房源卡信息层级优化**：`components/BuildingManager.tsx` 的移动房源列表改为按楼层分组，每张卡优先展示状态 / 房号 / 面积 / 租户说明，并保留可处理数量、合同控制提示、快速处理、编辑和批量选择入口；该阶段只优化移动端扫读和触控层级，不改变房源状态、面积统计、合同锁定、租户引用或云端保存口径。
- **已落地预算移动明细 / 轻编辑弹层语义化**：`components/BudgetManager.tsx` 的预算行月度明细和单月预算增减 / 导入月额编辑弹层补齐可见标题关联与 Escape 关闭；12 个月预算 / 实收 / 差额、`amount_delta` 调整和导入预算快照更新口径不变。
- **已落地财务移动核销弹层语义化**：`components/FinanceManager.tsx` 的单笔收款核销和批量核销弹层补齐 dialog / modal 语义、可见标题关联、背景点击关闭和 Escape 关闭；实收金额、尾差结清、批量核销和 `PaymentRecord` 写入口径不变。
- **已落地财务缓缴 / 发票延期弹层语义化**：`components/FinanceManager.tsx` 的「申请缓缴」和 `components/InvoiceManager.tsx` 的「延期开票」弹层补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭和 Escape 关闭；缓缴目标账期、开票计划日期、状态切换和写入口径继续复用原 `onDeferPayment` / `handleConfirmDefer` 路径。
- **已落地云端保存冲突弹窗语义化**：`components/ConflictDialog.tsx` 的数据冲突决策弹窗补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；默认跳过、用我的值 / 用服务端值 / 跳过此条、批量决策和 `onResolve(decisions)` 写入口径不变。
- **已落地云端保存冲突弹窗字号收口**：冲突卡片的操作类型、更新时间、字段名、本地值 / 服务端值标签和空态提示统一使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫；该阶段只提升冲突决策在手机毛玻璃弹层里的判断清晰度，不改变默认跳过、批量决策、单条决策或 `onResolve(decisions)` 写入口径。
- **已落地预算导入名称关联工具字号收口**：`TenantMergeTool` 的预算年度、预算行、合同客户、候选客户 ID、无匹配空态和已保存关联标签统一使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫；该阶段只提升名称关联在手机和平板上的判断清晰度，不改变 `readBudgetCustomerNameLinks`、`writeBudgetCustomerNameLinks`、`findImportedRowByKey`、唯一绑定约束或 `onBatchUpdate` 写入口径。
- **已落地合同 / 楼宇导入结果弹窗语义化**：`components/ContractManager.tsx` 与 `components/BuildingManager.tsx` 的批量导入结果弹窗补齐 dialog / modal 语义、可见标题关联、命名关闭按钮、背景点击关闭、面板点击隔离和 Escape 关闭；导入汇总、失败明细、失败明细导出、楼宇合同同步行和导入写入口径不变。
- **已落地移动首屏底栏避让优化**：针对 375–430px 手机视口，工作台移动主卡启用更紧凑的 hero、指标单元和待办卡垂直排布；底部避让只服务今日待办和关注字段区域，园区切换统一上移到顶栏，不再在工作台底部保留园区汇总卡。该阶段只调整移动显示密度，不改变今日待办、关注字段、顶部园区切换或年度达成数据来源。
- **已落地移动 CSS 结构守卫**：`npm run qa:mobile-ui` 新增 CSS 大括号平衡和移动预算布局重复嵌套选择器检查；关注字段组件新增渲染测试，确保同步状态文案不是只存在于 hook，而是实际出现在字段标题区。
- **已落地 P2-1 第二阶段**：移动客户查询无结果状态从单行浅灰提示升级为 readable 毛玻璃空态卡片，区分「筛选无结果」与「暂无可查询客户」；筛选无结果时提供「清除筛选」，真实无数据时提供「回到指标」。该阶段只增强空态解释和下一步动作，不改变搜索索引、筛选或结果总数计算。
- **已落地 P2-2 第二阶段**：移动客户查询结果从固定截断改为渐进展示，默认保留 6 条轻量列表，结果超过当前展示数量时提供「显示更多」，展开后提供「收起结果」，并显示「已显示 X / 共 Y 条」状态；关键词或筛选变化会自动回到默认展示数量，不改变搜索索引、筛选、历史欠费或当前账期口径。
- **已落地 P2-2 第三阶段**：移动客户查询顶部合同状态 chips 接入共享横滑轨道，具备 iOS 惯性滚动、隐藏滚动条和边缘淡出提示；到期月份、状态 chips 和楼宇切换的横向滚动感知保持一致。该阶段只调整滚动容器和视觉提示，不改变状态筛选结果或默认筛选规则。
- **已落地 P1-2 第三阶段**：移动客户查询结果卡的「合同 / 核销」动作开始携带当前客户上下文；点「合同」进入合同轻管理时自动用客户名筛选并选中该客户，已退租客户进入历史页；点「核销」进入财务应收核销时自动用客户名筛选，避免用户跳到模块首页后再次搜索。该阶段只预填 UI 搜索和预览，不写入合同、收款、应收或筛选口径。
- **已落地移动查询长文本截断语义**：移动客户查询结果卡继续保持单行截断和紧凑扫读，但整张卡补充完整 `aria-label` / `title`，客户名、楼栋房号、状态、收款 / 欠费摘要保留完整标题，合同 / 核销按钮读出当前客户名；该阶段只增强长企业名、长位置和长金额摘要的可理解性，不改变搜索、筛选、跳转或结果排序口径。
- **已落地 P1-4 / 触控细化**：移动客户查询顶部「清除」动作纳入 44px 移动触控目标组，并补充 `aria-label="清除当前查询筛选"`；视觉仍保持轻量胶囊，但实际可点区域和辅助技术命名与筛选按钮一致。
- **已落地移动语义状态细化**：移动底部导航和「更多管理」入口对当前项补充 `aria-current="page"`；移动客户查询搜索框改为 `type="search"` 并声明 `enterKeyHint="search"`，状态 chips、楼栋、到期月份、收款记录、当前账期和历史欠费筛选项补充 `aria-pressed`。该阶段只增强移动键盘、选中状态和辅助技术理解，不改变导航、搜索、筛选或结果计算口径。
- **已落地移动导航上下文语义守卫**：移动底部导航补充稳定 `aria-label="移动端主导航"`，更多管理弹层通过 `aria-describedby="mobile-more-management-context"` 关联当前园区与年份说明。该阶段只增强移动导航层与更多入口的上下文理解，不改变导航项、权限、展开关闭或跳转逻辑。
- **已落地移动筛选分组选项语义守卫**：移动客户查询顶部状态 chips 声明为“合同状态筛选”分组；筛选 sheet 内楼栋、到期月份、收款记录、当前账期和历史欠费选项均通过可见标题建立 `role="group"` / `aria-labelledby` 关系，筛选触发按钮会读出已启用高级筛选数量。该阶段只增强筛选上下文和辅助技术播报，不改变筛选条件、结果数量、当前账期或历史欠费计算口径。
- **已落地移动查询筛选徽标显式字号收口**：移动查询「筛选」按钮上的已启用数量圆标和历史欠费「需封账明细」说明从 10px 改为源码层 `text-xs`，不再依赖兜底字号；该阶段只增强筛选状态可读性，不改变高级筛选数量、历史欠费可用性或筛选结果计算。
- **已落地移动顶部与快捷入口可读性收口**：移动端顶部页面标题提升到显式 `text-base`，并通过 `liquid-mobile-toolbar-title` 固定 16px 阅读下限；非管理员工作台快捷入口的辅助文案改为 `liquid-mobile-action-helper text-xs font-bold`，去掉半透明文字。该阶段只增强手机首屏标题和快捷入口说明清晰度，不改变导航、园区上下文或入口跳转逻辑。
- **已落地移动微指标截断语义补全**：390px 视口真实审计发现“已租面积”等三列微指标可能因单位较长被截断；`App.tsx` 现在把收款缺口、已租面积、出租目标的显示值统一抽成 label，并同步写入 `title` / `aria-label`。该阶段只补全可读性和辅助技术语义，不改变收款缺口、面积或出租目标计算口径。
- **已落地移动首屏截断语义扩展**：移动顶部标题、园区胶囊、固定 KPI cell、快捷入口和今日待办卡都补齐完整 `title` / `aria-label` 或分组语义，避免紧凑 `truncate` 在长园区名、长金额、长客户提示时只留下半截文字。该阶段只补完整文本可确认性，不改变导航、待办或 KPI 数据来源。
- **已落地移动顶部园区切换语义补全**：工作台园区切换统一收敛到顶栏园区胶囊；点击后打开顶部玻璃弹层，只列授权园区并用 `aria-current` 标识当前园区，不再提供“全部园区”汇总入口。该阶段只增强顶部园区切换的可理解性，不改变授权园区、`switchProject` 权限校验或单园区 KPI 计算逻辑。
- **已落地登录入口分段语义收口**：登录 / 注册分段控件补充 `role="group"` 与“登录方式”标签，两个分段按钮用 `aria-pressed` 同步当前模式；该阶段只增强入口页选中态语义，不改变登录 / 注册切换、提交或审批逻辑。
- **已落地登录入口反馈语义收口**：登录错误使用 `role="alert"` / `aria-live="assertive"`，注册申请失败走 assertive alert，成功或普通提示走 `role="status"` / `aria-live="polite"`；提交按钮同步 `aria-busy`。该阶段只增强等待、失败和成功状态播报，不改变登录、注册或审批逻辑。
- **已落地移动弹层触发器关联**：底部「更多」触发器补充 `aria-expanded` 与 `aria-controls="mobile-more-management-sheet"`，并不再把「更多」本身标记为当前页面；移动查询「筛选」触发器补充 `aria-expanded` 与 `aria-controls="mobile-tenant-filter-sheet"`。该阶段只增强触发器与 bottom sheet 的语义关系，不改变更多入口、筛选面板或业务跳转。
- **已落地移动弹层焦点圈定**：底部「更多管理」和移动查询「筛选客户」打开后会先把焦点移入弹层关闭按钮，Tab / Shift+Tab 被圈定在弹层内，关闭后再把焦点还给对应触发按钮；Escape、背景点击、关闭按钮和完成按钮共用关闭路径。该阶段只补键盘 / 辅助技术的操作闭环，不改变弹层内容、筛选状态、更多入口或跳转逻辑。
- **已落地移动弹层焦点逻辑组件化**：新增 `components/useMobileSheetFocus.ts`，把焦点移入、Tab 圈定、Escape 关闭和焦点返回抽成共享 hook；底部「更多管理」和移动查询「筛选客户」复用同一逻辑，后续新增 bottom sheet 不再重复维护焦点陷阱。
- **已落地楼宇移动轻管理焦点闭环**：楼宇「快速处理」和「批量调整房源状态」action sheet 接入 `useMobileSheetFocus`，打开后焦点进入关闭按钮，Tab 限定在弹层内，关闭后回到对应触发按钮；房源状态、合同控制锁定、面积统计和保存口径不变。
- **已落地楼宇移动 action sheet 触发器语义**：楼宇单元「快速处理」和批量「处理 N」按钮补齐 `aria-expanded` / `aria-controls`，分别指向稳定的快速处理与批量状态面板 id；该阶段只增强触发按钮与底部面板的语义关系，不改变房源状态、合同锁定或批量处理口径。
- **已落地关注字段设置触发器语义**：`DashboardCustomFields` 的「自定义」按钮和空状态「点击添加个人关注字段」入口补齐 `aria-expanded` / `aria-controls="dashboard-custom-field-settings-panel"`，设置弹层补稳定面板 id；该阶段只增强设置入口与弹层的语义关系，不改变字段候选、最多 5 个限制、偏好保存或 resolver 计算口径。
- **已落地楼宇移动 action sheet 组件化测试**：新增 `components/BuildingMobileActionSheets.tsx` 与渲染测试，单独覆盖楼宇「快速处理」和「批量调整房源状态」弹层的 dialog 语义、可见标题、关闭按钮、状态动作和口径提示；`BuildingManager` 继续只负责状态、权限和写入回调。
- **已落地楼宇移动轻管理字号收口**：楼宇快速处理 / 批量状态 action sheet 的状态胶囊、已选单元 chips，以及单元编辑底部抽屉里的状态、合同锁定说明和锁定徽标统一抬到 `text-xs`；该阶段只增强楼宇轻管理在手机上的状态可读性，不改变房源状态、合同控制、面积统计或保存口径。
- **已落地楼宇平面 / 列表扫读字号二次收口**：楼栋切换卡面积 / 户数摘要、平面图图例数量、平面图说明、楼层房间数、移动房源楼层标识和桌面列表表头 / 状态胶囊统一使用 `text-xs` / slate-500 可读层；该阶段只提升楼宇平面与列表扫读清晰度，不改变面积、出租率、单元状态、合同锁定或租户引用口径。
- **已落地上线验收清单**：新增 `docs/10-移动端与字段口径上线验收清单.md`，把移动端 / 平板截图 QA、字段口径来源、历史欠费迁移、关注字段云端偏好、预算轻编辑、楼宇轻管理、阿里云部署前检查和回归命令收束为部署前门槛，并纳入 `npm run qa:mobile-ui` 守卫。
- **已落地上线回归一键化**：新增 `npm run qa:rollout`，串联移动 UI 守卫、关键字段 / 移动端回归测试、验收记录脚本测试、TypeScript 检查和生产构建；部署文档已把该命令列为 Docker / 阿里云上线前验收入口。
- **已落地上线验收记录模板**：新增 `docs/11-移动端与字段口径上线验收记录模板.md`，用于每次上线留存自动回归、截图 QA、字段口径抽查、封账 dry-run、云端接口、灰度观察和最终结论；该模板补足 `qa:rollout` 无法自动证明的生产环境与人工视觉验收项。
- **已落地上线验收记录生成器**：新增 `npm run qa:rollout:record`，可从模板生成 `docs/rollout-records/` 下的当次验收记录草稿，自动填入日期、分支和 commit，并避免覆盖同日已有记录。
- **已落地上线验收记录完整性检查**：新增 `npm run qa:rollout:record:check -- docs/rollout-records/记录文件.md`，用于检查验收记录是否仍残留关键占位项，避免有记录文件但截图 QA、接口、dry-run 或最终结论未真正填写。
- **已落地验收记录脚本回归**：新增 `scripts/__tests__/rolloutRecordScripts.test.ts`，覆盖记录生成、同日不覆盖、未填草稿失败和已填记录通过，避免模板文案或脚本改动破坏上线留痕。
- **已落地移动截图 QA 辅助命令**：新增 `npm run qa:mobile-screenshots -- --url http://127.0.0.1:5173/`，批量输出 375x812、390x844、430x932、768x1024、1024x768 五个基础视口截图和报告到 `output/mobile-screenshot-qa/`；该命令用于辅助填写验收记录，不替代更多入口、筛选 sheet、月度明细、预算编辑和楼宇 action sheet 的人工状态截图。
- **已落地移动端深色系统可读性修复**：针对真机深色外观下移动工作台文字发白的问题，`index.css` 已把 `ios-liquid-app.mobile-typography-guard` 内的顶部 readable 区、KPI 辅助字段、关注字段、今日待办、空态和底部导航纳入浅色玻璃深色正文兜底；该阶段只修正移动材质对比度，不改变 KPI、待办、关注字段或收款明细口径。
- **已落地移动端工作台可读性二次收口**：年度收款达成主 KPI 改为浅色可读玻璃卡，缺口胶囊、顶部上下文胶囊和底部导航标签纳入 12px 可读下限；深色外观下浅底玻璃卡片强制保持深色正文，避免白字压在浅玻璃或高亮蓝色透明层上。该阶段只优化移动端字体、层级和对比度，不改变年度收款、缺口、出租率、欠款、客户数或关注字段计算口径。
- **已落地移动端 PC 同源阅读层加固**：`index.css` 新增 `--liquid-mobile-readable-fill / border / shadow` 作为手机首屏统一承载层，移动工作台主卡、年度收款达成、三列辅助字段、今日待办、关注字段和顶部上下文统一回到高不透明白色 readable 玻璃；蓝色只保留在主值、进度条、选中态和状态胶囊上，避免真机低亮度或截图压缩后出现“蓝底白雾、文字发灰”。该阶段只收紧移动端材质和字体可读性，不改变年度收款、欠款、出租率、客户数、待办或关注字段口径。
- **已落地移动工作台真机可读层加固**：移动主卡、hero、今日待办区、顶栏和底部导航新增 `liquid-mobile-dashboard-* / liquid-mobile-toolbar-shell / liquid-mobile-bottom-readable` 专用 readable 类，手机 readable 层进一步加厚并纳入深色系统、高对比和降低透明度兜底；移动端与 PC 端保持同一套“浅玻璃承托 + 蓝色少量强调”层级。该阶段只修正字体发灰、蓝底白雾和 chrome 不一致，不改变年度收款、缺口、出租率、欠款、客户数、待办、关注字段或月度明细口径。
- **已落地移动首屏显式字号收口**：移动工作台顶部上下文、hero 状态、今日待办范围和年度达成微指标标签从依赖 `mobile-typography-guard` 兜底的 `text-[11px]` 改为源码层 `text-xs`，让核心首屏文案直接满足 12px 可读下限；该阶段只提升移动端字体清晰度，不改变 KPI、待办、园区上下文或关注字段计算。
- **已落地移动首屏显式字号二次收口**：固定三列 KPI 辅助胶囊和关注字段“最多显示 / 同步状态”说明也改为源码层 `text-xs`，不再依赖 11px 兜底；该阶段只提升首屏辅助信息清晰度，不改变 KPI、字段数量限制、同步偏好或字段 resolver。
- **已落地工作台提醒卡片字号收口**：`DashboardAlerts` 的发票风险摘要、本月 / 下月关怀提示、生日 / 对接人徽标和卡片数量胶囊全部使用 `text-xs`，并纳入移动 UI 守卫；该阶段只提升工作台提醒和关键时刻列表在手机上的清晰度，不改变开票风险、入园周年、企业纪念日或生日筛选口径。
- **已落地移动入口与初始化字号收口**：注册入口的已选园区数量、可见园区编号、初始化数据入口 chips 和 2025 年 12 月欠款口径徽标统一改为源码层 `text-xs`；该阶段只提升登录 / 注册与初始化管理流的辅助信息清晰度，不改变账号申请、园区选择、初始化数据或欠款口径。
- **已落地移动关注字段卡摘要语义**：手机端自定义关注字段卡继续采用紧凑横滑玻璃卡片，但每张卡都用字段名、resolver 输出值和辅助说明生成完整 `aria-label/title`，字段名、数值和说明也保留独立 `title`；该阶段只补全截断后的读取能力，不改变关注字段候选、最多 5 个限制、云端 / 本地偏好或字段计算口径。
- **已落地工作台辅助表字号收口**：`Tables` 的最新签约动态、到期预警、预算执行月度卡和年度经营对比卡统一去除 11px / slate-400 弱灰字，辅助说明、空态和指标标签使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫；该阶段只提升工作台辅助表在手机和平板上的阅读清晰度，不改变 `recentSignings`、`expiringSoon`、`executionRows`、`monthlyTrends` 或年度经营指标口径。
- **已落地工作台预算执行明细字号收口**：`StatsCards` 的预算执行月度明细、租金 / 物业费分段、完成率 / 累计达成胶囊、合计行和累计欠款说明统一使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫；该阶段只提升首页预算执行明细的阅读清晰度，不改变 `monthlyBreakdown`、`budgetExecutionTotal`、`initialBudgetMonthMap`、`scopeLabels`、同比、完成率或累计达成口径。
- **已落地移动模块顶部阅读层收口**：移动客户查询、合同查询 / 快速办理、财务待处理账款的 `liquid-mobile-hero` 统一回到浅色 readable 玻璃和深色正文；筛选 chips、筛选按钮、清除按钮改为浅色玻璃控件，蓝色仅保留在 active / 主操作强调。该阶段只修正移动模块顶部的材质与对比度，不改变查询、筛选、合同、应收或核销逻辑。
- **已落地共享客户选择器字号收口**：财务记账 / 押金转租金复用的 `SearchableTenantSelect` 将客户副信息、重选按钮、无匹配空态和范围提示统一抬到 `text-xs`，并为搜索框补齐 `inputMode="search"` / `enterKeyHint="search"`；该阶段只提升移动弹层内客户选择清晰度和键盘体验，不改变客户排序、筛选、押金过滤或选择回调。
- **已落地移动合同卡片字号收口**：手机合同 / 历史退租卡片里的楼层标题、状态徽标、账期 / 特殊业态 / 免租处理标签和字段网格统一抬到 `text-xs`；共用物业费标签采用 `text-xs lg:text-[10px]`，手机和平板优先可读、桌面保留紧凑。该阶段只增强合同卡片阅读性，不改变合同筛选、续签、退租、物业费或账期调整计算口径。
- **已落地移动合同概要弹层字号收口**：合同概要里的账单状态徽标、预算叠加标签、移动账单卡“账单月 / 应收金额 / 覆盖租期”和空置预算提示统一达到 `text-xs`；桌面表格徽标继续可用 `md:text-[10px]` 保持紧凑。该阶段只提升合同概要账单预览在手机弹层里的可读性，不改变账单匹配、应收金额、覆盖租期或预算叠加口径。
- **已落地移动发票卡片字号收口**：发票平板预览标题、状态胶囊、日期 / 视图 / 预算归属指标标签和手机预测胶囊统一使用 `text-xs`；手机日期块标签改为更高对比的 `font-black text-slate-500`。该阶段只提升发票计划在手机和平板上的可读性，不改变 `filteredInvoices`、预测禁用、确认开票、恢复待开、延期或预算归属月计算口径。
- **已落地发票状态字号二次收口**：发票桌面表格预测胶囊、桌面已开票日期和移动已开票时间统一使用 `text-xs` / slate-500 可读层；该阶段只让预测 / 已开票状态在 PC、平板和手机上保持一致清晰，不改变 `buildPotentialInvoices`、`filteredInvoices`、状态切换、预测禁用或延期写入口径。
- **已落地客户来源分析字号收口**：`SourceAnalysisDashboard` 的来源 KPI 标签、手机来源明细小指标、展开合同副信息和空态提示统一使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫；该阶段只提升来源分析在手机和平板上的扫读清晰度，不改变 `summary`、`sortedRows`、稳定指数、排序、展开来源、云端计算或本地 fallback 口径。
- **已落地移动财务卡片字号收口**：财务移动收款流水卡、应收核销卡、缓出 / 缓入提示、预算归属说明、备注标签、核销快捷入口和待核销 / 缓缴 / 全部筛选统计统一抬到 `text-xs`，并把关键标签从浅灰普通字改为更稳定的 `font-black / font-bold text-slate-500`。该阶段只增强财务移动端可读性，不改变收款流水、应收核销、缓缴调账、备注或批量核销口径。
- **已落地财务平板预览 / 概览字号二次收口**：财务 641–1023px 主从预览里的收款流水预览、应收预览指标、特殊业态录入预览、移动特殊业态录入卡，以及财务概览卡和缓缴说明统一使用 `text-xs` 可读层；该阶段只提升平板 / 手机横屏下的财务阅读清晰度，不改变 `displayReceivableSections`、`groupedPayments`、`specialBizAmountDraft`、缓缴或核销写入口径。
- **已落地移动预算卡片字号收口**：预算主列表卡右上角年度预算 / 累计实收、月度明细抽屉里的月份标签、无特殊标记、预算 / 实收 / 差额标签统一使用 `text-xs` 以上字号，并用 slate-500 加粗替代浅灰小字。该阶段只增强预算移动端阅读性，不改变 `monthlyValues`、`detailMonthlyData`、年度预算、累计实收或差额计算口径。
- **已落地移动登录入口触控守卫**：登录 / 注册输入框和园区多选项纳入统一 44px 命中区与蓝色 `focus-visible` 焦点环，未登录首屏也按移动端主流程验收；该阶段只增强入口页触控与键盘定位，不改变登录、注册或审批逻辑。
- **已落地移动登录入口表单语义守卫**：登录 / 注册的邮箱、姓名、密码字段补齐 `label htmlFor` / `id` 关联；邮箱输入声明 `inputMode="email"`、关闭自动首字母大写和拼写检查，密码输入按登录 / 注册声明 `go` / `done` 完成键，园区多选 checkbox 聚焦时显示整行焦点环。该阶段只增强移动键盘、表单语义和焦点定位，不改变提交或审批逻辑。
- **已落地登录入口表单标题关联守卫**：登录 / 注册 `<form>` 补齐 `aria-labelledby="auth-form-title"` 与 `aria-describedby="auth-form-description"`，可见标题和辅助说明拥有稳定 id；该阶段只增强入口表单的名称与说明关系，不改变展示文案、切换或提交逻辑。
- **已落地注册入口园区多选语义守卫**：注册申请的园区多选区补充可见标签关联、`role="group"`、加载态 `aria-busy`、已选数量 polite 播报、加载 / 空态 status 播报，并为每个园区 checkbox 提供项目编号说明；该阶段只增强园区选择理解和状态反馈，不改变可选园区来源或 `requestedProjectIds` 写入逻辑。
- **已落地移动高对比系统可读性守卫**：系统开启更高对比度时，登录 / 注册页、移动年度达成主 KPI、缺口胶囊、三列 KPI、关注字段外壳、底部导航和月度明细表格壳会退到近不透明 readable 层并保持深色正文；`npm run qa:mobile-ui` 已把该兜底纳入守卫，避免后续样式改动让透明材质重新压低文字对比度。
- **已落地移动降低透明度可读性守卫**：系统开启降低透明度时，登录 / 注册页、移动年度达成主 KPI、缺口胶囊、三列 KPI、今日待办、关注字段外壳、底部导航、筛选项和月度明细表格壳会取消实时模糊并使用更实的背景；`npm run qa:mobile-ui` 已把该兜底纳入守卫，避免首屏关键数据继续依赖强透明材质。
- **已落地移动减少动态效果守卫**：系统开启减少动态效果时，移动卡片进入动画、通用 `animate-in` 弹层、`animate-spin` 加载旋转、跑马灯、pulse 状态和 Tailwind transition 工具类会被关闭；月度明细、移动筛选、更多管理、抽屉和 elevated 弹层不再依赖滑入、缩放或淡入表达层级，加载图标保留静态状态。该阶段只收敛动效，不改变导航、筛选、加载或业务写入逻辑。
- **已落地移动入场动画可读性兜底**：`mobile-card-in` 第一帧和 `mobile-card-enter` 默认终态都保持可见，系统开启减少动态或浏览器中断动画时强制 `opacity: 1` / `transform: none`，避免今日待办、关注字段或轻管理卡片停在透明初始帧导致文字发虚。该阶段只修正动效兜底，不改变待办或字段计算口径。
- **已落地移动截图报告校验**：新增 `npm run qa:mobile-screenshot-report -- output/mobile-screenshot-qa/截图目录/README.md`，发布前可校验截图报告是否真正包含登录态、`requireAuthenticated=true`、登录后壳 selector、浅色+深色、默认+减少动态和五个基础视口；上线记录检查也会自动读取该报告，避免只填路径但误截登录页、未启用登录后校验、缺失暗色证据或缺少减少动态效果证据。
- **已落地移动截图 QA 深色 / 动效 / 登录态参数**：`npm run qa:mobile-screenshots` 支持 `--color-scheme light|dark|both`、`--reduced-motion no-preference|reduce|both` 与 `--storage-state 登录态文件`，上线前可同时留存浅色 / 深色、默认动态 / 减少动态基础视口截图，并在已有登录态时直接截取真实工作台而不是登录页。
- **已落地登录态保存辅助命令**：新增 `npm run qa:mobile-auth-state`，由验收人员在 Playwright 浏览器中手动登录 QA 账号，关闭窗口后把 storage state 保存到 `output/mobile-auth-state/`，供 `qa:mobile-screenshots --storage-state` 复用；该文件只作本机验收凭据，不提交仓库。
- **口径约束**：预算移动卡复用 `BudgetManager` 现有 `detailMonthlyData` / `detailGroups` / 月度合计数据，不新增预算、实收、出租率或达成率计算路径。
- **口径约束**：非导入预算年份的移动端预算轻编辑只新增可追溯的 `BudgetAdjustment.adjustmentKind = amount_delta`；导入预算表存在的年份不再新增人工金额调整，已匹配导入明细的行改为编辑导入快照，避免导入模板覆盖导致“保存了但显示不变”。
- **口径约束**：导入预算行移动编辑不新增 `BudgetAdjustment`，只更新 `pb_billing_period_notes` 中的导入预算快照，并通过 `mergeBudgetTotalsIntoInitData` 同步预算执行目标列；导入格填 0 保持既有「未覆盖则回落合同滚动口径」规则。
- **口径约束**：今日待办和移动筛选仅重组 `DashboardData.currentMonthBilling`、`invoices`、`expiringSoon`、累计欠款与移动搜索索引，不新增欠款、应收、开票金额或合同状态计算路径。
- **口径约束**：移动查询空态只根据 `searchResults.length`、`searchResultCount` 与当前筛选状态切换文案和安全动作；不得用空态反推客户状态、欠费金额或业务同步结果。
- **口径约束**：移动查询「显示更多 / 收起结果」只调整 `selectMobileTenantSearchResults` 的展示上限，不改变 `filterMobileTenantSearchIndex`、`countMobileTenantSearchResults`、当前账期未收、历史欠费或合同状态筛选口径。
- **口径约束**：移动查询结果卡进入合同 / 核销只传递 `tenantId + tenantName` 作为一次性 UI focus；合同页复用 `searchTerm`，财务页复用 `receivableKeyword`，不得新增合同、应收、收款或欠费计算路径，也不得自动触发核销保存。
- **口径约束**：到期月份筛选仅按合同 `leaseEnd` 年月匹配，不替代 `expiringSoon` 到期预警算法；当前账期筛选仅按现有 `currentMonthBilling` 中 `amountDue - amountPaid > 0` 判断，不代表历史欠费总额，也不新增正式欠款计算路径。
- **口径约束**：历史欠费筛选只使用 `buildTenantHistoricalArrears`；封账月必须有 `SealedMonth.billingDetails`（来自 `pb_sealed_months.details_json`）才能拆分到客户，缺少封账明细时自动隐藏/重置历史欠费筛选，避免用园区累计欠款或当前账期未收误判客户级历史欠费。
- **性能约束**：客户级历史欠费属于低频管理筛选，不应进入移动首屏或普通工作台主包的同步计算路径；正式部署优先走服务端 `tenant-historical-arrears` 计算接口，前端只保留按需动态计算 fallback。若继续增加欠费类筛选，优先复用云端接口 / 快照，不新增移动首屏同步计算。
- **架构约束**：移动端复杂面板（查询、筛选、批量轻管理、主从预览、底部导航、关注字段配置）应优先抽成独立组件，主应用文件只保留数据编排和业务口径状态；新增移动 UI 不应继续把大段展示 JSX 堆入 `App.tsx`。
- **偏好约束**：关注字段属于用户 UI 偏好，不进入 `DashboardData`、业务保存版本或园区数据差异；保存必须保持本地即时兜底，并通过 `pb_user_preferences.user_id + preference_key` 做跨设备同步，同时在界面上保留同步中、已同步和本机兜底状态反馈。
- **视觉 QA 约束**：移动端每次调整工作台、底部导航、弹层或管理卡片后，先运行 `npm run qa:mobile-ui` 做基础守卫，再至少用 375×812、390×844、430×932、768×1024 复核首屏截图；底部固定导航不得遮挡可读内容，若内容接近底栏，应优先用短高手机安全间距或滚动留白承接。
- **截图报告约束**：基础视口截图完成后必须运行 `npm run qa:mobile-screenshot-report -- 截图报告路径`；报告需要证明登录态、登录后壳校验、浅色+深色、默认+减少动态和五个基础视口均通过，不能只提交登录页、单一色彩模式、缺少减少动态效果证据或缺视口截图。
- **移动字号约束**：核心移动卡片、待办、关注字段、预算卡、楼宇房源卡、合同 / 财务 / 发票卡、月度明细卡、顶部上下文胶囊和底部导航标签的标签与辅助文案不得低于 12px；确需 10px 的场景只能用于桌面宽表、徽标或非手机主流程，并应避免放在 `mobile-typography-guard` 覆盖的移动卡片内。
- **移动阅读层约束**：移动首屏承载文字的工作台、待办、关注字段和轻管理卡片必须优先复用 `--liquid-mobile-readable-fill` 这类高不透明 readable 层；不得用大面积蓝色 / 高透明玻璃直接承载正文，品牌色只用于主数字、进度、选中态和少量状态强调。
- **移动深色系统约束**：移动端浅色 readable 玻璃不跟随全局暗色 slate 文本变浅；iPhone 深色外观下仍要验证登录 / 注册页、顶部上下文、年度达成主 KPI、三列 KPI、关注字段、今日待办和底部导航文字清楚可读。
- **移动高对比系统约束**：系统开启更高对比度时，登录 / 注册页、工作台主 KPI、三列 KPI、关注字段、底部导航、月度明细和筛选 / 轻管理浮层必须转为近不透明内容层；不得继续依赖透明蓝底、白字或弱化文字承载关键信息。
- **移动降低透明度约束**：系统开启降低透明度时，登录 / 注册页、工作台主 KPI、三列 KPI、今日待办、关注字段、底部导航、月度明细和筛选 / 轻管理浮层必须取消实时模糊并使用更实的内容层；透明质感可以弱化，但关键信息不能跟着变弱。
- **移动减少动态效果约束**：系统开启减少动态效果时，移动卡片、筛选 / 更多 bottom sheet、月度明细、抽屉、toast、加载图标和管理弹层不得依赖滑入、缩放、淡入、旋转、跑马灯或 pulse 来表达状态；层级应通过稳定位置、标题、焦点和高对比内容层表达。
- **移动截图证据约束**：涉及真机深色外观或文字发白的问题，验收截图必须使用 `--color-scheme dark` 或 `--color-scheme both`；涉及动效、弹层或加载状态的问题必须使用 `--reduced-motion reduce` 或 `--reduced-motion both`；涉及登录后工作台、管理入口和明细弹层的问题，应先通过 `npm run qa:mobile-auth-state` 生成本机登录态，再通过 `--storage-state` 加载并追加 `--require-authenticated`，不能只提交登录页截图作为通过证据。
- **触控约束**：移动端新增按钮、chip、图标按钮、summary 折叠项、登录 / 注册输入框、园区多选项、可点击 label 或 action sheet 选项时，必须满足 44px 最小触控目标；若视觉尺寸需要更小，也要通过全局触控层或额外内边距补足实际命中区，不允许只保留 32px 左右的小图标可点区。
- **横滑约束**：移动端新增状态 chips、月份 chips、楼宇切换或横向卡片轨道时，应复用 `liquid-mobile-search-chips` / `liquid-mobile-filter-months` 等共享滚动提示规则，保留边缘淡出、iOS 惯性滚动和隐藏滚动条后的可滑动感知。
- **移动弹层语义约束**：新增 bottom sheet、筛选、更多入口、轻管理 action sheet 或设置弹层时，应声明 `role="dialog"` / `aria-modal="true"`，提供可见标题或明确 `aria-label`，并至少支持背景点击、关闭按钮和 Escape 退出。
- **移动选中态语义约束**：登录 / 注册分段、底部导航、更多管理入口、状态 chips 和筛选选项只要有视觉选中态，就应同步声明 `aria-current` 或 `aria-pressed`；移动查询类输入应优先使用 `type="search"` 与 `enterKeyHint="search"`，避免视觉上可用但辅助技术和软键盘不知道当前控件意图。
- **移动表单语义约束**：登录 / 注册、搜索、金额、日期、面积等移动表单字段必须有明确标签关联或 `aria-label`，关键表单容器应通过 `aria-labelledby` / `aria-describedby` 关联可见标题和说明，并声明适合当前任务的 `inputMode` / `enterKeyHint`；邮箱类字段应关闭自动大写和拼写检查，避免 iOS 键盘自动修正造成登录失败。
- **移动反馈语义约束**：登录 / 注册、筛选、保存、导入和轻管理等移动流程的错误必须使用 `role="alert"` 或 assertive live region，成功 / 普通状态使用 `role="status"` 或 polite live region；提交、同步或导入按钮应暴露 `aria-busy`，避免用户只看到视觉 spinner。
- **移动分组选项约束**：登录 / 注册、筛选、园区多选、状态批量处理等成组选项必须有可见分组标签或 `aria-label`，加载时暴露 `aria-busy`，计数变化用 polite live region，空态 / 加载态不能只是一行静态小字。
- **移动弹层触发器约束**：凡是打开 bottom sheet、更多面板或筛选面板的按钮，都应声明 `aria-expanded` 和 `aria-controls` 指向稳定面板 id；触发器表达“是否展开”，具体页面或选项再用 `aria-current` / `aria-pressed` 表达，避免把临时弹层按钮误标为当前页面。
- **移动焦点圈定约束**：bottom sheet、更多面板、筛选面板和轻管理 action sheet 打开后，应把焦点移入弹层内的明确控件，并将 Tab / Shift+Tab 限定在弹层内；关闭后，应把焦点回到打开它的按钮。Escape、背景点击、关闭按钮和完成按钮应走同一关闭路径，避免键盘用户打开、浏览或关闭弹层后丢失操作位置。
- **性能约束**：移动端新增固定底栏、bottom sheet、明细弹层或遮罩时，优先复用现有移动玻璃性能规则；实景 `backdrop-filter` 不应超过 18px，复杂弹层应使用 `contain: paint` 或等价隔离，避免玻璃叠玻璃造成滚动掉帧。
- **迁移约束**：启用客户级历史欠费筛选前，应先执行 `npm run seal:backfill -- --from 2026-01 --to 上月 --dry-run` 检查旧封账行，再正式回填；缺 `details_json` 的旧行必须用脚本补齐，不能在前端用园区累计欠款反推客户欠费。
- **口径约束**：楼宇移动端单房源与批量状态处理均不修改合同、面积、租户引用、导入历史或云端保存口径；批量保存前二次确认，保存后仍需点击系统「保存」同步云端。
- **口径约束**：合同平板主从预览只复用 `filteredTenants`、`buildingFloorGroups` / `sortedYears`、`tenantAssetDisplayById`、`resolveRentUnitPriceForDisplay` 和 `getManagementFeeCardStatus` 的现有展示结果；预览选中状态不写入合同数据，也不新增租金、物业费、账期或退租计算路径。
- **口径约束**：财务平板主从预览只复用 `displayReceivableSections`、`groupedPayments`、`receivableBudgetDisplay`、`getEffectivePaidAmount`、`getRemainingReceivable`、`receivableRowSettlementDelta` 和既有流水编辑 / 删除、核销 / 缓缴回调；预览选中状态不写入 `PaymentRecord`、`BillingDetail`、账期备注或合同数据。
- **口径约束**：发票平板主从预览只复用 `filteredInvoices`、`allTenantById`、`buildingById`、`getBudgetMonth` 和既有 `handleToggleStatus` / `handleOpenDefer`；预览选中状态不写入 `InvoiceRecord`，预测租户 `virt_` 仍禁用确认和延期，不改变开票计划生成、延期、状态切换或预算归属月逻辑。
- **口径约束**：特殊业态平板主从预览只复用 `filteredSpecialBusinessTenants`、`specialBusinessSavedByTenantInMonth`、`specialBizAmountDraft` / `specialBizRemarkDraft` 和既有 `saveSpecialBusinessRow` / `removeSpecialBusinessReceivable` / `openTenantContractSummary`；预览选中状态不写入账期备注，保存和清除仍通过 `SPECIAL_BUSINESS_RECEIVABLES_NOTE_KEY` 更新当前账期特殊业态 JSON，不改变特殊业态注入应收、核销、合同滚动或历史备注合并逻辑。
- **已落地预算管理移动 / 平板阅读层收口**：预算行卡片补齐完整 `aria-label/title`，让客户名、楼宇房号、年度预算、累计实收和达成率在截断时仍能被完整确认；空预算月份、平板头部空置去化预测、应收专用说明、方案继承说明和导入预览提示统一使用 `text-xs` / slate-500 可读层，并纳入移动 UI 守卫。该阶段只优化预算管理阅读和辅助技术语义，不改变预算合计、实收合计、空置去化预测、导入预览或调整写入口径。
- **已落地工作台账单明细阅读层收口**：`BillingTable` 的账单卡补齐完整 `aria-label/title`，缓出 / 缓入、预算对齐说明、原账面已缓出、备注标签、当月应收 / 实收汇总和租金 / 物业费汇总标签统一回到 `text-xs` / slate-500 可读层；该阶段只提升工作台账单明细的扫读清晰度和辅助技术理解，不改变应收分段、手工应收差额、租金 / 物业费 tab、缓缴展示或备注保存口径。
- **已落地系统与备份阅读层收口**：`SystemSettingsPanel` 的小操作按钮、用户页签数量角标、当前园区、顶栏切换说明、AI 折叠提示、备份时间、导入 / 导出 project_id 说明和账号园区编号不再使用 10px / 11px 或 slate-400 弱灰；系统设置属于高风险维护入口，即使在移动“更多”中也必须保持可读。该阶段只优化管理信息清晰度，不改变云端配置、用户审批、备份恢复、导入导出或账号管理回调。
- **已落地财务应收核销表格阅读层收口**：`FinanceManager` 的应收来源胶囊、缓出 / 缓入说明、预算对齐提示、原账面已缓出提示、应收表格合计脚注、收款流水编号和空态统一回到 `text-xs` / slate-500 可读层；该阶段只提升财务应收核销和收款追溯阅读清晰度，不改变应收分段、剩余待收、实际核销、结算调整、缓缴撤销、收款撤销或批量核销口径。
- **已落地客户洞察阅读层收口**：`TenantInsights` 的搜索空态、未选客户空态、系统生成徽标、名称变更和付款周期变更箭头不再使用 10px、slate-300 或 slate-400 弱文字；客户洞察作为移动 / 平板可查看的分析入口，必须与 PC 端 readable 文字层保持一致。该阶段只提升文字清晰度，不改变 AI 动态采集、客户筛选、动态排序、园区周年生成或变更记录展示逻辑。
- **已落地 AI 助手抽屉阅读层与语义收口**：`AssistantPanel` 的实时徽标、消息时间戳和输入占位符不再使用 10px / 11px 或弱灰文字；助手抽屉补齐 dialog / modal 语义、可见标题关联、Escape 关闭和加载状态播报。该阶段只提升移动与桌面浮层阅读体验，不改变 AI 分析、发送消息或快捷问题逻辑。
- **已落地图表轴与空态阅读层收口**：通用趋势图、来源分析图和虚拟滚动表默认空态不再使用 10px / 11px 或 slate-400 弱文字；坐标轴 tick、趋势徽标和暂无数据提示统一回到 12px / slate-500 以上。该阶段只提升图表扫读清晰度，不改变 `monthlyTrends`、来源分析数据、tooltip formatter 或虚拟滚动逻辑。
- **已落地合同轻变更弹层阅读层查漏补缺**：名称变更历史箭头、付款周期账单预览分界说明、计算中 / 无账单状态、更多账单提示和历史变更箭头不再使用 slate-300 / slate-400 / blue-400 弱文字；该阶段只补齐确认型弹层内部状态可读性，不改变名称变更、付款周期变更、账单预览或确认写入口径。
- **已落地移动客户查询阅读层查漏补缺**：移动客户查询的搜索图标、搜索占位符和到期月份数量不再使用 slate-400 / placeholder slate-400 弱灰层，统一回到 slate-500 可读层；该阶段只提升查询入口在真机毛玻璃背景上的辨识度，不改变搜索、筛选、结果数量或跳转口径。
- **已落地移动底部导航阅读层查漏补缺**：底部“更多管理”面板里的方向箭头不再使用 slate-400 弱灰层，常驻导航和更多入口源码守卫禁止 9px / 10px / 11px 与 slate-300 / slate-400 回流；该阶段只提升导航触达清晰度，不改变导航项、更多入口或模块跳转逻辑。
- **已落地移动月度明细合计卡阅读层查漏补缺**：移动「月度收款明细」合计卡中的“年度累计达成”说明和累计达成值从低透明白字提升到 text-white/85、text-white/90；该阶段只优化深色合计卡可读性，不改变月度明细汇总或累计达成口径。
- **已落地移动登录 / 注册入口阅读层查漏补缺**：邮箱、姓名、密码输入图标和占位符统一从 slate-400 提升到 slate-500；该阶段只提升入口表单在毛玻璃面板上的辨识度，不改变登录、注册申请、园区授权或移动键盘口径。
- **已落地初始化数据录入单位提示阅读层查漏补缺**：初始化月度实收、出租率在移动卡片和桌面表格里的“￥ / %”单位提示统一从 slate-400 提升到 slate-500；该阶段只提升输入单位辨识度，不改变初始化字段、数值转换或保存口径。
- **已落地 App 外壳状态与图标阅读层查漏补缺**：启动加载、工作台加载、同步中状态、移动侧栏关闭图标、顶栏用户图标和云端保存操作人员图标统一从 slate-400 提升到 slate-500；该阶段只提升全局反馈和入口操作辨识度，不改变加载、同步、密码修改或保存逻辑。
- **已落地侧栏品牌区字号查漏补缺**：Kingdee 侧栏的 `V4.0` 徽标和 `Software Park` 副标题从 10px 提升到 `text-xs`，副标题字距同步收敛；该阶段只提升 PC / 移动侧栏打开时的品牌辅助信息可读性，不改变侧栏展开、导航或关闭逻辑。
- **已落地月度收款明细桌面宽表空值状态查漏补缺**：桌面月度明细宽表中空完成率和合计行同比占位从 slate-300 提升到 slate-500；该阶段只提升“暂无 / 不适用”状态辨识度，不改变月度明细汇总、完成率、同比或累计达成口径。
- **已落地云端保存弹层输入提示查漏补缺**：保存到云端弹层的操作人员和备份备注 placeholder 统一补充 `placeholder:text-slate-500`，避免移动端 elevated 毛玻璃表单沿用浏览器默认浅灰导致看不清；该阶段只提升保存弹层输入提示辨识度，不改变 `operatorName`、`snapshotNote`、云端保存或同步状态逻辑。
- **已落地财务移动搜索提示查漏补缺**：应收核销移动搜索条的搜索图标和“搜索客户” placeholder 统一从 slate-400 提升到 slate-500；该阶段只提升财务移动查询入口辨识度，不改变 `receivableKeyword`、筛选、选中预览、核销或收款写入口径。
- **已落地共享客户选择器提示查漏补缺**：`SearchableTenantSelect` 的客户副信息分隔符从 slate-300 提升到 slate-400，搜索 placeholder 从 slate-400 提升到 slate-500；该阶段只提升财务记账 / 押金转租等复用弹层的客户选择辨识度，不改变客户检索字段、排序、筛选、截断或选择回调。
- **已落地合同移动搜索提示查漏补缺**：合同轻管理移动搜索条的搜索图标和“搜索企业名称” placeholder 统一从 slate-400 提升到 slate-500；该阶段只提升合同移动入口检索辨识度，不改变 `searchTerm`、在租 / 历史 tab 切换、客户筛选、预览选中或合同写入口径。
- **已落地合同移动 / 平板预览阅读与触控收口**：合同主从预览里的状态胶囊、指标标签从 10/11px 提升到 `text-xs`，详情 / 续签 / 退租 / 回退按钮从 `min-h-10` 提升到 `min-h-11`，并纳入 `qa:mobile-ui` 守卫；该阶段只优化合同预览的阅读和触达，不改变详情、续签、退租、回退或筛选联动逻辑。
- **已落地合同 PC / 平板搜索提示查漏补缺**：合同工具条搜索框的“搜索企业名称…” placeholder 从 slate-400 提升到 slate-500，与移动合同搜索保持同一 readable 表单层级；该阶段只提升 PC / 平板合同检索入口辨识度，不改变 `searchTerm`、筛选、批量操作或合同写入口径。
- **已落地 AI 识别弹层输入提示查漏补缺**：AI 合同识别的字段 / 文本输入、AI 收款识别的文本输入和识别结果备注 placeholder 统一从 slate-400 提升到 slate-500；该阶段只提升 elevated AI 弹层输入提示辨识度，不改变 AI 请求、Excel 解析、楼宇 / 客户匹配、选中项或导入生成 `Tenant` / `PaymentRecord` 口径。
- **已落地 AI 收款匹配下拉箭头查漏补缺**：AI 收款识别移动卡片和桌面表格里的客户匹配下拉箭头从 slate-400 提升到 slate-500；该阶段只提升 select 可操作提示辨识度，不改变客户模糊匹配、置信度、选中项或 `PaymentRecord` 导入口径。
- **已落地 AI 上传区主图标查漏补缺**：AI 合同识别和 AI 收款识别的截图上传 / Excel 上传 Dropzone 主图标统一从 slate-400 提升到 slate-500；该阶段只提升上传入口操作信号辨识度，不改变图片预览、拖拽、粘贴、Excel 读取或 AI 识别请求逻辑。
- **已落地移动首页次级 KPI 材质收口**：工作台移动端的出租率、累计欠款、客户数从分割线区块改为独立 readable 玻璃小卡，自定义关注字段外壳改用命名 readable 玻璃层并加高字段卡；该阶段只优化移动首页经营字段可读性，不改变 KPI、关注字段 resolver、用户偏好保存或同步逻辑。
- **已落地移动更多管理触控与材质收口**：底部“更多管理”弹层的关闭按钮回到 44px 触控目标，管理入口行提升为更实的 readable 玻璃卡片，并为每个入口补齐完整 `aria-label/title`；该阶段只优化高频模块入口辨识和辅助技术理解，不改变导航项、权限判断、展开关闭或模块跳转逻辑。
- **已落地楼宇移动房源卡操作触控收口**：楼宇移动卡片和批量工具条中的「批量 / 全选 / 清空 / 取消 / 处理 N / 选择 / 快速处理 / 编辑」按钮从源码层统一到 44px 触控目标，并纳入 `qa:mobile-ui` 守卫；该阶段只优化手机楼宇轻管理的手指命中区，不改变房源状态、批量二次确认、合同锁定、面积或保存口径。
- **已落地楼宇移动 action sheet 触控与说明语义收口**：楼宇快速处理和批量状态处理弹层的关闭按钮、底部操作按钮统一回到 44px 触控目标，并用 `aria-describedby` 关联房源上下文 / 批量处理规则；房源说明保留完整 `title`，避免小屏截断后无法确认。该阶段只优化移动弹层可读性、触控和辅助技术理解，不改变房源状态、合同控制、面积或保存口径。
- **已落地财务移动 / 平板预览操作触控收口**：收款流水预览的修改 / 删除、特殊业态保存 / 合同概要 / 清除、应收预览的合同概要 / 收款 / 缓缴 / 撤销核销按钮在源码层统一使用 44px 触控目标，并纳入 `qa:mobile-ui` 守卫；该阶段只优化财务移动与平板主从预览的可点击性，不改变收款流水、特殊业态应收、核销、缓缴或撤销写入口径。
- **已落地移动客户查询结果 / 筛选操作触控收口**：顶部「筛选 / 清除」、筛选 sheet 关闭按钮、结果卡「合同 / 核销」、空态「清除筛选 / 回到指标」和长结果「显示更多 / 收起结果」按钮在源码层统一使用 44px 触控目标，并纳入组件测试与 `qa:mobile-ui` 守卫；该阶段只优化客户查询顶部操作、结果卡和筛选弹层的点击稳定性，不改变搜索索引、筛选、结果数量、合同定位或核销跳转口径。
- **已落地移动首页年度切换触控收口**：工作台首屏年度左右切换按钮从 `h-9 w-9` 提升到源码层 `h-11 w-11`，与底部导航、筛选和弹层关闭按钮同样满足 44px 命中区；该阶段只优化年度步进的手指命中和键盘焦点稳定性，不改变年度切换、KPI 重算或缓存读取逻辑。

后续仍建议继续推进：对移动端关键屏做 375 / 390 / 430 / 768 / 1024 宽度截图 QA，重点复核毛玻璃文字对比度、底部浮层遮挡、横滑提示和长列表滚动手感。
