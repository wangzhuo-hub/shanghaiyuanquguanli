# 运算后移：代码分析、Bug 排查与优化建议

> 分析目标：系统已将计算模块后移到服务端（compute-engine + integration-gateway），前端不再承接大量运算。本文围绕这一目标，梳理当前实现、排查潜在 Bug，并提出可继续优化的方向。
>
> 分析范围：`services/computeFallbackPolicy.ts`、`services/cloudComputeClient.ts`、`scripts/compute-engine.ts`、`scripts/integration-gateway.ts`、`scripts/paymentApiAuth.ts`、`services/metricsWorkerClient.ts`、`App.tsx` 调用链，及既有多轮优化文档。
>
> 结论按可信度分两档：**【已确认】**（基于完整源码精读）与 **【待核实】**（建议在最新代码上二次验证，见文末说明）。

---

## 一、运算后移架构现状（一句话脉络）

前端发起计算请求 → `services/cloudComputeClient.ts`（10 个 compute 端点 + in-flight 去重）→ HTTP → `scripts/integration-gateway.ts`（鉴权 + 路由）→ `scripts/compute-engine.ts`（**复用前端 `services/*` 同一套算法**）→ 读 PocketBase 算完返回。

这套设计的几个**优点**值得肯定：

1. **口径一致性强**：`compute-engine.ts` 直接 import 前端的 `dashboardMetrics`、`billingService`、`receivableListHelpers`、`sourceAgentMetrics`、`contractAnalysisMetrics`，服务端与浏览器跑的是同一份计费/指标内核，从根上避免了"两套口径"问题。
2. **降级链路清晰**：`computeFallbackPolicy.ts` 用统一策略函数（`isServerComputeEnabled` / `shouldRunLocal*Fallback`）决定是否回退本地重算；生产环境通过 `VITE_ENABLE_LOCAL_HEAVY_COMPUTE_FALLBACK` 默认禁用前端重算。
3. **鉴权边界完整**：所有 `/api/v1/app/compute/*` 用户态端点都经过 `authenticateRequest` + `resolveAuthorizedProjectId`（按 `users.allowed_project_ids` 做跨园区越权拦截）；集成态 `/api/integration/compute/*` 经 `requireIntegrationAuth`（内部 token 或来源签名 + 跨园区校验）。
4. **服务端缓存 + 写后失效**：compute-engine 对 dashboard / billing 结果按 `projectId|...|version` 做 TTL 缓存；网关在每次成功写入后调用 `clearComputeCachesAfterSuccessfulWrite` 清理对应园区缓存。
5. **五轮优化已基本落地**：从 git 历史看，`compute/refresh` 防抖、服务端封账（`sealMonth`）、定时任务 opt-in（`GATEWAY_SCHEDULER_ENABLED`）、外部写入轮询、batch 原子性复审修正等均已合入。

总体判断：**架构方向正确、落地度高**。下面的问题多为"收尾级"的健壮性与性能项，而非架构缺陷。

---

## 二、Bug 与风险排查

### 2.1【已确认】Worker 计算无超时，消息丢失会导致 Promise 永久挂起

`services/metricsWorkerClient.ts` 的 `computeMetricsInWorker`：

```ts
return new Promise<WorkerComputeResult>((resolve, reject) => {
    pending.set(id, { resolve, reject });
    worker.postMessage({ id, data, options });
});
```

`pending` 里的 Promise 只在两种情况下被 settle：worker 回 `message`，或 worker 触发 `error` 事件。但若 worker 内部卡死、或 message 因某种原因未送达（非 `error`），该 Promise **永不 resolve/reject**，`pending` 条目泄漏，调用方（fallback 路径）一直挂起。

**建议**：加请求级超时（如 15–30s），超时则 `reject` 并 `pending.delete(id)`，同时考虑回收/重建 worker 实例。

### 2.2【已确认】草稿计算上行整包 DashboardData，前端承接了"序列化 + 全量 hash"的隐性重活

`cloudComputeClient.ts` 的 `fetchCloudDraftComputedDashboard` / `fetchCloudDraftComputedBilling` 把**整个 `DashboardData`** 作为 `draft_data` POST 给服务端；同时 `dashboardDraftIdentityKey(data)` 会对 buildings/tenants/payments/invoices/... 所有集合在**主线程**逐一计算 identity key 作为去重缓存键。

这与"前端不承接大量运算"的目标存在张力：CPU 密集的指标计算确实后移了，但**大对象的 JSON 序列化、identity hash 计算仍留在浏览器主线程**，在大园区（如北京）下可能成为新的卡顿点，上行 payload 也偏大。

**建议**：(a) 草稿计算只上送 dirty 子集 + baseline 版本号，由服务端按版本补齐 baseline；(b) identity key 计算移入 Worker，或直接改用 `cloudSaveVersion` + dirty 摘要作为缓存键，避免全量 hash。

### 2.3【已确认】前端仍打包完整本地计算引擎（fallback 用），增加首屏体积

生产环境虽默认禁用本地重算，但 `recalculateMetrics → calculateDashboardMetrics`、`billingService` 等 fallback 引擎仍随主包加载（`dashboardMetrics.ts` 64KB、`billingService.ts` 69KB 源码量级）。即"运算后移"减轻了运行时 CPU，但**没有减轻 bundle 体积**。

**建议**：将 fallback 引擎做成动态 `import()`，仅当 `shouldRunLocal*Fallback` 命中时按需加载；生产路径下用户完全不下载这部分代码。

### 2.4【待核实】compute-engine billing 缓存的 version 读写键可能不对称

`scripts/compute-engine.ts` 的 `computeBilling`：读缓存时键用 `cacheVersion = readVersionForCache()`（读 `dashboard_data_version` note）；写缓存时键用 `dataVersion = rawData.cloudSaveVersion ?? cacheVersion`（取自 `fetchPocketBaseBackup` 返回的数据）。

正常两者应一致，但若"已 bump 版本但 note 与 backup 的 `cloudSaveVersion` 短暂不同步"，会出现**写入键 ≠ 读取键**，导致缓存频繁 miss（性能损失）或读到过期版本（正确性风险）。dashboard 缓存也有类似 `rawDataVersion ?? cacheVersion` 的兜底逻辑，建议一并核对。

**建议**：统一缓存 version 的唯一来源（要么都用 backup 内的 `cloudSaveVersion`，要么都用 `readCloudSaveVersion`），并加一条单测覆盖"写后立即读命中"。

### 2.5【待核实】集团大屏 `handleAppBigScreen` 未做 `project_id` 越权校验

`handleAppBigScreen` 经过 `authenticateRequest`，但（与其它 app compute 端点不同）**未调用 `resolveAuthorizedProjectId`**。考虑到大屏本身是跨园区汇总，这在设计上可理解，但需确认其内部是否对传入的 `park_ids` 逐个做 `canAccessProject` 过滤——否则普通 `group_admin` 可能通过构造 `park_ids` 拉取到授权范围外园区的大屏数据。

**建议**：在该 handler 内对 `park_ids` 按用户 `allowedProjectIds` 过滤，未授权园区直接剔除并在响应中记 `skipped`。

### 2.6【待核实】`isServerComputeEnabled` 的 `authToken === undefined` 分支

`computeFallbackPolicy.ts` 中 `return options.authToken === undefined ? true : !!options.authToken;`——未显式传 token 时即判定服务端可用。需确认所有调用点都正确传入了 token，否则在无 token 场景会先打一个注定 401 的请求，再 fallback，多一次无效往返。

---

## 三、基于"前端不承接大量运算"目标的优化建议

> 按"是否进一步减轻前端负担"排序。P0 直接服务于目标，P1/P2 为配套健壮性与体验。

| 优先级 | 事项 | 说明 | 对应上文 |
|---|---|---|---|
| P0 | 草稿计算 payload 瘦身 + hash 移出主线程 | 只上送 dirty 子集；identity key 改用版本号或移入 Worker | 2.2 |
| P0 | fallback 引擎动态分包 | 生产路径不下载本地计算代码，减小首屏 | 2.3 |
| P1 | Worker 超时与降级 | 防止挂起与 pending 泄漏 | 2.1 |
| P1 | 缓存 version 源统一 + 单测 | 提升服务端缓存命中、消除过期风险 | 2.4 |
| P1 | 大屏 park_ids 授权过滤核查 | 安全 | 2.5 |
| P2 | 巨型文件/组件拆分 + 路由级懒加载 | `App.tsx` 5938 行、`ContractManager.tsx` ~398KB、`BudgetManager.tsx`/`FinanceManager.tsx` ~160KB，首屏解析与维护成本高 | — |
| P2 | compute 可观测性埋点 | 记录各 compute 端点耗时、缓存命中率、fallback 命中次数，**量化"前端是否真的不再重算"** | — |
| P2 | 历史月封账调度默认开启 | `sealMonth` 已服务端化，但调度是 opt-in；若运维未开 `GATEWAY_SCHEDULER_ENABLED`，历史月仍可能被实时重算 | — |

### 关于巨型文件（补充）

运算后移解决的是"算得慢"，但前端"加载慢/难维护"另有来源：单文件过大。建议把 `ContractManager` / `BudgetManager` / `FinanceManager` 按子视图拆分并配合 `React.lazy` + 路由级代码分割，与运算后移正交、可并行推进。

### 关于可观测性（补充）

目前难以回答"前端究竟还在算多少"。建议在 `cloudComputeClient` 统一封装里加耗时/失败/fallback 计数，在 compute-engine 加缓存命中日志。有了数据才能确认后移目标的达成度，并定位剩余热点。

---

## 四、验证建议（请在本地执行）

本次分析以**源码精读**为主。受运行环境的命令行工具中途不可用影响，未能在本会话内完成编译与测试的动态验证。请在落地修改前后运行：

```bash
npx tsc --noEmit -p tsconfig.json   # 类型与语法
npm test                            # services/__tests__ 现有用例
npm run build                       # 产物体积 / 构建可用性
npm run check:pb                    # 防误提交 PB 运行时库
```

重点为 2.4（建议补"写后即读命中"单测）与 2.1（Worker 超时）补测试用例。

---

## 五、本文结论与局限

- 运算后移**架构方向正确、落地度高**，鉴权与口径一致性是亮点，五轮优化已基本合入。
- 剩余问题集中在**收尾级健壮性与前端隐性开销**：Worker 超时、草稿全量 payload/hash、fallback 引擎分包、缓存 version 一致性。
- 标注【待核实】的条目（2.4 / 2.5 / 2.6）建议结合最新代码与一次完整 `tsc`/`test` 二次确认后再决定是否修改。
