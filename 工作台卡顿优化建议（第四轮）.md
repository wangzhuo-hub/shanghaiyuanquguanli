# 工作台打开卡顿 — 第四轮优化建议

场景：云端数据已全量拉到本地，打开北京园区工作台仍然明显卡顿。数据本地化后，卡顿与网络无关，问题全部在**主线程同步计算 + 全量重渲染**这条链上。代码复核确认的执行路径如下。

## 一、打开工作台时实际发生了什么（卡顿解剖）

切到工作台 tab 的那一刻，依次发生：

**① 主线程同步全量重算（最大头）** — App.tsx:760-767：`activeTab` 变化触发 `recalculateMetrics`，此时 `quickMode=false`，一次性同步计算：当年 12 个月趋势 + **去年 12 个月趋势** + 欠款循环（2026-01 至上月）+ 每月合同应收/物业费应收。每个月都对全部租户跑一遍账单生成，北京园区几百租户 × 24+ 个月，轻松秒级。`runWhenBrowserIdle` 只是把这块任务"晚一点开始"，开始后仍是一整块不可中断的同步计算，页面照样冻住。

**② setData 整树重渲染，且全项目没有一个 React.memo** — 全 components 目录 grep 不到任何 `React.memo`。`data` 每次重算都是全新引用，StatsCards、Charts（recharts 重绘）、TenantInsights、DashboardAlerts、RecentActivityTable 全部无条件重渲染。BillingTable 仍然全行渲染——`VirtualizedTable.tsx` 写好三轮了，至今零引用。

**③ 2 秒后自动保存链补两刀** — 重算 setData 触发 autosave effect：`JSON.stringify(data)`（含 monthlyTrends/currentMonthBilling/recentSignings 等全部派生字段，体积可能数十 MB）同步序列化 + `dashboardDataToPbRecords` 全量建图 + `diffPbRecords` 全量深比较。数据全在本地后这三步都是 O(全部行)，又是几百毫秒级的主线程占用，表现为"打开后过两秒又卡一下"。

**④ 账单明细重复重建** — App.tsx:812-861 的 effect 依赖里有 `data` 引用，每次重算后即使 `dashboardBillingKey` 没变也会重新调度 `buildBillingDetailsForPeriodService(year, month, data)`——且**不传共享 cache**，每次都从零生成全部租户账单。

**⑤ 切园区双渲染** — switchProject 先 KPI 快照 setData（预览），全量重算后再 setData 一次，整树渲染两遍。

## 二、优化建议（按投入产出排序）

### 2.1 切 tab 不重算：结果缓存（改动最小，立竿见影）
工作台指标只由（原始数据, 年, 季度）决定。用 ref 缓存上次完整计算结果及其 key（原始数据引用 + year + quarter），activeTab effect 里 key 没变就直接跳过。从财务/合同页切回工作台时数据通常没变，①的整块计算直接归零。预计半天内完成。

### 2.2 拆分一次性计算：当年先渲，去年与欠款延后
`calculateDashboardMetrics` 里 `prevYearMonthlyTrends`（只供同比对比表用）和欠款循环不必阻塞首屏：先算当年 trends → setData 渲染 KPI → `requestIdleCallback` 分片补算去年和欠款 → 二次 setData 补齐。把一次 N 秒冻结拆成「短冻结 + 后台补齐」。这是 Worker 化之前的过渡方案；**根治仍是把 calculateDashboardMetrics 移进 Web Worker**（输入输出均为纯 JSON，无技术障碍，前三轮均已建议）。

### 2.3 渲染侧：memo + 虚拟化（②的解）
给 StatsCards、Charts、TenantInsights、DashboardAlerts、RecentActivityTable、BillingTable 包 `React.memo`，props 从「整个 data」收敛为各自需要的切片（monthlyTrends、currentMonthBilling 等数组引用在数据未变时应保持稳定——配合 2.1 的结果缓存天然成立）。BillingTable / RecentActivityTable 接入已存在的 VirtualizedTable。

### 2.4 持久化与 diff 减负（③的解）
- stringify 前剔除派生字段（monthlyTrends、prevYearMonthlyTrends、currentMonthBilling、recentSignings、expiringSoon、parkingStats），缓存体积通常能砍 50-80%，序列化时间同比例下降；这也是"派生/原始分离"的低成本过渡实现；
- autosave 的 `dashboardDataToPbRecords + diffPbRecords` 挪进 `requestIdleCallback`（或与 2.2 一起进 Worker），不要在 setTimeout 回调里直接同步跑。

### 2.5 账单明细 effect 修依赖 + 传 cache（④的解）
effect 依赖从 `data` 收敛为 `dashboardBillingKey`（用 ref 取最新 data）；维护模块级 `WeakMap<DashboardData, BillingCache>`，让账单明细与重算共享同一 cache，避免每次从零生成账单。

## 三、先量化再动手（强烈建议）

改之前先确认各环节占比，避免优化错位：

1. **看现成日志**：DevTools Console 过滤 `[metrics]`，应用自带 `[metrics] recalculate XXXms {tenants, payments, buildings}` 输出——打开工作台时这个数字是多少？顺带把 tenants/payments 数量发我；
2. **Performance 面板**：录制一次「切到工作台」，看 Long Task 分布——计算（calculateDashboardMetrics）、渲染（React commit）、序列化（JSON.stringify）三段各占多少；
3. 连上 Claude in Chrome 扩展后我可以直接在 localhost:1001 上帮你跑这些测量。

若 `[metrics]` 显示重算只有几百 ms 而页面卡数秒，则瓶颈在渲染侧（②），优先做 2.3；若重算本身数秒，优先 2.1/2.2。

## 四、优先级

| 顺序 | 事项 | 预期效果 | 工作量 |
|---|---|---|---|
| 1 | 2.1 切 tab 结果缓存 | 切回工作台基本零卡顿 | 半天 |
| 2 | 2.4 持久化剔除派生字段 + diff 进 idle | 消除"打开后 2 秒再卡一下" | 半天 |
| 3 | 2.3 React.memo + 虚拟表接入 | 重算后渲染时间大幅下降 | 1-2 天 |
| 4 | 2.2 拆分计算（过渡）→ Worker（根治） | 任何场景主线程不再冻结 | 1 天 / 2-3 天 |
| 5 | 2.5 账单明细依赖收敛 + 共享 cache | 消除重复账单生成 | 半天 |
