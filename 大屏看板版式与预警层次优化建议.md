# 大屏看板版式与预警层次优化建议

评估日期：2026-05-16  
评估页面：`https://kdpark.fun/?screen=tv`  
评估依据：当前大屏截图与页面展示效果

## 1. 总体结论

当前大屏已经具备基础播放能力，但视觉层面存在两个核心问题：

1. 页面底部“最新动态”和轮播圆点遮挡正文数据，尤其是集团总览页下方关键指标被压住。
2. 智能预警页风险提示以单列列表堆叠展示，缺少类型区分、优先级分层和重点突出。

建议把大屏从“内容自然滚动 + 底部覆盖”调整为“三段式固定布局”，并把智能预警从“日志列表”改为“分组风险看板”。

## 2. 当前问题拆解

### 2.1 页面底部数据遮挡

从截图看，集团总览页下方“关键指标”区域被底部最新动态条遮挡，右侧还出现了页面滚动条。

这说明当前布局中：

- 主内容区没有明确扣除底部动态条高度。
- 底部“最新动态”和轮播圆点更像覆盖层，而不是正常文档流中的固定行。
- 主 slide 区域允许 `overflow-auto`，导致电视大屏场景出现滚动条。

大屏播放场景不建议依赖用户滚动。会议室电视、前台屏幕、驾驶舱屏幕都应该一屏完整呈现当前轮播内容。

### 2.2 最新动态条承载过多风险信息

底部动态条中出现大量“到期”“逾期”等风险卡片，与智能预警页内容重复。

这会带来两个问题：

- 底部动态条变高、变拥挤，压缩主内容区。
- 用户难以区分“刚刚发生的新动态”和“需要处理的风险预警”。

建议最新动态条只承担“最近发生的业务事件”，风险类内容只保留少量高优先级提醒。

### 2.3 智能预警缺少层次感

智能预警页当前展示：

- 高风险 17 条
- 中风险 48 条
- 低风险 67 条
- 总计 132 条

但下方列表是单列平铺，所有风险看起来像日志流。

问题：

- 高风险内部没有进一步排序。
- 逾期未收、合同到期、经营指标异常混在一起。
- 每条卡片视觉权重接近，不容易看出最重要的问题。
- “待收款”和“逾期未收”文案容易混用，财务口径不够清晰。

## 3. 布局优化建议

### 3.1 调整为三段式固定布局

建议 `BigScreenDashboard` 外层改成明确的行布局：

```text
顶部状态栏：44px
主内容区：自动占满剩余空间
最新动态条：96px - 110px
轮播指示器：32px
```

目标：

- 主内容永远不会被底部动态条遮挡。
- 底部动态条和轮播圆点固定占位。
- 页面不出现浏览器滚动条。

推荐结构：

```tsx
<div className="h-screen w-screen overflow-hidden grid grid-rows-[44px_minmax(0,1fr)_104px_32px]">
  <TopBar />
  <main className="min-h-0 overflow-hidden">
    <CurrentSlide />
  </main>
  <BigScreenEventTicker />
  <SlideDots />
</div>
```

如果需要兼容小屏，可用 CSS clamp 或媒体查询：

```css
grid-template-rows: 44px minmax(0, 1fr) clamp(84px, 12vh, 110px) 32px;
```

### 3.2 主内容区禁止自然滚动

当前 slide 区域建议从：

```tsx
<div className="flex-1 min-h-0 overflow-auto">
```

改为：

```tsx
<div className="min-h-0 overflow-hidden">
```

大屏页面应通过压缩内容密度、减少首屏模块数量来适配屏幕，而不是让用户滚动。

### 3.3 给底部动态条固定高度

建议 `BigScreenEventTicker` 高度固定，不随内容变高。

建议高度：

- 标准电视：104px
- 小屏：84px
- 超宽屏：96px

事件卡片内部用单行或两行展示，超出截断。

## 4. 集团总览页优化

### 4.1 压缩纵向空间

当前集团总览页包含：

- 顶部标题
- 年度累计实收 + 综合出租率 hero 卡
- 当前账期三卡
- 关键指标四卡
- 面积动态三卡

内容过多，导致底部遮挡。

建议首屏只保留：

1. 顶部标题
2. Hero 核心指标
3. 当前账期三卡
4. 关键指标四卡

“面积动态”可以移动到：

- 单园区页
- 园区对比页
- 新增“招商动态页”

### 4.2 Hero 卡片压缩建议

当前 hero 卡片视觉效果不错，但高度偏大。

建议：

- 外层 padding 从 `p-6 md:p-10` 降为 `p-5 md:p-7`
- 标题区 margin 从 `mb-8` 降为 `mb-5`
- 大数字字体保留，但减少辅助文案高度
- 卡片最大宽度可保持 `max-w-5xl`

### 4.3 当前账期卡片固定高度

建议当前账期三张卡使用固定高度：

```tsx
className="h-[96px] md:h-[112px]"
```

卡片内容：

```text
本账期已收
46万
```

辅助文字尽量减少，不要让卡片高度由内容撑开。

### 4.4 “本月”文案建议改为“本账期”

截图中显示：

- 本期已收
- 本期应收
- 本期未收

这是合理方向。建议避免使用“本月实收”，因为当前值来自 `currentMonthBilling.amountPaid`，是账期核销口径，不一定等于自然月到账现金流。

如果后续要展示自然月现金流，建议另设：

- 本月到账：来自 `payments.date`
- 本账期已收：来自 `currentMonthBilling.amountPaid`

## 5. 最新动态条优化

### 5.1 明确职责

最新动态条建议只展示“刚发生的业务事件”：

- 新签约
- 新收款
- 新开票
- 极少量高优先级风险提醒

不建议把所有合同到期、所有待收款都放到底部滚动条。

### 5.2 风险类事件限流

建议规则：

| 类型 | 是否进入底部动态条 | 数量限制 |
|---|---|---|
| 新签约 | 是 | 最近 30 天 |
| 新收款 | 是 | 最近 30 天 |
| 新开票 | 是 | 最近 30 天 |
| 高风险逾期 | 可选 | Top 3 |
| 高风险到期 | 可选 | Top 3 |
| 中低风险 | 否 | 不进入底部动态条 |

完整风险列表应进入智能预警页。

### 5.3 动态卡片简化

当前底部卡片信息较多。建议压缩为：

```text
[收款] 上海园区 · 某某公司 · 18.6万
2026-05-16
```

到期提醒：

```text
[到期] 北京园区 · 某某公司 · 55天后到期
```

逾期提醒：

```text
[高风险] 深圳园区 · 某某公司 · 未收169.4万
```

## 6. 智能预警页优化

### 6.1 从单列列表改为分组看板

当前预警页不应把 132 条全部平铺展示。建议改为分组看板：

```text
预警总览
├─ 高风险待收款 Top 6
├─ 合同到期预警 Top 6
├─ 经营指标风险 Top 4
└─ 中低风险待跟进 Top 6
```

每个分区显示：

- 分区标题
- 数量
- Top N 列表
- 最高风险摘要

### 6.2 建议分区

| 分区 | 包含类型 | 排序 |
|---|---|---|
| 高风险待收款 | `receivable_overdue` 且 `level = high` | 未收金额倒序 |
| 合同到期预警 | `contract_expiring` | 剩余天数升序 |
| 经营指标风险 | `low_occupancy` / `low_collection` | 差距倒序 |
| 中低风险待跟进 | 其它 medium / low | 等级 + 金额 / 天数 |

### 6.3 预警页版式建议

建议使用两栏或三栏结构：

```text
顶部：高 / 中 / 低风险统计卡

左侧大栏：
  高风险待收款

右侧上：
  合同到期预警

右侧下：
  经营指标风险

底部或次屏：
  中低风险待跟进
```

在 1920x1080 下可以使用：

```text
grid-cols-[1.4fr_1fr]
```

左侧放最重要的高风险收款，右侧放到期和经营预警。

### 6.4 预警卡片结构

当前卡片标题里塞了太多信息，右侧只有一个类型标签。建议改为更有层次的结构：

```text
[高] 待收款                         未收 169.4万
深圳园区 · 深圳市博金纳实业发展有限公司
账期 2026-05 · 状态：逾期未收
```

合同到期：

```text
[中] 合同到期                       剩余 54 天
北京园区 · 某某公司
面积 487㎡ · 月租 2.0万
```

经营指标：

```text
[中] 出租率低于目标                 差 22 个百分点
集团总览 / 北京园区
当前 64% · 目标 86%
```

### 6.5 风险颜色建议

| 风险类型 | 主色 | 用法 |
|---|---|---|
| 高风险待收款 | 红色 | 左侧色条、金额、风险徽标 |
| 合同到期 | 黄色 / 橙色 | 左侧色条、剩余天数 |
| 经营指标风险 | 蓝色 / 紫色 | 左侧色条、指标差距 |
| 中低风险 | 灰蓝 | 弱化展示 |

高风险卡片可以轻微提高背景亮度：

```tsx
bg-red-500/8 border-red-500/20
```

普通卡片：

```tsx
bg-white/5 border-white/8
```

## 7. 预警排序优化

当前只按高 / 中 / 低排序还不够。建议在同等级内部继续排序：

### 7.1 待收款排序

```ts
receivable_overdue:
  1. level: high > medium > low
  2. amount: desc
```

### 7.2 合同到期排序

```ts
contract_expiring:
  1. days: asc
  2. amount/monthlyRent: desc
```

### 7.3 经营指标排序

```ts
low_occupancy / low_collection:
  1. level
  2. gap: desc
```

建议在 `BigScreenAlert` 中补充 `sortValue` 或 `metricGap` 字段，避免 UI 层重复解析 description。

## 8. 预警文案口径优化

当前“待收款”和“逾期未收”容易混用。建议按 `BillingDetail.status` 细分：

| `BillingDetail.status` | 建议文案 |
|---|---|
| `Overdue` | 逾期未收 |
| `Partial` | 部分未收 |
| `Unpaid` | 待收款 |
| `Paid` | 不生成预警 |

底部动态条和智能预警页都应使用相同文案规则。

这样可以避免把尚未真正逾期的未收款全部标为“逾期”，更符合财务管理口径。

## 9. 推荐修改文件

### 9.1 `components/BigScreenDashboard.tsx`

重点：

- 改为 grid rows 固定布局。
- 统一 slide 总数。
- 主内容区 `overflow-hidden`。
- 底部动态条和轮播圆点成为布局行，不覆盖正文。

### 9.2 `components/big-screen/BigScreenSummary.tsx`

重点：

- 压缩顶部标题和 hero 卡片高度。
- 固定“当前账期”卡片高度。
- 首屏减少模块数量。
- 将面积动态移到其它页面或缩小展示。

### 9.3 `components/big-screen/BigScreenEventTicker.tsx`

重点：

- 固定高度。
- 限制风险类事件进入数量。
- 压缩事件卡片结构。
- 确保不会撑高底部区域。

### 9.4 `components/big-screen/BigScreenAlerts.tsx`

重点：

- 从单列列表改为分区看板。
- 高风险待收款、合同到期、经营指标风险分区展示。
- 每区 Top N。
- 卡片信息分层展示。

### 9.5 `services/bigScreenAlerts.ts`

重点：

- 增强排序规则。
- 区分 `Overdue` / `Partial` / `Unpaid` 文案。
- 给 `BigScreenAlert` 增加 `sortValue`、`metricGap`、`statusLabel` 等辅助字段。

### 9.6 `services/bigScreenEvents.ts`

重点：

- 底部动态只保留业务新动态。
- 风险事件限流或交给 `BigScreenAlerts` 页面。

## 10. 建议实施顺序

1. 先修 `BigScreenDashboard` 固定布局，解决底部遮挡。
2. 压缩 `BigScreenSummary` 高度，确保 1366x768 和 1920x1080 都不遮挡。
3. 调整 `BigScreenEventTicker` 高度和事件类型范围。
4. 重构 `BigScreenAlerts` 为分组看板。
5. 优化 `bigScreenAlerts` 排序和文案口径。
6. 用浏览器验证 1366x768、1920x1080、移动横屏三种尺寸。

## 11. 验收标准

### 11.1 版式验收

- 页面底部不遮挡任何正文数据。
- 右侧不出现浏览器滚动条。
- 顶部状态栏、主内容、最新动态、轮播圆点各自占位清晰。
- 1366x768 下仍能完整显示当前 slide 的核心内容。
- 1920x1080 下视觉不显空、不拥挤。

### 11.2 最新动态验收

- 最新动态条高度稳定。
- 不因事件数量变多而撑高。
- 新签约、新收款、新开票能滚动。
- 中低风险不大量进入底部动态条。

### 11.3 智能预警验收

- 高 / 中 / 低风险统计清晰。
- 高风险待收款优先展示。
- 合同到期预警独立分区。
- 经营指标风险独立分区。
- 单条卡片能一眼看出风险类型、园区、客户、金额或天数。

## 12. 总结

当前大屏主要问题不是数据不足，而是布局和信息组织方式需要升级。

核心优化方向：

1. 底部动态条从“覆盖层”改为“固定布局行”。
2. 主内容区不滚动，内容密度自适应。
3. 最新动态只讲“最新发生的业务事件”。
4. 智能预警从“堆列表”改为“分组风险看板”。
5. 预警文案区分“待收款、部分未收、逾期未收”，保持财务口径准确。

完成这些优化后，大屏会更接近管理驾驶舱：第一眼看趋势，第二眼看风险，第三眼知道该处理谁。
