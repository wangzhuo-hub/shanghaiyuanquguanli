# 园区经营关键指标计算逻辑评估

评估日期：2026-05-16  
评估范围：出租率、每月应收、实收归集、累计欠款等园区经营关键数据的计算口径与运算单元设计。  
结论摘要：当前项目已经具备较成熟的“合同滚动账单 + 财务核销后处理 + 预算方案快照”架构，核心方向是正确的；但对园区实时经营管理而言，出租率、欠款、预算执行收缴率仍存在若干口径边界，容易让管理层把“合同经营事实”“预算目标”“财务核销状态”混读。

## 一、总体架构判断

当前关键运算集中在：

- `services/dashboardMetrics.ts`：仪表盘指标、月度趋势、应收明细、欠款汇总。
- `services/billingService.ts`：合同账单生成、付款周期、免租、提前退租、账期调整、预算假设。
- `services/receivableListHelpers.ts`：收款按账期分摊、缓缴、特殊业态、手工应收等财务后处理。
- `components/FinanceManager.tsx`：财务页顶部统计和应收核销展示口径。

整体上，项目已经把“合同纯应收”和“实际核销应收”做了分离：

- 合同应收：`buildContractOnlyReceivableForPeriod` 只按合同条款、应收专用方案、预算假设和调整滚动生成，不含缓缴、导入覆盖、特殊业态手工录入、手工应收。
- 财务核销应收：`buildBillingDetailsForPeriod` 在合同账单基础上继续叠加导入预算覆盖、缓缴调入调出、特殊业态手工应收等。
- 实收归集：预算执行趋势按真实入账日期归集，核销明细按 `period` 账期字段分摊。

这个分层是合理的，适合园区经营看板：管理层可以看合同履约口径，财务可以看核销口径。但当前页面展示和累计指标里，仍有若干口径未完全显式化。

## 二、出租率计算评估

### 现有计算模式

出租率主要有两种：

1. 实时出租率  
   在 `calculateDashboardMetrics` 中，以当前日期为基准，分母为非自用、非场地类单元面积，分子为当前已签约且未终止的租户面积。`Expired` 被排除，`Terminated` 通过 `terminationDate` 判断。

2. 月度趋势出租率  
   在 `calculateTrends` 中逐月计算：分母仍为可出租面积，分子判断逻辑是签约/起租日期不晚于月末，并且合同有效截止日大于月末。

初始化数据中若存在 `MonthlyInitData.occupancyRate`，会直接覆盖月度趋势中的出租率。

### 正确性判断

总体方向正确：出租率按面积口径计算，排除自用单元和场地类资产，符合园区招商管理常见口径。实时出租率使用当前事实，月度趋势使用月末快照，也基本合理。

但有三个值得注意的点：

1. 月度趋势使用 `effectiveEnd > endDate`，如果合同在月末当天结束，该月不计出租；若园区希望“月内曾出租”或“截至月末仍有效含当天”口径，这里会偏保守。

2. 分子使用 `signingDate || leaseStart` 作为达成日期。对经营汇报来说，签约面积、起租面积、计租面积是三个不同指标。当前出租率更像“已签约占用率”，而不一定是“已起租计租出租率”。

3. 初始化出租率覆盖趋势值，适合历史数据导入，但若初始化数据过期或错误，会掩盖实时合同推算结果，且页面上不容易看出口径来源。

### 优化建议

- 拆分三类面积指标：签约出租率、起租出租率、计租出租率。首页默认可保留签约出租率，但月度经营分析建议同时展示起租/计租口径。
- 对合同结束日统一定义包含规则：建议按“合同结束当天仍视为有效”，月末到期合同当月计入出租率；如果不采纳，也应在指标说明中写明。
- 初始化数据只建议用于历史封账月份；当前年当前月应优先使用合同实时推算，并在数据模型中标记 `source=manual/imported/computed`。
- 增加空置面积拆解：可出租总面积、自用面积、已租面积、空置面积、即将到期面积，便于招商动作落地。

## 三、每月应收计算评估

### 现有计算模式

每月应收分为几层：

1. 合同账单生成  
   `generateBudgetedBills` 根据合同起止日期、月租金/单价、付款周期、首期收款、免租、账期偏移、提前退租、调价、付款周期变更等生成账单。

2. 园区账期偏移  
   上海/北京默认覆盖期前一个月产生应收，深圳为当月产生应收。当前逻辑写死在 `SAME_MONTH_RECEIVABLE_PROJECT_IDS`，默认非深圳都是前一个月。

3. 合同纯应收  
   `buildContractOnlyReceivableForPeriod` 是当前较清晰的单源入口，工作台、预算管理、财务页的合同应收都试图复用这个口径。

4. 财务核销应收  
   `buildBillingDetailsForPeriod` 在合同纯应收基础上叠加导入预算表覆盖、缓缴调入调出、特殊业态收入录入等后处理。

5. 预算执行目标  
   月度趋势中的 `revenueTarget` 优先使用导入预算表月合计，其次使用预算方案滚动应收；若初始化数据存在且 `revenueTarget > 0`，再由初始化值覆盖。

### 正确性判断

合同应收核心模型是比较完整的，尤其是以下设计比较合理：

- 合同纯应收与财务后处理分离，避免缓缴、手工行污染合同履约口径。
- 收款可按 `period` 关联账期分摊，支持一笔款覆盖多个账期。
- 免租支持扣减和顺延两种模式。
- 特殊业态不强行按合同滚动账单，而是走月度手工录入。
- 已有测试覆盖“导入预算表不制造幽灵应收”“缓缴不影响合同应收”等关键问题。

主要风险在口径命名和配置方式：

1. `revenueTarget` 名称容易误导。它有时是预算表目标，有时是生效方案滚动应收，有时被初始化值覆盖，并不等于“合同应收”。

2. 园区账期偏移写死在代码常量里。项目目标是园区经营看板，账期规则应属于园区配置，而不是代码分支。

3. 导入预算表只在合同已有应收时覆盖金额，避免幽灵行，这是谨慎的；但对于“合同漏录但预算表有明确应收”的场景，会被系统忽略，需要单独的数据质量提醒。

4. 特殊业态手工应收的 `contractAmountDue` 回落为 0，合同应收合计中不会体现这部分经营应收。若特殊业态是园区收入的重要组成，管理层看“合同应收”时可能低估当月应收。

### 优化建议

- 将收入指标命名分层：
  - `contractReceivable`：合同滚动应收。
  - `settlementReceivable`：核销口径应收，含缓缴、特殊业态、手工应收。
  - `budgetTarget`：预算目标或招商预算。
  - `cashReceived`：实际银行入账。
- 把园区账期规则迁移到园区配置，例如 `receivableMonthOffset`、默认付款周期、是否按当月产生应收等。
- 对导入预算表增加“未匹配/被忽略金额”校验报表：预算表有金额但合同无账单时，不自动入账，但应提示运营人员确认是合同漏录、账期错误还是预算误填。
- 特殊业态建议单独形成“经营应收”口径，不应硬塞进合同应收，但应在首页收入概览中与合同应收并列展示。
- 对每月应收增加可追溯明细：每一行显示来源标签，如合同滚动、预算表覆盖、缓缴调入、特殊业态、手工应收。

## 四、实收与收缴率评估

### 现有计算模式

项目存在两套实收口径：

- 工作台月度趋势：按真实银行入账日期归集，只统计 `Rent` 和 `DepositToRent`。
- 应收核销明细：优先按收款记录的 `period` 字段分摊到账期；没有 `period` 时才按收款日期所在月份匹配。

这两套口径分别服务现金流和账期核销，方向正确。

### 风险点

1. 首页 `collectionRate` 使用年度 `annualRevenueCollected / annualRevenueTarget`，分母是预算执行目标，不一定是合同应收或核销应收。

2. 月度趋势 `collectionRate` 用真实入账日期实收除以预算目标。若客户预付未来账期，本月现金流很好，但账期核销未必完成；若客户本月补缴历史欠款，本月收缴率会被抬高。

3. `PaymentRecord.status` 有 `Pending`，但当前多处统计没有过滤 `status === 'Received'`，若系统中存在待确认流水，可能被提前计入实收。

### 优化建议

- 首页同时展示两类收缴率：
  - 现金完成率：本月/本年银行实收 ÷ 预算目标。
  - 应收核销率：已核销到本账期金额 ÷ 本账期应收。
- 所有实收统计建议默认只计 `status === 'Received'`，待确认流水单独展示为“待确认收款”。
- 对预收、补缴、押金转租金进行单独归类，避免经营现金流和账期核销混为一个完成率。
- 财务页已经具备较好的核销口径，可将同类口径摘要同步到首页，而不是只展示现金口径。

## 五、累计欠款计算评估

### 现有计算模式

当前累计欠款从 2026 年 1 月开始，循环到当前系统月份的上一个月。每个月调用 `buildBillingDetailsForPeriod`，将状态为 `Unpaid` 的 `amountDue` 全额计入，将 `Partial` 的 `amountDue - amountPaid` 计入。

页面文案也写明为“2026年1月1日起所有未核销账单”。

### 正确性判断

这个计算能表达“系统内未核销账面余额”，对财务催收有价值；但作为园区经营看板的“欠款”还不够严谨。

主要问题：

1. 欠款起算固定为 2026-01，代码中存在读取最早初始化 12 月数据的变量，但实际未用于起算和期初欠款。这意味着 2025 年及以前欠款、初始化累计欠款字段不会进入当前欠款。

2. 欠款基于财务核销应收 `amountDue`，不是合同纯应收。因此缓缴调出会降低原账期欠款，调入会转移到目标账期。这对“催收待办”合理，但对“历史合同应收未收”不一定完整。

3. 欠款只算到上月，不含当月。对“逾期欠款”合理，但对“当前待收余额”需要另有指标承接。

4. 付款状态是否过滤 `Received` 的问题同样会影响欠款余额。如果待确认款被算入 `amountPaid`，欠款会被低估。

### 优化建议

- 拆成三个指标：
  - 历史逾期欠款：截至上月已到期未核销。
  - 当前待收：本月应收未收。
  - 累计应收余额：历史逾期 + 当前待收，可作为财务总待收。
- 恢复或明确废弃 `MonthlyInitData.accumulatedArrears`。如果用于期初欠款，应并入 2026-01 之前的期初余额；如果不用，应删除或标注不用，避免误会。
- 欠款计算应保存分解明细：客户、账期、合同应收、核销应收、已收、缓缴、逾期天数、最近跟进备注。
- 建议按账龄分桶：0-30 天、31-60 天、61-90 天、90 天以上，园区催收更实用。
- 欠款统计默认排除 `Pending` 收款，只用确认到账流水抵扣。

## 六、关键优化优先级

### P0：先明确指标口径

建立统一指标字典，至少包含：

- 出租率：签约出租率、起租出租率、计租出租率。
- 应收：合同应收、核销应收、预算目标、经营应收。
- 实收：银行实收、账期核销实收、待确认收款。
- 欠款：历史逾期、本月待收、累计待收。

这是最重要的优化。当前计算能力已经有了，但命名和页面表达还没有完全把口径讲清楚。

### P1：园区规则配置化

将目前写死的园区账期规则、特殊业态处理、起算年月、期初欠款等抽成园区配置。项目本身服务园区经营，未来不同园区规则不同，配置化比继续在代码中判断更稳。

### P1：欠款模型升级

累计欠款应从单一数字升级为“账龄 + 客户 + 账期 + 跟进状态”的催收模型。首页保留总额，财务页提供可钻取明细。

### P2：数据质量校验

增加自动审计：

- 预算表有金额但合同无账单。
- 合同有账单但预算表无金额。
- 收款未关联客户或客户名称模糊匹配。
- 收款有关联账期但金额跨多期均分可能不符合实际。
- 待确认收款被计入经营实收。

### P2：展示层提示来源

关键卡片和表格应显示来源标签，如“合同推算”“预算覆盖”“初始化覆盖”“银行流水”“账期核销”。这比单纯展示数字更适合经营决策。

## 七、最终评价

当前项目的运算逻辑总体是可用且方向正确的，尤其合同应收生成、账期核销、缓缴、特殊业态、预算方案快照这些能力已经具备真实园区经营系统的复杂度。最大问题不是“算不出来”，而是“不同业务口径混在同一个指标名下”。

如果下一步要提高看板可信度，建议优先做三件事：

1. 明确并固化指标字典。
2. 把园区账期、起算年月、期初欠款配置化。
3. 将欠款从总额升级为可追踪的账龄明细。

这样系统才能真正从“数据展示看板”升级为“园区经营实时决策看板”。
