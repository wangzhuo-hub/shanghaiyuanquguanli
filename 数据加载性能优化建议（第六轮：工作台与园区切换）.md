# 数据加载性能优化建议（第六轮：工作台与园区切换）

> 场景：项目打开后整体数据加载偏慢，**打开工作台**与**切换园区**两处最明显。
>
> 本轮以**当前最新代码精读**为准（非沿用旧轮假设）。前五轮的多数建议已落地，本文先明确"已经做了什么"，再定位"在新架构下仍然慢在哪"，最后按投入产出给策略。
>
> 结论分两档：**【已确认】**（基于源码精读）与 **【待核实】**（建议在最新代码上二次验证）。

---

## 一、先确认：已经落地的优化（不要重复造轮子）

精读 `App.tsx` 与 `services/*` 后，下列优化**已在现网代码中**，本文不再建议：

1. **运算后移已落地**：前端 server-first 调 compute 端点，`isServerComputeEnabled`（App.tsx:2008）控制；本地重算仅作 fallback，且计算引擎按需 `import()`（`loadDashboardMetricsModule` App.tsx:328/346）。
2. **路由级懒加载齐全**：`BuildingManager / ContractManager / FinanceManager / BudgetManager / SystemSettingsPanel` 及看板子组件 `BillingTable / RecentActivityTable / DashboardAlerts / AnnualMetricComparisonTable` 均 `React.lazy`（App.tsx:140-176）。
3. **打包分块**：`dist` 中 charts / xlsx / jspdf / excel / html2canvas 已各自独立 chunk。
4. **切 tab 结果缓存**：`metricsCacheRef`（App.tsx:2983/3000-3005）——数据引用+年+季度未变则跳过重算，从财务/合同切回工作台不再重算。
5. **切园区渐进式加载**：`switchProject`（App.tsx:3392）按 本地缓存 → KPI 快照预览 → 服务端 bootstrap → computed → 全量 backup 兜底 逐级 setData，先出可视再补齐。
6. **工作台已瘦身**：桌面工作台是轻量的 `FocusDashboard`（App.tsx:1690）——一个达成率圆环 + 出租率/欠款/到期 3 个数 + 自定义字段，**不在工作台首屏渲染 recharts 大图**；`StatsCards` 也已 `React.memo`（App.tsx:408）。
7. **账单明细延后挂载**：`showDashboardBillingTable` 用 `requestIdleCallback` 空闲后再挂（App.tsx:3008-3031），让 KPI 区先可交互。
8. **自动保存 diff 已 scoped + 缓存**：`dashboardDataToPbRecords + diffPbRecordsScoped`（App.tsx:2704-2721）按集合裁剪并按 `baselineRevision` 缓存快照。
9. **账单 identity key 基于内容**：`billingEffectDataIdentityKey`（services/billingEffectIdentity.ts:49）按集合内容生成，内容不变则账单明细 effect 不重建。

**因此"工作台慢"的病根，已不是 recharts 重渲染或缺 memo（前四轮主因，现已基本消解）。** 在新架构下，剩余卡顿主要落在**网络往返/服务端缓存命中**、**账单明细重建**、**主线程上的 hash 与序列化**这三条上。

---

## 二、打开工作台时仍然慢在哪（解剖）

切到工作台 tab 后依次发生：

**① 指标重算的服务端往返（【已确认】，首要）** — `shouldRunDashboardMetricsFilterEffect` effect（App.tsx:2991-3006）在非缓存命中时调用 `recalculateMetricsServerFirst`。命中 `metricsCacheRef` 时直接跳过；但只要数据有过改动、或刚切完园区、或首次进入，就要打一次 compute 往返。**若服务端 compute 缓存未命中（见第四节），这一次就是全量重算耗时，表现为"打开工作台转一下"。**

**② 账单明细构建是单次最重的一块（【已确认】）** — 账单 effect（App.tsx:3079-3155）在 `dashboardBillingKey`（含 `billingEffectDataIdentityKey`）变化时重建：server 走 `fetchCloudComputedBilling`/草稿，本地 fallback 走 `buildBillingDetailsForPeriodLocal`（App.tsx:341）。问题是 `dashboardBillingKey` 里的 identity key 一旦因任何编辑或换园区而变，就整段重建；且 `billingEffectDataIdentityKey` 本身要在**主线程**对 buildings/tenants/payments/... 全集合算一遍 hash（大园区即一笔可观主线程开销）。

**③ 轻量组件的非必要重渲染（【已确认】，次要）** — `FocusDashboard`（App.tsx:1690）是裸 `React.FC` 未 memo，且收**整个 `data`** 与 `isSyncing` 等易变 prop；`DashboardCustomFields`（App.tsx:1323）同样收整个 `data`。任何无关 setData（同步标志、自动保存状态）都会让它们连同自定义字段派生重算一遍。单次不贵，但叠加 ①② 与切园区的多次 setData 会放大。

---

## 三、切换园区时仍然慢在哪（解剖）

`switchProject`（App.tsx:3392-3551）：

**① 网络往返链偏长（【已确认】，首要）** — 顺序推进：连接检查 → KPI 快照 → bootstrap → computed → 全量 backup。理想路径（bootstrap 直接返回算好的看板）只需 1–2 跳即 `return` 短路；**但凡 bootstrap/computed 落空，就要走 `fetchCloudBackup` 全量拉取**（App.tsx:3495），大园区（如北京几百租户）payload 大、解析慢，再叠加本地 fallback 重算。

**② 首切无本地缓存则无即时预览（【已确认】）** — 即时预览依赖 `parkDataGet`（App.tsx:3426）命中本地缓存；第一次切到某园区时缓存为空，只能等 KPI 快照或 bootstrap 回来才有画面。

**③ 多次 setData → 多次整树渲染（【已确认】，次要）** — 缓存预览、快照预览、最终结果各 setData 一次（App.tsx:3452/3463/`applyComputedDashboardLoad`）。树已瘦身，单次渲染不贵，但与 ②③（第二节）叠加仍有感。

**④ 新园区服务端缓存大概率是冷的（【待核实】）** — 切到一个许久没人访问的园区，服务端 compute 缓存可能已失效/未预热，bootstrap 这一跳就退化成"服务端全量重算 + 回传"。

---

## 四、跨场景的真正放大器：服务端 compute 缓存命中率（【待核实】，最高杠杆）

第②轮《运算后移》文档 2.4 已指出 `scripts/compute-engine.ts` 的缓存**读键与写键可能不对称**：读用 `dashboard_data_version` note，写用 `rawData.cloudSaveVersion ?? cacheVersion`。两者一旦短暂不同步，会出现**写入键 ≠ 读取键 → 缓存恒 miss**。

这一条对"开/切都慢"是**总开关级**的影响：客户端无论把渐进式加载做得多好，每一跳最终都要等服务端返回；**若服务端缓存命中率低，每次打开工作台、每次切园区都在等一次全量重算**，前端所有优化都被这一层抵消。**强烈建议优先量化命中率**——这是性价比最高、且可能一改就普遍提速的点。

---

## 五、优化建议（按投入产出排序）

### P0-1 量化并修复服务端 compute 缓存命中（对应四、三④、二①）
- 在 `compute-engine` 的 dashboard/billing 计算入口加一行命中日志（`cache hit/miss + key`），先**量化命中率**；
- 统一缓存 version 唯一来源（读写都用 backup 内 `cloudSaveVersion`，或都用 note），补一条"写后即读命中"单测；
- 对热门园区做**缓存预热**（写入成功后或定时把当前年月的 dashboard/billing 结果算进缓存）。
- 预期：开/切若此前在等冷算，普遍提速；改动集中在服务端，前端零风险。

### P0-2 园区数据：常驻 + 预取 + 持久缓存（对应三①②④）
- **MRU 常驻**：最近访问的 N 个园区的最终 `data` 保留在内存 Map，切回**瞬时**出图，不再走网络链；
- **悬停预取**：园区切换器 `onMouseEnter` 时预发 KPI 快照 + bootstrap（App.tsx:3427/3435 已有函数，提前触发即可），点下去时多半已就绪；
- **持久化各园区本地缓存**：确保每次成功加载都 `parkDataPutObj`（App.tsx:3520 已有），让"首切某园区"也能先吃上一版缓存预览。
- 预期：切园区从"等一条网络链"变成"秒切 + 后台校正"。

### P0-3 账单明细加版本级缓存（对应二②）
- 在账单 effect 之上加一层 `Map<`projectId|month|cloudSaveVersion`, rows>`：key 未变直接复用，切走再切回 / 关掉再开工作台都不重建；
- `billingEffectDataIdentityKey` 的全集合 hash 移出主线程（Worker 或 idle 分片），或在"内容未脏"时用 `cloudSaveVersion` 直接当 key 跳过 hash（脏标记 `dirtyPbCollectionsRef` 已存在，App.tsx:3063）。
- 预期：消除"每次开工作台/切园区都重算账单"，这是单次最重的一块。

### P1-1 memo 工作台轻组件 + 收窄 props（对应二③）
- 给 `FocusDashboard`、`DashboardCustomFields` 包 `React.memo`，props 从整个 `data` 收敛为各自用到的切片（达成率/出租率/欠款/到期、自定义字段所需数组）。
- 预期：同步标志、自动保存等无关 setData 不再触发工作台派生重算与重渲染。

### P1-2 主线程减负：hash 与 autosave diff 后移（对应二②、自动保存）
- `dashboardDataToPbRecords + diffPbRecordsScoped`（App.tsx:2704-2721）虽已 scoped+缓存，仍在主线程；与 identity hash 一并挪进 `requestIdleCallback` 或 Worker，消除"打开后过两秒再卡一下"。

### P1-3 switchProject 链路收敛（对应三①③）
- 确认 bootstrap 命中即 `return` 短路（已具备），并在快照预览与最终结果"将在 ~100ms 内到达"时合并中间 setData，减少整树渲染次数；
- 全量 backup 兜底加并发/超时与"仅拉变更"上限，避免大园区整包阻塞。

### P2-1 首屏冷启动 bundle 瘦身（仅影响首次打开，不影响切 tab/切园区）
- 确认 `excel(917KB) / xlsx(419KB) / jspdf(380KB) / html2canvas(198KB)` 仅在点击"导出/打印"时 `import()`，不进首屏关键路径；
- 工作台已不用 recharts，确认 `charts(424KB)` chunk 不被首屏 eager 引入（仅 ContractAnalysisCharts/ContractManager 等懒加载路径用）。

### P2-2 清理 dead 重组件
- `StatsCards` 已 `React.memo` 但现网工作台未见渲染（`<StatsCards` 在 `App.tsx` 无引用）；若确为历史遗留，连同其它未用重组件移除，减小解析与包体。

### P2-3 可观测性补全
- 现有 `[metrics] recalculate XXXms`（App.tsx:4751）之外，补：compute 端点耗时与缓存命中率、billing 构建 ms、`switchProject` 各阶段耗时。**有数据才能确认杠杆点，避免优化错位。**

---

## 六、先量化再动手（强烈建议）

改前先确认各环节占比：

1. **看现成日志**：DevTools Console 过滤 `[metrics]`，记录"打开工作台"与"切园区"时的 `recalculate XXXms`，并把 tenants/payments 数量一并记下；
2. **Network 面板**：切园区时数一数实际打了几跳、哪一跳最慢（是否落到全量 `fetchCloudBackup`）；
3. **服务端命中率**（P0-1 落地后）：看 compute 缓存 hit/miss——这是判断"慢在网络等冷算 vs 慢在前端"的关键分水岭；
4. **Performance 面板**：录一次操作，看 Long Task 落在 计算 / React commit / JSON 序列化+hash 哪一段。

判读：`[metrics]` 小但仍卡 → 病根在网络/服务端缓存（P0-1/P0-2）或账单重建（P0-3）；`[metrics]` 本身数秒 → 命中了本地 fallback，回到运算后移与缓存命中。

---

## 七、优先级总表

| 顺序 | 事项 | 预期效果 | 工作量 | 对应 |
|---|---|---|---|---|
| 1 | P0-1 服务端 compute 缓存命中量化+修复+预热 | 开/切若在等冷算则普遍提速（总开关级） | 1–2 天 | 四 |
| 2 | P0-2 园区常驻 + 悬停预取 + 持久缓存 | 切园区秒切，首切也有即时预览 | 1–2 天 | 三①②④ |
| 3 | P0-3 账单明细版本级缓存 + hash 后移 | 消除单次最重的账单重建 | 1 天 | 二② |
| 4 | P1-1 memo 工作台轻组件 + 切片 props | 无关 setData 不再触发派生重算 | 半天 | 二③ |
| 5 | P1-2 autosave/hash 移出主线程 | 消除"打开后两秒再卡一下" | 半天 | 自动保存 |
| 6 | P1-3 switchProject 链路收敛 | 减少整树渲染与大包阻塞 | 半天 | 三①③ |
| 7 | P2 bundle 瘦身 / 清 dead 组件 / 可观测性 | 首次冷启动更快、可持续定位 | 1–2 天 | 五 |

---

## 八、验证建议（落地前后各跑一遍）

```bash
npx tsc --noEmit -p tsconfig.json   # 类型与语法
npm test                            # services/__tests__ 现有用例
npm run build                       # 产物体积 / 构建可用性
npm run check:pb                    # 防误提交 PB 运行时库
```

重点补测：P0-1"写后即读命中"单测、P0-3 账单版本级缓存的 key 命中/失效用例。

---

## 九、本文结论与局限

- **工作台已瘦身、运算已后移、懒加载与结果缓存齐备**——前四轮"渲染侧/重算"主因基本消解。
- 在新架构下，开/切的剩余卡顿主要由**服务端缓存命中率、账单明细重建、主线程 hash/序列化、园区切换网络链**决定；其中 **P0-1（服务端缓存命中）杠杆最大**，建议先量化再改。
- 标【待核实】项（四、三④）请结合最新 `compute-engine.ts` 与一次完整 `tsc`/`test` 二次确认后再动手。
