# 代码优化建议（第二轮）

基于第一轮建议落地后的代码复查。先核对落地情况，再指出**本轮改动新引入的问题**（部分是会丢数据的正确性 bug，优先级高于一切性能优化），最后列出仍未完成的上一轮事项。

## 一、落地情况核对

| 第一轮建议 | 状态 | 备注 |
|---|---|---|
| 保存互斥队列 | ✅ 已做 | 但 waiter 释放有 bug，见 2.3 |
| PB batch 保存 | ⚠️ 已做 | 实现有 4 处严重问题，且服务端版本不支持，见 2.1 |
| 收款/发票按年加载 | ⚠️ 已做 | 只用在基线重建处，与全量数据不一致，见 2.2 |
| localStorage → IndexedDB | ✅ 已做 | 双写设计合理；boot 读取路径有遗留问题，见 2.5 |
| 季度趋势改切片 | ✅ 已做 | `sliceQuarterFromFullYear`，实现正确 |
| contractReceivable 结果缓存 | ⚠️ 已做 | 模块级缓存 key 会跨调用方串号，见 2.4 |
| refreshAfterSave 增量化 + 降频 | ✅ 已做 | 自动保存走 applied 增量、每 10 次/手动才全量，方向正确 |
| 长表虚拟滚动 | ⚠️ 半成品 | `VirtualizedTable.tsx` 已创建但**没有任何组件引用它** |
| 原始/派生数据分离 | ❌ 未做 | `recalculateMetrics` 仍 `setData(processedData)` |
| 欠款历史月封账 | ❌ 未做 | 欠款循环仍从 2026-01 逐月全算（dashboardMetrics.ts:1438） |
| Web Worker 重算 | ❌ 未做 | |
| pb_hooks 服务端 CAS | ❌ 未做 | |

---

## 二、本轮新引入的问题（P0，建议立即修复）

### 2.1 batch 保存路径有 4 处问题，其中 1 处会静默丢数据

位置：`pocketbaseService.ts` `saveBatchToPocketBase`（2309-2459）。

**① notes 行被替换成空 PATCH，改动永久丢失（最严重）。**
2380-2388 行：billing_period_notes 行的 `fieldsToWrite` 被置为 `{}`（注释说"需要在串行路径做合并"），但请求**仍然被 push 进 batch**。后果链：空 PATCH 成功 → 该行被记入 `applied` → 调用方 `applied.length>0 && errors.length===0` 直接返回，串行路径永不执行 → `applyPartialIncrementalSave` 把"已写入"的 notes 快照刷进 baseline → 下次 diff 认为无变化。**缓缴、特殊业态收入、手工应收、跟进备注的修改就此丢失且无任何提示。** 修复：notes 行直接不进 batch（continue），并在 batch 返回后对"未进 batch 的残余 payload"补跑串行路径。

**② batch 路径没有乐观锁。**
Phase 1 已查回每行 `updated`（2347 行 fields 里有），但从未与 `recordMeta` 比对；`baseUpdated`（2379 行）算出来后没有使用。batch 一旦真正启用，所有并发冲突检测失效，他人改动被静默覆盖，ConflictDialog 变成死代码。修复：Phase 1 拿到 `updated` 后逐行与 baseUpdated 比对，不一致的行从 batch 中剔除并记入 conflicts（保持与串行路径同语义）。

**③ 查不到 pbId 的 update/delete 被静默吞掉。**
2378/2395 行 `if (!pbId) continue`——若 Phase 1 的 OR 查询失败（2352 行 catch 后什么都不做），这些操作既不在 batch 里也不会进 errors；batch 成功返回后调用方直接收工，**这部分改动丢失**。修复：被跳过的操作必须返回给调用方（残余 payload），由串行路径兜底。

**④ 服务端根本不支持 /api/batch。**
`pocketbase/CHANGELOG.md` 顶部是 **v0.22.0**，`/api/batch` 是 PB 0.23 才加入的，且默认关闭（需在 Admin UI → Settings → Application 里启用并设置 max requests）。当前线上每次保存的实际行为是：Phase 1 多次 getFullList 预查 + 一次注定 404 的 batch 请求 + 回退串行——**比改造前更慢**。修复：升级 PB 二进制到 ≥0.23、启用 Batch API；并在前端探测一次能力（404 后本会话不再尝试 batch）。

另两处小问题：Phase 1 的 OR filter 把所有 originalId 拼进一个 GET 查询字符串，改动几百行时会超 URL 长度上限，需按 50~100 个一批分片；`batchResp.ok` 未检查（靠"非数组→空结果→applied=0→回退"侥幸兜住，建议显式判断）。

### 2.2 按年过滤只用在基线重建，制造了基线与数据的系统性错位

启动加载（App.tsx:614）、登录、切园区都是**全量**拉取；而手动保存/每 10 次自动保存后的基线校准（App.tsx:1390）用 `{ year: selectedYear }` **过滤后**的数据重建 baseline。

失败链：手动保存一次 → baseline 里只剩当年收款/发票 → 2 秒后自动保存 diff(全量 data, 当年 baseline) → **所有历史年份收款被判为 creates**（数千条）→ batch Phase-1 巨型 OR 查询超长失败 → 回退串行 → 每条 create 撞唯一索引 → duplicate-key 又触发一次 findOne 兜底查询 → 每次自动保存都是一场数千请求的风暴，且 recordMeta 缺失历史行，期间真正的冲突检测全部失效。

修复方向（二选一，关键是**两侧口径必须一致**）：
1. 短期：基线校准去掉 `{ year }`，恢复全量（牺牲该次拉取的体积，先保正确性）；
2. 长期（推荐）：加载与基线统一按年窗口（如当年+上一年），`data.payments` 只保留窗口内数据；窗口外的欠款/趋势依赖封账快照（见 3.2），diff 的 deletes 判定限制在窗口内。

### 2.3 互斥锁 waiter 路径释放错位，争用一次即卡死 30 秒

`releaseSaveLock`（App.tsx:367-387）把锁转交给等待者时，holder 被设为 `label + '/waiter'`；但等待者（`runCloudSave`）finally 里用自己的 `saveLabel` 释放（1582 行），holder 不匹配 → 释放变 no-op → 锁要等 30 秒超时才放开。表现为：自动保存与手动保存碰一次后，后续保存全部"队列繁忙"半分钟。

修复：`acquireSaveLock` 返回一个 release 函数（闭包持有真实 holder 标识），调用方 finally 里调它，而不是凭 label 字符串对账。顺带可把 `saveLabel = note` 改成固定 'manual-save'——note 含时间戳，目前每次锁标签都不同，纯属调试噪音。

### 2.4 contractReceivable 模块级缓存会跨调用方串号

`_contractReceivableCache`（dashboardMetrics.ts:61）是模块级 Map，但 key 里的 contextId 来自**每个 BillingCache 实例自己的自增序号**（都从 1 开始）。`buildContractOnlyReceivableForPeriod` 除了被 `calculateDashboardMetrics` 调用（开头会 clear），还被 `BudgetManager.tsx:744` 用**自建的临时 cache** 直接调用——两个调用方的 key 形如 `2026|3|cr|1|2`，完全可能撞车：预算管理浏览方案快照时，可能直接拿到工作台 Live 口径的缓存结果，**数字是错的**。

修复：把这个结果缓存从模块级移进 `BillingCache` 实例（与 `detailsByKey` 同级），天然隔离调用方，也不再需要 `invalidateContractReceivableCache` 和 500 条上限。

### 2.5 boot 仍同步读 localStorage，配额满后会用陈旧缓存参与合并

写入已是 IndexedDB 主、localStorage 兜底（quota 满时静默放弃，App.tsx:396-400），但启动路径用的是 `parkDataGetSync`——只读 localStorage。一旦数据量大到 localStorage 写不进去，boot 读到的就是**最后一次写成功的旧快照**，而 `mergeLocalDashboardCacheIntoCloud` 对任意 notes key 是"本地不同即覆盖云端"（localCloudMerge.ts:48-51）——旧的跟进备注/缓缴会被复活。

修复：boot 的 `loadData` 本来就是 async，把 `parkDataGetSync` 换成 `await parkDataGet`（IndexedDB 优先）即可，没有理由保留同步路径。localStorage 兜底写建议同时砍掉派生字段以延缓配额（或干脆只写 IndexedDB + 内存）。

---

## 三、上一轮未完成、仍然建议做的事项

3.1 **原始/派生数据分离**：`setData(processedData)` 仍把 monthlyTrends、currentMonthBilling、recentSignings 等塞回 `data`。它现在还放大了 2.5 的问题（缓存体积）和每次持久化的 stringify 成本。这是 Worker 化的前置条件，建议下一轮优先。

3.2 **欠款历史月封账**：欠款循环原样保留（2026-01 起逐月），且是 2.2 长期方案（按年窗口加载）的前置依赖——不做封账，按年加载就动不了。两者应作为一个整体设计：每月初由服务端（compute-engine 已有雏形）把上月应收/欠款定格成快照行。

3.3 **VirtualizedTable 接进真实表格**：组件已写好但零引用。优先接收款明细与应收核销两张最长的表。

3.4 **手动保存仍同步全量重算**：`refreshAfterSave` 的 KPI 快照分支（App.tsx:1363-1369）在锁内同步跑 `quickMode:false` 全量计算，手动保存时 UI 仍会冻结一下。短期可挪到 `runWhenBrowserIdle`，长期随 Worker 化解决。

3.5 **pb_hooks 服务端 CAS**：batch 修好乐观锁（2.1②）后，剩余的 check-then-write 窗口仍只能靠服务端钩子彻底关闭；升级 PB 到 0.23+ 时一并做。

---

## 四、优先级

| 优先级 | 事项 | 性质 |
|---|---|---|
| P0 | 2.1① notes 空 PATCH（丢数据） | 正确性 |
| P0 | 2.2 基线按年过滤与数据错位（请求风暴+冲突检测失效） | 正确性 |
| P0 | 2.3 互斥锁 waiter 释放 bug（保存卡死 30s） | 正确性 |
| P0 | 2.4 contractReceivable 缓存串号（数字算错） | 正确性 |
| P1 | 2.1②③④ batch 乐观锁/残余兜底/PB 升级启用 | 正确性+性能 |
| P1 | 2.5 boot 改读 IndexedDB | 正确性 |
| P1 | 3.1 派生分离 + 3.3 虚拟表接入 | 性能 |
| P2 | 3.2 封账快照（与按年加载联动）、3.4 Worker、3.5 服务端 CAS | 架构 |

**补充建议**：本轮 batch、互斥、按年过滤三处改动都没有对应测试（`__tests__` 里无相关用例）。修复 P0 时请同步补：① batch payload 构建的单测（notes 行不得进 batch、跳过项必须返回残余）；② "保存后基线与当前数据 diff 应为空"的回归测试——这一条能同时拦住 2.1① 和 2.2 这类基线污染问题。
